# Run with `dart run ffigen --config ffigen.yaml`.
name: JetCadBindings
description: |
  Bindings for `src/jet_cad.h`.

  Regenerate bindings with `dart run ffigen --config ffigen.yaml`.
output: 'lib/jet_cad_bindings_generated.dart'
headers:
  entry-points:
    - 'src/jet_cad.h'
  include-directives:
    - 'src/jet_cad.h'
comments:
  style: any
  length: full
