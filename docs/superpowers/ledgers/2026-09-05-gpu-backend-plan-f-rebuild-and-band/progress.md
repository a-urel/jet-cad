# SDD ledger — plan: docs/superpowers/plans/2026-09-05-gpu-backend-plan-f-rebuild-and-band.md

Spec: docs/superpowers/specs/2026-08-29-gpu-resident-render-backend-design.md (revision 5, d2095e7 + Plan E's Ruling E9 word), trigger table, collection section, watermark section, seam, budgets, invariant 1, testing, criteria 2/5/7/9/10/12, open question 3.
Worktree: .worktrees/plan-f-rebuild-and-band, branch plan-f/rebuild-and-band, cut from main at c5b8ee8 (plan committed on main at 88e3f01, pre-flight corrections at c5b8ee8).

## Pre-flight scan (2026-09-05)

| pair / task | produces vs consumes | found |
|---|---|---|
| T1 / T2 (ResidentCollection.collect) | T2.rebuildNow calls collect(document, painter, live, devicePixelRatio, pixelsPerPaperMm, lineweightScale, measurer, textStyleOf, bandLowerScale); T1 defines exactly that | consistent |
| T1 / T5 (classifyTextPatches) | T1 calls (data, count, texts, {devicePixelRatio, bandLowerScale}); T5 adds an optional `stats` and keeps the rest | consistent |
| T1 / T7 (collectionFrameFor, CollectionFrame.camera/viewport) | T7's ResidentZoomRig and band sweep read .camera/.viewport | consistent |
| T2 / T3 (ResidentRebuilder ctor, noteFrame(camera, viewport, dpr, tablesRevision), markDirty, backend, collection, uploadFailed, disposed, landed, lastTrigger, bandStaleFrames) | T3 reads every one of these; T2 defines them (disposed is a getter over _disposed) | consistent |
| T2 / T2 (gpu_draw_backend.dart imports resident_rebuilder.dart, which imports gpu_draw_backend.dart) | a library import cycle | legal in Dart; no part files, no top-level initialisation order dependence | consistent |
| T2 / T4 (uploadFailed, backend null after a null upload) | T4 asserts both | consistent |
| T2 / T8 (widgetRebuilder.landed/lastWalkMicros/lastClassifyMicros/lastUploadMicros/lastTotalMicros/lastTrigger/collection/bandStaleFrames assignable) | T8 reads all, ASSIGNS bandStaleFrames = 0 — T2 declares it a public mutable int | consistent |
| T3 / T8 (DraftCanvasState.resident, DraftCanvas(key: GlobalKey<DraftCanvasState>)) | T8 reads resident through the key | consistent |
| T5 / T6 (text_patches.dart edited by both) | T5 adds the grid + oracle; T6 makes PatchRegion mutable and adds `out` to patchRegionFor — disjoint regions of the file | consistent |
| T6 / T7 (gpu_comparison.dart) | T6 does not touch it; T7 adds `uncovered` (6th positional) and `collectionViewport:` — gpu_comparison calls patchRegionFor without `out` and constructs PatchImage unchanged | consistent |
| T6 / T2 (GpuDrawBackend implements ResidentFramePainter) | T2 adds the `implements` + @override; T6 edits render's body | consistent |
| T3 self | `_repaint` list uses collection-if; `resident` assigned before `_changes`' onChange closure captures it; residentUploader excluded from didUpdateWidget so re-pumping a fresh tear-off does not re-attach; tileCache built only when resident == null — existing draft_canvas_test.dart never enables the GPU so its tiles tests keep their path | consistent |
| T2 self | band probe test: 1.005/2.01 is 0.5 in real numbers — a coin toss in doubles | **Ruling P1** (fixed in the plan at c5b8ee8) |
| T4 self | tests swapped FlutterError.onError with addTearDown restore — the test binding checks onError at the end of each test body, before tearDowns run | **Ruling P2** (fixed at c5b8ee8) |
| T5 self | `stats.overflow > 0` on the generated corpus assumed a room wall spans > 16 label-sized cells; rooms are 30–120 units, labels hundreds | **Ruling P3** (fixed at c5b8ee8) |
| T7 self | zoom-out 0.94^12 = 0.476 < 0.5 → exactly one in-rig rebuild; zoom-in 1.02^12 = 1.27 → none; TileRig has dispose() (tile_fixture.dart:138) | consistent |
| T8 self | phase() gains `frameCount` for the 40-frame band exit; backendOf(a) replaces state.backend at both read sites; fireDocumentTrigger's constants (kDefaultOriginX, kOriginY, kFloorWidth, kFloorHeight) are the ones _addPatchedLabels reads | consistent |
| Global "no shader change" vs every task | no task touches shaders/ or assets/ | consistent |
| Global "DraftPainter gains no API" vs T1 | the frame is a camera + viewport handed to paint | consistent |
| Global "vm_service harness-only" vs T8 | pubspec of apps/dev_harness_2d only | consistent |

Ruling P1: the rebuilder band test probes 1.1 (ratio 0.547) inside the 2.01x collection's band instead of 1.005 (ratio 0.5 exactly) — because an assertion on an exact floating-point edge is a coin toss, not a test — costs nothing if wrong.
Ruling P2: criterion 10's widget tests observe the one-shot report through `tester.takeException()` rather than by swapping `FlutterError.onError` — because the test binding verifies onError at the end of the test body and a tearDown restore runs too late — costs the `library` field assertion (takeException returns the exception alone), which `debugResidentFallbackReports` covers.
Ruling P3: the grid differential's generated corpus gains one line across the whole extents so `stats.overflow > 0` is a fact — because the generated rooms are 30–120 units and cannot span 16 label-sized cells — costs nothing if wrong (the differential itself is unchanged).

## Execution

Task 1: implementer ac467b06d007e1ec5 (sonnet) DONE -- commit 9cb10f9, base c5b8ee8; 629/629, analyze clean, format clean; mutation (cull to live viewport) fired red and restored. Formatter run on the four touched files (brief code not 80-col clean).
Task 1: reviewer a48dbe6b301735287 (sonnet): spec ✅, quality Approved. Minor (deferred, plan-mandated): resident_collection_test "the timings are read" asserts walk/classify micros >= 0 -- true of any Stopwatch, cannot catch a field wired to a constant.
Task 1: complete (commits c5b8ee8..9cb10f9, review clean)
Task 2: implementer a6a38e6b21206b8a8 (sonnet) DONE -- commit 061fa67, base 9cb10f9; 639 pass, analyze clean, format clean; one unnecessary dart:ui import dropped, formatter run.
Task 2: reviewer ae604e8fe42571210 (opus): spec ✅, quality Approved-with-one-Important. Important (plan-mandated): resident_rebuilder_test:83-84 M-F6 comment names "schedule a callback per markDirty -> rebuilds 4" but _run's `_pending == null` early return absorbs the extra callbacks, so rebuilds stays 2 -- the label overstates coverage. Fix: a `schedules` counter incremented in _schedule, asserted == 2 in that test (initial + document), comment corrected.
Task 2: minor (deferred): a throwing uploader/collect escapes _run as an unhandled async error (uploadResidentCollection cannot throw for GPU reasons -- create catches); Task 3's production wiring should consider routing a throw to uploadFailed.
Task 2: minor (deferred): band bounds only exercised at defaults; inverting the ratio is an equivalent mutation under the symmetric [0.5, 2.0] band -- one asymmetric-band case would pin direction and parameters.
Task 2: minor (deferred): dispose does not null _collection.
Task 2: minor (deferred): public rebuildNow unguarded against overlapping a scheduled _run -- doc it (the harness's trigger phase in Task 8 fires triggers and waits for landing; it does not call rebuildNow directly).
Task 2: minor (deferred): bandStaleFrames keeps climbing after uploadFailed.
Task 2: fix round 1/5 dispatched -- resumed implementer a6a38e6b21206b8a8 with the M-F6 finding (schedules counter).
Task 2: fix round 1/5 (1 addressed, 0 open -- schedules counter witnesses M-F6; commits 061fa67..97eccb9). Re-reviewer a9175bf8b7a5b0720 (sonnet): ADDRESSED, no new breakage.
Task 2: minor (deferred): resident_rebuilder.dart:143-145 doc "a value above landed means marks were not coalesced" overreaches -- dispose during flight also leaves schedules > landed.
Task 2: complete (commits 9cb10f9..97eccb9, review clean after 1 fix round)
Task 3: implementer aa641dbcfb0c372b2 (sonnet) dispatched at base 97eccb9; cut off by a provider rate limit before running tests (edits left in tree); resumed 2026-09-06 on the user's "Try again".
Task 3: implementer NEEDS_CONTEXT -- (a) brief's assertion `uploader.painters isEmpty` right after the first pumpWidget is false: the first paint's noteFrame registers a post-frame callback that fires inside the SAME handleDrawFrame, and the ungated FakeUploader completes synchronously, so the rebuild lands inside pumpWidget; (b) _DraftCustomPainter.shouldRepaint == false meant a dpr-only or re-attach-only rebuild never painted, so the dpr trigger and a re-attach's initial trigger were unreachable; (c) debugSetGpuAvailable was never barrel-exported; (d) render_backend_test's two residentGpu-without-GPU tests now hit the one-shot FlutterError report.
Ruling F3-a: the first-frame test asserts the truthful form -- after pumpWidget alone, `resident.landed == 1` AND `uploader.painters.single.paints == 0` (the frame drew before the landing, through vertices; the landing asked for a frame not yet pumped), then after land(t) `paints >= 1` -- because the same-frame post-frame landing IS Ruling F3 working (the frame never waited; the rebuild ran after paint), and the brief's `isEmpty` was a test-timing claim, not a design property — costs nothing if wrong; the property "the frame draws whatever is current" is what is asserted.
Ruling F3-b (plan erratum): `_DraftCustomPainter.shouldRepaint` returns `old.resident != resident || old.devicePixelRatio != devicePixelRatio` when resident != null, false otherwise — because a delegate whose only change is a field is never painted by RenderCustomPaint unless shouldRepaint says so, and the dpr trigger reads that field on the frame — costs one extra paint on a dpr change or re-attach, which is exactly the frame the trigger needs. Pre-existing and out of scope: the non-resident path also never repaints on a dpr-only change (vertices?.devicePixelRatio is set in build but no frame follows) — deferred to the final review.
Ruling F3-c: the barrel exports `gpu_facade.dart` with `show debugSetGpuAvailable` only — the wildcard re-export the barrel comment refuses stays refused.
Ruling F3-d: render_backend_test.dart's two residentGpu fallback tests may reset the latch and observe/absorb the one-shot report; the reviewer checks the mechanism is takeException/expect-driven where the test is a testWidgets, and not a silent swallow that hides a second report.
Task 3: implementer committed 03a7b31 after the rulings (base 97eccb9); review package built.
Task 3: reviewer adcce91fe02645c51 (opus): spec ✅ (as amended by F3-a..d), quality Approved. ⚠️ upload-failure half of F5 untested here — Task 4 covers it.
Task 3: minor (deferred): draft_canvas.dart:222-223 and :506 doc comments still say "until Plan F" — reword to the F5 fallback sentence.
Task 3: minor (deferred): DraftCanvas.tiles doc does not say the flag is ignored on a residentGpu canvas.
Task 3: minor (deferred): render_backend_test.dart:71 takeException assert is host-coupled (assumes no GPU); guard on resolveBackend != residentGpu. Stray empty `//` at :60.
Task 3: minor (deferred): draft_canvas_resident_test.dart:61-63 land() doc contradicts the first test's same-frame landing — describes steady-state pumps only.
Task 3: minor (deferred, plan-mandated F3-b): shouldRepaint guard on the NEW resident leaves the last GPU frame on screen when backend switches away from residentGpu; `old.resident != resident` alone would close it. Controller owns the choice — revisit in the final review.
Task 3: complete (commits 97eccb9..03a7b31, review clean; NEEDS_CONTEXT round resolved by Rulings F3-a..d)
Task 4: implementer a332ad87896c15e6f (sonnet) DONE -- commit e7b0b53, base 03a7b31; 3/3 new tests green first run, no production change; 648 pass, analyze/format clean.
Task 4: reviewer a3a486c0d55a2b00d (sonnet): spec ✅, quality Approved, no findings.
Task 4: complete (commits 03a7b31..e7b0b53, review clean)
Task 5: implementer a9e5f304da98c8b50 (sonnet) DONE -- commit 9612142, base e7b0b53; 651 pass; CLASSIFY grid=16.8 ms brute=31.6 ms on the 3000-entity test corpus (instances=46550 labels=160 tested=34455 of 7.4M pairs, overflow=1) -- only 1.9x under flutter test JIT; the device number (Task 8) is the one criterion 7 reads.
Task 5: reviewer a94344224ab038851 (opus): spec ✅, quality Approved-with-one-Important. Important (plan-mandated): M-F10 (`.floor()` -> `.round()` in cellX/cellY) is EQUIVALENT -- one closure pair serves both binning and lookup, round is monotone, the grid stays conservative -- so the mutation named in the test cannot go red.
Ruling F7-a: M-F10 is REPLACED by a non-monotone mutation of the instance side only -- `cx1 = cx0` at the binning site (bin by the min corner) -- because the plan's mutation set must contain a red-capable witness for the cell arithmetic and a monotone shift of the whole grid is provably equivalent; the old M-F10 is recorded in the mutation log as equivalent, not deleted (spec's own rule) — costs nothing if wrong.
Task 5: minor (deferred): NaN/inf coordinates throw in `.floor()` where the oracle returns false; inverted label box yields no bucket hits (precondition undocumented); `_reaches` doc lacks the reciprocal "change _expandedBox too" warning; `tested < pairs/2` guard uses the upper-bound denominator; overflow branch exercised by exactly one all-extents instance; report's test counts do not reconcile (28 vs "29/29", "11 tests" vs 19).
Task 5: diagnosis (reviewer, reported): the grid's algorithmic work is ~2 ms of the 16.8 ms under flutter test; the rest is un-inlined closure calls (cellX/cellY capture five variables, ~190K calls) plus unoptimised-second-call JIT; cell = max label box collapses the grid when one label is oversized. Device number in Task 8 decides; closure hoisting is a cheap lever if it misses.
Task 5: fix round 1/5 dispatched -- resumed implementer a9e5f304da98c8b50 with Ruling F7-a.
Task 5: fix round 1/5 (1 addressed, 0 open -- M-F10 recorded equivalent, new witness `cx1 = cx0` at the binning site fired red 160 vs 80; commits 9612142..ca6bda2). Re-reviewer a038a91bb067273ed (sonnet): ADDRESSED, no new breakage.
Task 5: complete (commits e7b0b53..ca6bda2, review clean after 1 fix round)
Task 6: implementer a030e08de2b36ef3b (sonnet) DONE -- commit 8b1427e, base ca6bda2; 654 pass; inverted-rejection mutation killed by the new test and text_order_test.
Task 6: reviewer a7d90ccdc6ebf37e9 (opus): spec ✅, quality Approved-with-two-Important (both plan-mandated test-strength gaps): (1) frame_info_test's `out` test compares a zeroed block to a zeroed block -- pre-dirty `out` with 0xFF so "all twenty floats written" is guarded; (2) text_compositor_viewport_test exercises only the off-left and below sides and only translations -- add off-right and above labels (an x/y-swapped rejection ships green today) and one non-translation transform. Risks (a) pool keyed by patch index, (b) HostBuffer.emplace copies on both backends (flutter_gpu buffer.dart:298-311; web shim buffer.dart:236-250), (c) bound ordering, (d) reset discipline -- all confirmed.
Task 6: minor (deferred): class doc's per-patch enumeration still incomplete (RenderTarget.singleColor, ColorAttachment unlisted; three BufferViews not one; two Transform2s for toPatch) -- Task 8's probe compares against it; `labelsSkipped` is a public mutable field unlike `patchesComposited`'s getter; "by the viewport test" doc phrasing ambiguous.
Task 6: ⚠️ (controller): threading of `out:` into the two emplace sites and `_regionPool[p]` has no flutter test witness (render needs a GPU) -- Task 8's device run is the witness; recorded, not fixable here.
Task 6: fix round 1/5 dispatched -- resumed implementer a030e08de2b36ef3b with the two Important findings.
Task 6: fix round 1/5 (2 addressed, 0 open -- out pre-dirtied 0xFF, f(19,0) deletion red at byte 76; six-label fixture, axis-swap red, rotation test; commits 8b1427e..500f67c). Re-reviewer aadee27fffcf66f7d (sonnet): both ADDRESSED, no new breakage. Minor (deferred): rotation test comment overstates the two-corner-bound claim at exactly 90 degrees.
Task 6: complete (commits ca6bda2..500f67c, review clean after 1 fix round)
Task 7: implementer aa50bdab7febd3f7e (opus) NEEDS_CONTEXT at base 500f67c -- no commit; see rulings below.
Task 7: measured (uncommitted run, rows in task-7-report.md): curves@fit passes criterion 1 ONLY at ratio 1.0 (0.85-0.93 elsewhere, referenceInk 1,873 < the brief's 5000 floor); curves@8x FAILS at ratio 1.0 itself (overEight 6 of 479) and inks nothing at 2.8/4.0; text-lod@fit passes 0.35..1.0 and fails from 1.4 up with `uncovered` growing (153, 293, 545, 1081) -- the frozen text-culling row: TINY sits under kMinTextCapPixels at the reference and above it at 1.4x live. Tiled reproductions land on the probe's exact numbers (5,730 / 4,893; 25,275 / 16,681 / 0). Resident zoom-out reads uncovered [0,0,2,2,0,0,0,0,1,0,1,0,...] (1-2 px edge jitter on ~thousands of ink px), resident zoom-in exact.
Ruling F6-a (amends F6): the band constants STAY at [0.5, 2.0] and criterion 2 is recorded as the design failure the spec names, decomposed -- because the two frozen watermark rows (chord count; text culling) make criterion 1 fail at ANY scale change on a corpus with a curve or a label near the threshold (a step function at each threshold crossing, not a drift), so "the widest ratio at which criterion 1 still holds" is 1.0 on such corpora by the design's own definition; shrinking the constants to [1,1] would rebuild after every zoom step and is a design change the human makes, not the controller — costs: the constants are not the measurement's output; flagged for the human at the finish with the rows.
Ruling F6-b: the band tests assert what is TRUE and load-bearing -- (1) a straight-only corpus (crossingGrid) meets criterion 1 at every ratio of the sweep (the resident arm is exact wherever no decision is frozen); (2) text-lod: criterion 1 at 0.5, 0.7, 1.0, AND the 1.4 failure is the frozen cull by construction (`uncovered > 0` at 1.4 with LOD on, criterion 1 PASS at 1.4 with minTextCapPixels 0); (3) curves@fit: criterion 1 at 1.0, agreement at 1.4 below 1.0's (the step is real), referenceInk floor 1000 not 5000 (the fixture is 1,873 px); (4) curves@8x: agreement >= 0.98 at ratio 1.0 (Ruling F2's precision at a working zoom, measured: 6 px of 479), rows at 2.8/4.0 printed only (nothing on screen); (5) passingRun requires 1.0 itself to pass; the intersection is printed as the reported band.
Ruling F13-a: the resident zoom-out gate is `uncovered <= 4` per frame AND `uncovered * 2000 < referenceInk` (0.05%), not the literal 0 -- because 1-2 px of edge jitter between two exact rasterisations of float32-vs-float64 vertices is not the zoom-out defect (5,730 px of unpainted ink on tiles) and a literal 0 would fail on rasteriser tie-breaks; the spec's "zero" is recorded as met-within-jitter with the numbers — costs: a real 3-px regression would pass; the tiled contrast makes the class of defect unmistakable.
Task 7: second run -- decomposition proven (text-lod 1.4: cull on uncovered=153 FAIL; cull off agreement 1.00000); zoom_defect green under F13-a; straight corpus bit-exact at eight ratios and fails criterion 1 at ratio 1.0 ONLY, by 8 px of 9,792 (0.082%): axis-aligned strokes at the identity put edges on device-pixel boundaries where float32-vs-float64 ties flip; identical with no frame shift and with the shift rounded to whole pixels; 0.999 and 1.001 exact.
Ruling F13-b: criterionOne stays the spec's literal criterion 1; the straight corpus asserts it at the eight non-identity ratios and, at ratio 1.0 alone, the tie-jitter bound `agreement >= 0.999 && uncovered * 1000 < referenceInk` with the cause named -- because the identity is the one ratio where axis-aligned edges sit exactly on pixel boundaries and a 1-ulp tie is not a drift; the reported straight run treats 1.0 as passing under that bound — costs: 0.1% of ink at exactly ratio 1.0 is unguarded by the literal criterion on this corpus; every other ratio is bit-exact.
Task 7: implementer committed b4fd795 after Rulings F6-a/F6-b/F13-a/F13-b; reported band [1.0, 1.0] (straight 16x control, text-lod [0.35, 1.0], curves [1.0, 1.0]); decomposition proven by one changed argument.
Task 7: reviewer a0bdb05f0c8e1f23c (opus): spec ✅ (as amended), quality Approved-with-one-Important: zoom_defect_test:87-89 M-F5 note says "in the thousands of pixels" -- the measured mutation read 418; an unmeasured magnitude violates the no-synthesis rule. Fix: quote 418.
Task 7: minor (deferred unless cheap in the fix round): test 1's name contradicts its 1.0 exception; :154-156 "pixel-exact across the band" false (1.4, 2.0 inside the band fail); collapse message hard-codes criterion 1; staleFrames == rebuilds always; resident rows print only `uncovered` (referenceInk not on record); straightTieJitter vacuous at union 0 (guarded by ink floor in test 1); test 5 re-sweeps all corpora.
Task 7: fix round 1/5 dispatched -- resumed implementer aa50bdab7febd3f7e with the Important plus the three cheap minors (name, comment, first-frame print).
Task 7: fix round 1/5 (4 addressed -- 418 quoted, name, comment, first-frame print referenceInk=20410; commits b4fd795..ed94773). Re-reviewer ac52f18b4eae5248f (sonnet): all ADDRESSED, no new breakage.
Task 7: complete (commits 500f67c..ed94773, review clean after 1 fix round; NEEDS_CONTEXT rounds resolved by Rulings F6-a, F6-b, F13-a, F13-b). Reported band [1.0, 1.0] (straight control 16x; text-lod [0.35, 1.0]; curves [1.0, 1.0]); constants unchanged at 0.5/2.0; criterion 2 = design failure, decomposed, for the results note.
Task 8: implementer adc0b03560f6c7ac3 (opus) DONE_WITH_CONCERNS -- commit 635e3e7, base ed94773; nine gates green (harness 81, flutter 664, dart 798). Adaptations: probe handle per sweep (DuplicateHandleError on repeat 2); arm D no-GPU-frame guard on the cold switch only (Offstage keeps its layer); alloc phase settles after the band-exit phase leaves the collection at 2.04x; brief test needed seen.clear() before the tables fire. Task 10 must keep SPIKE_FRAMES=30 (60 reaches 3.28x mid-phase). Nothing has run on a GPU yet.
Task 8: reviewer a08b3eeaa54c7919c (opus): spec ✅, quality Needs fixes -- Important: (1) alloc settle loop exits when the in-flight rebuild lands but before the repaint it schedules marks `band` (ratio 0.49 after band back), so a walk+upload can land inside the probe window; (2) bandexit `staleFrames` counts only painted frames and the phase stops 4 steps after the exit with no wait for the landing -- reads a constant 5 with `rebuilds landed=0` (plan-mandated snippet); (3) the probed sum includes the rig's own per-frame ViewportTransform and dart:ui compositing objects -- the verdict line must say to read the per-class lines first. All four adaptations judged necessary and correct.
Ruling F9-a (amends the brief's band-exit phase): the phase keeps stepping the camera 1.02 per frame past the exit until `r.landed` advances (cap 200 frames total; throw if it never lands), reports `staleFrames` = frames painted out of band before the landing, `exitStep`, `landedAtStep`, and `submits` -- because criterion 9's number is the interval between the exit and the landing, and a phase truncated at 40 steps measures the phase length; the FrameTimingLog window covers the whole stepped run — costs: the bandexit build/raster stats now include the landing frame, which is what criterion 9 asks to see.
Task 8: minor (deferred unless cheap): rebuildPhase does not re-check uploadFailed; `frames` shadowed; `patches=` means three things across lines; bare `D` vs full label; bandexit prints no submits; anonymous phase blocks; P read from the last frame; no test that two sweeps need two handles; only connect() guarded in the alloc block.
Task 8: fix round 1/5 dispatched -- resumed implementer adc0b03560f6c7ac3 with findings 1-3 + Ruling F9-a + the cheap minors.
Task 8: fix round 1/5 (3 Important + 5 minors addressed; commits 635e3e7..9914f6b). Re-reviewer af063f2b7f721f3ae (opus): all ADDRESSED, no new Critical/Important; settle loop traced closed (no exit with a rebuild in flight); phase() unchanged in what it measures. Low: probe.dispose inside the try after read(); negative submits if an upload fails mid-phase (pre-existing); upload failure during the settle reads as a null-check UNEVALUABLE. Plan drift for Task 10: the trigger lines are prefixed `GSPIKE D residentGpu (DraftCanvas) rebuild` and the bandexit line reports `exitStep=/landedAtStep=/staleFrames=/submits=` (not `rebuilds landed=`).
Task 8: complete (commits ed94773..9914f6b, review clean after 1 fix round)
Task 9: implementer a0c5646e64e6f90e7 (sonnet) DONE -- commit a1ef1fe, base 9914f6b; 14 fired/killed first shot + M-F10prime killed; M-F10 and E-F1 fired and green (equivalent); zero survivors; band_sweep_test does not independently fire under M-F5 (fixture 800x600 coincides with the mutant viewport) -- documented, M-F5 killed by its other two witnesses.
Task 9: reviewer a0b47e5ea4c883400 (sonnet): Approved -- re-executed M-F6, M-F5 (three witnesses), M-F10prime, E-F1 in a scratch copy, all tails byte-identical; every other hunk names a current line. Minor (deferred): summary table shape is id|verdict, counts in prose.
Task 9: complete (commits 9914f6b..a1ef1fe, review clean)
Task 10: dispatched -- implementer runs the device spike in the worktree (macOS profile), writes the results note, strikes spec open question 3, updates STATUS; the window checks are recorded OWED unless a human looks (the controller cannot see the window; the user is asked at the finish).
Task 10: implementer ada0a62ee298ecc60 (opus) DONE_WITH_CONCERNS -- commit 10eaae7, base a1ef1fe; exit gate 7 of 14: crit 2 MISS (band 1.00x, design failure, decomposed), crit 7 MISS (6/10 triggers over 16.67; worst CommandApplied 22.64; classify 6.7 ms vs 27.4), crit 8 MISS (5/6 cells; D-C <= +0.37 ms -- inherited from Plan E's compositor), crit 9 MISS (p95 raster 6.57/4.75/7.36; staleFrames=1 exitStep=36 landedAtStep=37), crit 6 MISS at 2.5x (9.57 MB; fit 6.79 MB), crit 5 UNEVALUABLE (SocketException errno 1 -- App Sandbox refuses loopback; DebugProfile.entitlements has network.server but not network.client), crit 13 OWED (four checks + Plan E's fifth). Findings: 1.5-2.8 ms of every edit-triggered rebuild is document.extents recomputation (uncounted lever); Plan E's crit-6 figure double-counted the sub-buffer (6.79 not 7.06).
Ruling F8-a: the harness's macOS entitlements gain `com.apple.security.network.client` (DebugProfile and Release) and the spike is re-run so criterion 5 gets its number -- because the probe's only failure is the App Sandbox refusing an outbound loopback socket, which is enablement of the same class as FLTEnableFlutterGPU (the spec puts enablement in the plan), and an UNEVALUABLE for a missing entitlement would leave invariant 1 unmeasured for a reason that is not the design's — costs one more device run (~10 min) and the run-2 numbers replace run-1's for every criterion only if run 2 completes; otherwise run 1 stands and run 2's alloc lines are recorded beside it.
Task 10: fix round 1/5 -- entitlement (network.client, both files), run 2 clean and the run of record; crit 5 measured MISS perFrame=2333.2 vs 2200 (+6.1%) but ~2 collection walks landed inside the probe window (ResidentTextRecord 11.0/frame) -- recorded as printed, not re-scored; per-patch classes 12.3 of 24; R6-2 has its number (95.6 Image handles/frame). Gate 7 of 14 (6 MISS + 1 OWED). commits 10eaae7..98285d3.
Task 10: reviewer a05b10de2c96e5432 (opus): every recomputed cell matches gspike-run2.log; exit-gate 14 rows faithful; window OWED. Needs fixes -- Important: STATUS.md:946 says 38 frames (run 1; run 2 n=37); STATUS.md:1049-1050 says classify 6.7 / 4.1x / 40% (run 1; run of record 6.3 / 4.3x / 38%); "no blank frame" at STATUS:946 and note:618 is an inference the harness does not measure. Minor (deferred unless cheap): five figures trace only to the git-ignored ledger; a reformatted fenced BAND line; 6.79 vs 6.78 on the dpr rows; run conditions in no committed log; "band out (2.5x)" is a phase definition; the 7-of-14 tally folds two amended PASSes.
Task 10: fix round 1/5 dispatched -- resumed implementer ada0a62ee298ecc60.
Task 10: fix round 1/5 (3 Important + 4 minors addressed; commits 98285d3..2b0dd0e). Re-reviewer acca27bcf0c3edeab (sonnet): all ADDRESSED, no new breakage; grep sweeps clean.
Task 10: complete (commits a1ef1fe..2b0dd0e, review clean after 1 fix round; exit gate 7 of 14 -- 6 MISS + 1 OWED, two PASS-as-amended)
Final review: reviewer aa1c2ef8ffa3fccff (opus), whole branch c5b8ee8..2b0dd0e: "With fixes". Important: (1) the ledger is unarchived while the note cites it -- archive onto the branch before merge; (2) a throw in rebuildNow (collect/classify) escapes _run and bypasses the F5 fallback -- catch, set uploadFailed, clear backend, notify, report once; (3) three "until Plan F" doc comments (text_patches.dart:10-16 band constants; draft_canvas.dart:223, :506); (4) DraftCanvas.tiles doc silent about residentGpu. Minor taken now: #5 shouldRepaint `old.resident != resident || (resident != null && dpr changed)`; #6 render_backend_test guard + stray `//`; #7 class doc adds RenderTarget/ColorAttachment; #8 test comment M-F10 -> M-F10prime; #9 band_sweep test name [0.35, 1.0]. Deferred to Plan G: #10 rig inBand pointer, #11 extentsMicros (needs a device run to mean anything), #12 rebuilder hygiene, #13 duplication note, #14 new-document didUpdateWidget test (fold into the wave -- three lines), criterion 5's divisor (backend.frames delta). Spec finding: criterion 2 is not measurable as written (criterion 1 is a step at each frozen decision) -- for the human.
Ruling FR-1: ONE fix wave -- Important 2, 3, 4 + minors 5, 6, 7, 8, 9, 14 (all small, none changes a measured number; the `catch` gets a test with a throwing FakeUploader and a throwing painter walk is not simulated). Important 1 (archive the ledger) is the controller's own commit at the finish, after the wave, so the archived ledger carries the wave. Not taken: extentsMicros and the criterion-5 divisor (both need a device run to mean anything -- Plan G, listed in STATUS) — costs: Plan G inherits two harness edits.
Final fix wave: implementer afa4fe203b2a2d2a0 (sonnet) -- commit ea2a192; nine gates green (798/668/82). Deviation: the two-report widget assertion uses a scoped FlutterError.onError interception because the binding collapses two same-microtask reports into one pending "Multiple exceptions".
Final fix wave: re-reviewer ae9d17330cd5b8aca (opus): A-I all ADDRESSED, no new Critical/Important; focused run of five files +45 green. Minors (deferred to Plan G): fallback test restores onError by assignment not try/finally; its setUp comment still says "no swap"; band test name [0.35, 1.0] vs loop [0.5, 0.7, 1.0]; tile_comparison.dart:555 says shouldRepaint is unconditionally false (stale before this wave); BufferView 89.9/frame does not decompose as 3 x patches -- for the human at merge.
Final review: clean after one fix wave (commits 2b0dd0e..ea2a192). Important 1 (archive the ledger) done by the controller in the next commit.
Plan F: all ten tasks complete; final whole-branch review clean; branch plan-f/rebuild-and-band at ea2a192 (+ the archive commit) awaits the human: the four window checks (+ Plan E fifth), the criterion-2 spec decision, and the merge.
