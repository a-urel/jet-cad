# SDD ledger — fix/post-07 (the three defects deferred from Plan 07)

Branch fix/post-07, cut at afa4edb (plan-07/walls tip; the human merged it --no-ff locally on 2026-09-25, origin/main still 22957d1), worktree `.claude/worktrees/fix-post-07`.
Source of the three items: docs/superpowers/notes/2026-09-24-plan-07-results.md, "Found and deferred to a post-07 fix/ branch".
Merge is the human's (--no-ff, from the main checkout). Push only on the human's word.

## Branch-point counts (this container)
- engine +972 -2 (standing); render +931 ~1 -7 (standing); app +142.

## Decisions
Human (2026-09-25): "it works, merged. start the fix/ branch" — the 07 look passed on the human's machine.
Human (2026-09-25): white ByLayer — ACI 7 resolves to a foreground colour: DocumentStyleResolver takes it (default white, so nothing existing moves); the app passes black. Drafting stays ByLayer (spec 05 D2 intact). Chosen over "tools draft black" and "layer 0 black in the app".
Ruling (F-order): three fixes, three commits: F1 ACI 7 foreground (engine + app); F2 the Page panel's field hands focus back after Enter; F3 Box m3 (the Selection panel's tap-outside walks the focus history past its own fields). F2+F3 go to one implementer (same mechanism, app only). Each gets an independent review — cost: none.

## Tasks
F1 dispatched (brief f1-brief.md). F2+F3 brief written (f2f3-brief.md); dispatched after F1 commits (same worktree, same app test run).
F1: implementer reports c960d1e — DocumentStyleResolver(document, {foreground = 0xFFFFFF}), _rgbOf checks encoded == 7 (one int compare, every route); ArgumentError outside 0..0xFFFFFF (deviation: an ARGB black would overwrite alpha); shell owns one resolver per document, PlannerView.resolver required; kWallColor comment; spec 05 D2 + 07 D3 amendments. Gates engine +983 -2, render +931 ~1 -7, app +143, web ✓. Mutants M-F1a/b/b'/c/d/e/e'/f/g all red. Review pending.
F1 review dispatched in a detached worktree (.claude/worktrees/fix-post-07-review at c960d1e) so its mutants cannot collide with F2+F3's implementer — cost: none.
F2+F3 dispatched (f2f3-brief.md).
F1: review Approved (no Important). Reviewer re-fired M-F1a/c/d/e red; own M-RV3 (planner_view drops resolver:) red A17:438; M-RV4 ((encoded & 0xFFFFFF) == 7) red 469. Gates reverified: engine +983 -2, render +931 ~1 -7, app +143, web ✓. Minors: m1 the Page panel's Blueprint paper (0xFF1F3A5F, navy): hard-wired black takes ByLayer drafting there from ~11.6:1 to ~1.8:1 (reviewer's estimate); m2 spec 07's amendment says "the contrast rule now exists" — overclaims a host-given fixed foreground.
Human (2026-09-25), on m1: follow the paper — foreground black on light paper, white on dark, by the page background's luminance; the shell swaps its resolver when the paper changes; walls keep their concrete black (unchanged). Chosen over "walls too" and "keep black".
Ruling: F1b (follow the paper) is a follow-up commit by a fresh implementer, dispatched after F2+F3 commits (same worktree; main.dart); it also rewrites both spec amendments accurately (covers m2) — cost: one more review.
F1: complete (commit c960d1e; follow-up F1b pending).
F2+F3: implementer reports e5c2a89 (F2: PanelFieldFocusNode.handBack in new panel_focus.dart; Scale field Enter + onTapOutside) and e87f66c (F3: Selection fields use it; unreachable hasFocus guard removed). Reproduced on c960d1e: Enter in Scale, mouse tap-outside Scale (title and canvas), B7, Enter in Height after Width, cross-panel Width→Scale. Deviation: Scale also got onTapOutside (both tap cases reproduced with a mouse). Tests A18, A19, SE11–SE14. Mutants M-F2a/b/c/d, M-F3a/a'/b/c/d/e red; `seen` guard not fired (no known looping sequence). App +149, analyze/format clean, web ✓. Review pending.
F2+F3 review dispatched (review worktree moved to e87f66c, detached); F1b dispatched in parallel in the branch worktree.
Human (2026-09-25): push fix/post-07 when the reviews are done.
F2+F3: review Approved (no Important). Reviewer re-fired M-F2a, M-F2c, M-F3a, M-F3c (its readings) red; own M-RV1 (walk capped at 2) red SE13:822, M-RV2 (walk past the canvas) 8 red. Gate reverified: app +149, analyze/format clean, web ✓; engine/render untouched. Probes: canvas click keeps its focus; checkbox, segment, text-entry overlay, Tab, unmounted fields in the history — all end on the canvas; walk terminates.
F2+F3 minors: m1 EditableText arms onTapOutside from its last build's _hasFocus, so within a frame handBack can run on an unfocused node: its unfocus returns early, the walk moves from the truly focused node back to the caller, and `seen` stops ON the caller (probe P10: Width tapped, Scale tapped without a frame, mouse on the Page title → Width focused, W dead). Suggested: start the walk from the focused node; correct the docstring. m2 Scale on tap-outside commits nothing (spec 04: "committed on submit") but keeps the typed text: field 1:75 while the page is 1:20. Nit: A18/SE13 commit 50 = PageComponent's constructor default; use 75.
Ruling (m1): take it — the walk starts from the node that has the focus (hasFocus ? this : primaryFocus); P10 lands as a test; mutant = the old start — cost: one test.
Ruling (m2): keep spec 04's commit-on-submit; when the Scale field loses focus without Enter it re-syncs to the model (_syncScale), so it never shows a scale the page doesn't have. Not "commit on focus loss" (that would change spec 04's contract) — cost if wrong: a user expecting a blur commit retypes and presses Enter.
Ruling (nit): A18/SE13 commit 75 — cost: none.
F2F3b brief written; dispatched after F1b commits (same worktree).
F1b: implementer reports 754aa83 — engine foregroundFor(background) (WCAG contrast, sRGB linearised, alpha ignored; crossover grey 0x767676 black / 0x757575 white); shell's resolver from the page, rebuilt in a PageNotifier listener only when the choice changes (every route: swatch, undo, redo, load, purge); no page = white paper; spec 05 D2 / 07 D3 amendments rewritten (m2 gone); kWallColor comment. Tests: engine group (4), app A20 (swatch Blueprint, undo, redo, White→Ivory identity), A21 (load), A22 (no page). Mutants a, b, c, c2, d, e, e2, f, g, i, j, k red; h (tie → white) survived, equivalent: exhaustive 2^24 run, 0 ties. Deviation: e/e2 fired in PageNotifier (the shell's one seam). Flag: on Blueprint, drafting off the sheet sits white on the shell's light surface. Gates engine +987 -2, render +931 ~1 -7, app +152, web ✓. Review pending.
F1b review dispatched (review worktree at 754aa83); F2F3b dispatched in parallel in the branch worktree.
F2F3b: implementer reports 159ee7d — handBack starts from the focused node (hasFocus ? this : primaryFocus; nothing to do if that is not a panel field), docstring corrected; Scale re-syncs on focus loss (_onScaleFocus), commit still on submit; SE15 (P10) red before the fix at :869; A23 (tap-outside shows 20, model 20, depth unchanged); A18/SE13 commit 75. Mutants M1 (old start) SE15:869, M2a (no re-sync) A23:660, M2b (re-sync before commit) A18:484 + SE13:821. A18's "field shows 75" check is killed by no mutant (reported honestly). App +154, analyze/format clean, web ✓. Review pending (second review worktree .claude/worktrees/fix-post-07-review2 at 159ee7d; the first is busy with F1b's review).
F1b: review Approved (no Important). Maths re-derived (crossover L 0.17913; 0x76 black, 0x75 white; 0.03928 vs 0.04045 identical on all bytes; 2^24 exhaustive: 0 ties, min diff 2.4e-7 → h equivalent). Real pixels probed through PlannerShell: White 0,0,0 → Blueprint 255,255,255 → undo 0,0,0. Re-fired M-F1b-a/b/c/d/e red; own rv3-W (weights), rv3-G (pow 2.2), rv3-X (compare vs no-page), rv3-Y (no setState), rv3-Z (no page = dark), rv3-V (didUpdateWidget ignores resolver) red. Gates engine +987 -2, render +931 ~1 -7, app +152, web ✓.
F1b minors: m1 (latent, pre-existing) _DraftCustomPainter.shouldRepaint ignores painter identity, so a resolver swap WITHOUT a DocChange paints stale colours on the vertices backend (bare-canvas probe); never visible in the app (every page change is a DocChange). m2 spec 05 D2's amendment lists A17/A20/A21, omits A22. Observations: on Blueprint, off-sheet drafting white on the light surface; grid colours are black-alpha (near-invisible on navy); the startup plan's concrete colours (walls 0x202020) don't follow the paper; walls black by decision; overlays legible.
Ruling (m1): recorded as debt in the closing note, not fixed here — pre-existing, render layer, unreachable from the app; a fix is a render-layer change with its own test — cost if wrong: a future host swapping resolvers without an edit paints stale colours until the next edit.
Ruling (m2): the controller adds A22 in the closing docs commit — cost: none.
Ruling (observations): recorded for the human's look as known consequences of "follow the paper" — cost: none.
F1: complete (c960d1e + 754aa83).
F2F3b: review Needs fixes. I1: the Enter-order claim is false in three places (page_panel.dart:58-61, the commit message, A18's comment at :488): document.changes is an async broadcast stream, so _onScaleFocus reads the NOT-yet-updated PageNotifier and writes "20", then the page listener writes "75" (probe PA seq=[20, 75]); R2 (drop the page listener) is red only at A18:489 — the implementer's "no mutant kills that check" is false. m-a: nothing pins "handBack does nothing when the focused node is not a panel field" (R1 survives; probe PB kills it). m-b: window blur (web/desktop → AppLifecycleState.inactive → FocusManager moves primary to the root scope) fires the re-sync and drops the typed text; on resume the field shows the model value (probe PZ). Q1: stale tap-outside can't steal a field the user just chose (all TextFields share one tap group; probe PH ends on Height); SE15 pins it. Q2: invalid Enter, undo while focused, dispose while focused all fine; a page change while typing overwrites the text (pre-existing). Side: nothing but A18:489 pins that the scale field follows undo. Gate app +154, web ✓.
Ruling (I1): option (b) — the focus-loss re-sync reads the page from the document (already updated synchronously by the command), so the comments become true; the three false statements are corrected (the commit message by the fix commit's own message); a test pins that the scale field follows an undo (so R2 stays killed) — cost: one test.
Ruling (m-a): land PB as a test — cost: one test.
Ruling (m-b): not intended — the re-sync is for the user moving focus inside the app, not for the window losing focus; skip it when the app is not resumed (or an equivalent the implementer can justify), with PZ as a test — cost if wrong: a stale value stays visible after an unusual focus path.
F2F3c dispatched (f2f3c-brief.md).
F2F3c: implementer reports f5920de — _syncScale reads the document's PageComponent (all three callers); comments corrected, commit message corrects 159ee7d's; A18 pins every shown text == '75' from Enter on (PA); A24 undo/redo follow; SE16 = PB; _onScaleFocus skips while focused and when lifecycleState is non-null and not resumed; A25 (inactive keeps 75, resume restores focus and text, Enter commits). Ordering evidence: SchedulerBinding sets _lifecycleState before observers; FocusManager._appLifecycleChange moves focus synchronously; flutter_test's initial state is null (guard accepts null — MBnull). Mutants MI1 A18:499, R2 A24:693, MB A25:723, MBnull A23:671, R1 SE16:896, M1 SE15:869, M2a A23:671, M2b (reconstructed) 5 red. App +157, analyze/format clean, web ✓. Open gap: window blur, then a click into the app before resume (likely on web: pointerdown precedes window focus) → field shows the typed text over 1:20, canvas focused; documented in _onScaleFocus, not fixed.
Ruling (open gap): accepted — the cost m-b's ruling named; recorded as debt in the closing note with the implementer's sketch (re-sync on resume once focus has settled) — cost if wrong: after leaving the window mid-edit and clicking back in, the field shows unsubmitted text until the next page change or focus.
F2F3c review dispatched (review2 worktree at f5920de).
F2F3c: review Needs fixes. I-1: m-b is not fixed on web — the focused <input> gets its own blur (relatedTarget null) BEFORE the window blur; the engine closes the text connection (text_editing.dart:1701-1712 handleBlur) → EditableText.connectionClosed → focusNode.unfocus() while lifecycleState is still resumed, so the re-sync drops the typed text (Playwright on the release build: browser order input-blur then window-blur → field 1:50; a no-re-sync variant keeps 1:75). A25 pins only the lifecycle path. The comment at page_panel.dart:71-77 and the commit message's web claim are false. (Pre-existing, Flutter's: after that refocus the letters are dead until a canvas click.) m-1: A18's everyElement('75') passes on an empty list — the correct code shows nothing; expect(shown, isEmpty) pins it. Passed: document read consistent with PageNotifier; null lifecycle = resumed correct (web initial ''); A25 does not leak; Enter-order comments true. Mutants MI1/R2/MB/MBnull/R1 red; own MH (drop hasFocus early return) A25:728. Gate app +157, web ✓.
Ruling (I-1): change the approach, not the guard — the Scale field re-syncs in its onTapOutside (after handBack), i.e. when the user clicks elsewhere IN the app without Enter; the focus-loss listener and the lifecycle guard are removed. A window blur is never an in-app tap, whatever the platform's order. Leaving Scale by clicking another text field (same tap group, no onTapOutside) keeps the typed text visible — accepted, recorded. _syncScale keeps reading the document. A25 is rewritten to the web order (the field loses focus while resumed → text kept) — cost if wrong: after Tab/clicking a text field mid-edit, the unsubmitted text stays visible.
Ruling (m-1): A18 expects shown to be empty — cost: none.
F2F3d dispatched (f2f3d-brief.md).
F2F3d: implementer reports e0336f8 — _onScaleFocus and the lifecycle guard removed; onTapOutside = handBack then _syncScale; doc comment states the web order (attributed to the F2F3c review's run), that a field rebuilt without focus is no longer armed, and the accepted gap (clicking/Tabbing into another text field keeps the text). A18 → isEmpty; A25 rewritten: lifecycle resumed + testTextInput.closeConnection() (the real onConnectionClosed path) → field keeps 75, model 20, depth unchanged. Mutants (a) A23:668, (b) A25:718 (f5920de's own page_panel.dart also red there), (e) no != check A18:496, R1 SE16:896, M1 SE15:869; (c) re-sync before handBack survives — equivalent by observation (identical controller + text-input logs); (d) _syncScale reads widget.page.value survives — no caller runs between a command and the notifier now. App +157, web ✓. Review dispatched (review2 at e0336f8).
F2F3d: review Approved (no Important). Chromium on e0336f8's release build (synthetic blurs; the real order from the F2F3c review): input-blur-then-window-blur keeps 1:75 after blur and resume (old f5920de: 1:50); window-blur-only keeps 1:75 and refocuses; an in-app tap shows 1:50. Stale-arming probe: the stale Scale handler fires, handBack walks from Width to the canvas, _syncScale touches only Scale, nothing committed. (c) equivalent (focus change in a microtask); (d) unkillable by real input — keep the document read. Re-fired (a) A23:668, (b) A25:720 (the implementer said 718 — the lifecycle expect; 720 is the text expect), R1 SE16:896. Gate app +157, web ✓. Minors: m1 the report's line number; m2 the comment's web order does not hold on Safari desktop (no input blur listener; the conclusion holds); m3 (pre-existing) after a web alt-tab mid-edit, the focus ends on the route's scope and letters are dead until a canvas click. Out of scope, pre-existing: text_entry_overlay.dart uses the same lifecycle guard; in Chromium a web-order blur discards a pending Text-tool entry (breaks Ruling T7-b on web; A13 drives only lifecycle changes).
Ruling: m2 — the controller corrects the comment (Chromium; Safari desktop has no input blur) in the closing commit; m1 recorded here; m3 and the Text-tool entry recorded as debt in the closing note, offered to the human as follow-ups — cost: none.
F2+F3: complete (e5c2a89, e87f66c, 159ee7d, f5920de, e0336f8).
Closing (controller): note docs/superpowers/notes/2026-09-25-fix-post-07.md; 07 results note links it; spec 05 D2 amendment lists A22 (F1b m2); page_panel.dart comment (F2F3d m2). STATUS.md NOT touched on this branch: the human's merge of plan-07/walls is local only (origin/main 22957d1) and may carry STATUS edits; the resume point is updated after this branch merges — cost if wrong: STATUS's Resume here names the fix/ branch as pending until then.
Ledger archived to docs/superpowers/ledgers/2026-09-25-fix-post-07/ as the branch's last commit (progress.md plus every brief). Nothing is appended after the archive.
Pushed fix/post-07 on the human's instruction ("push fix/post-07 when the reviews are done").
