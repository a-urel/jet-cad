# SDD ledger — plan: docs/superpowers/plans/2026-08-20-jet-cad-2d-plan-3d-vertices-sink.md

Spec: docs/superpowers/specs/2026-08-20-jet-cad-2d-plan-3d-design.md (read, binding authority)
Branch: spike/vertices-sink, worktree .claude/worktrees/vertices-spike, plan head 548fa8e

## Pre-flight conflict scan

Every pair of tasks sharing a file or an interface, plus each task against itself.

| Rows | Produces → consumes | Found |
|---|---|---|
| T1 → T7, T10, T11, T13 | `RenderBackend`, `defaultRenderBackend()`, `DraftCanvas.backend`, `DraftCanvasState.resolvedBackend` | names identical at every site — clean |
| T1 → T2,3,4,5,8 | all modify `vertices_draw_sink.dart` sequentially, never concurrently | clean (strict order, single implementer at a time) |
| T4 → T5 | `_beginRun` / `_runTo` / `_endRun` / `_emitJoin` / `_emitQuad` / `_emitTriangle` | clean; T4 leaves `_endRun`'s closed branch asserting, T5 makes it live — deliberate TDD split |
| T4 → T7 | `frameSegmentCount` renamed `frameTriangleCount` (T4 step 10), used under new name in T7 | clean |
| T8 → T9, T10, T11 | `FlushObserver`, `VerticesDrawSink.observer` | clean |
| T9 → T10, T11 | `TriangleRasterizer.observe/pixels/inked/toImage` | clean |
| T3 → T4 | T3 pins the paint-path allocation; T4 adds the join walk with run state in fields, not an object | clean — T4's text names the constraint T3 established |
| T6 → T10 | `CanvasDrawSink.point` changes shape; could disturb the 14 existing canvas goldens | **checked**: no golden fixture emits a point op (`grep point test/golden/*.dart` finds only `basePoint:` and prose). No conflict. |
| T2 → T8 | T2 disposes the submitted `Vertices` in `flush()`; T8 adds an observer called from the same `flush()` | ordering matters — observer must fire on the buffer **before** disposal. Carried into T8's dispatch. |
| T12, T13 → T7 | both consume the harness `BACKEND` define and the `backend=` printed field | clean |
| T14 → T4, T5, T6 | mutants against code those tasks write | clean |
| T15 → T3, T12, T13, T14 | exit gate reads their results | clean |
| Each task vs itself | tests specified against code specified; files created against files later touched | four off-by-one cross-references found, ruled below; no other self-contradiction |

## Rulings (pre-flight)

Ruling: the plan was renumbered late and four cross-references kept the old numbers. All four resolve to the task whose heading matches the described work, not the number written. Specifically —
  (a) line 16, "Task 4 extends the measurement to the paint path … see Task 4, step 8" → **Task 3**, step 8. Task 3's heading is the paint-path measurement and its step 8 is the do-not-amend stop.
  (b) line 329, "Task 6 fixes it" (the harness) → **Task 7**. Task 7's heading is "The harness picks a backend".
  (c) lines 53 and 2471, "Task 14 writes the note" (the results note) → **Task 15**. Task 14 is the mutation log; line 2739's own summary already says "Mutants → Task 14. Exit gate → Task 15."
  (d) line 728, "Plan 3d stops at Task 14" if the CLAUDE.md amendment is refused → **Task 15**, which is where a refusal gets recorded as the result.
  — Why: the headings and the plan's own self-review section are self-consistent; only these four numeric references are not, and every one of them points at work another task's heading owns. Cost if wrong: a task does something already done elsewhere, or the results note lands one task late. Both are visible in the ledger and cheap to undo.

Ruling: `apps/dev_harness_2d`'s `flutter analyze` is **not** part of the green gate for Tasks 1 through 6, and **is** from Task 7 onward. — Why: Task 1 deletes `useVertices`, which the harness passes, and Task 7 is the task that repairs it; the plan states this at line 329 for Task 1 but Tasks 2-6 inherit the same broken harness without saying so. The two package suites stay in every task's gate unchanged. Cost if wrong: a harness compile break survives six tasks instead of one — bounded, because Task 7 cannot pass its own gate without fixing it.

## Progress

Task 1: executed inline by the controller. The dispatched implementer was rejected by the human after it had already written the lib/ changes; the controller verified that diff against the brief hunk-by-hunk, established RED by backup/restore (no `git checkout`), finished the task, and committed. Report: task-1-report.md.

Task 1: Ruling: flipping the platform default to `vertices` turned 10 goldens red — `dash_ladder` and `text_ladder` build a `DraftCanvas` without naming a backend and began rendering through the vertices sink under software Skia (0.8-0.9% pixel diff). Both now pass `backend: RenderBackend.canvas`. — Why: Task 10's own text says the existing 14 PNGs become the canvas backend's suite, so this is a strict subset of work already planned, and the alternative is leaving the golden suite out of gate for nine tasks. Cost if wrong: Task 10 finds two of its three ladders already carrying the parameter and has less to do.

Task 1: Ruling: fixed a third `useVertices` reference the brief did not list — the doc comment on `_DraftCustomPainter.vertices` at draft_canvas.dart:220, which pointed at a symbol the same commit deletes. — Why: a dangling doc reference to a deleted parameter is a defect the commit itself creates. Cost if wrong: none; it is a comment.

Task 1: complete (commits 548fa8e..2a7b762, controller-executed, review pending)
Task 1: review clean (spec ✅, quality approved, no findings; reviewer independently corroborated the RED transcript's line/column positions and named a distinct mutation for each of the five tests).
Task 1: ⚠️ resolved by the controller — the reviewer could not settle whether `draft_canvas_test.dart`'s nine unmodified widget tests now run under the flipped default. They do. Eight are backend-agnostic (repaint, dispose, ownership) and the culling one drives `RecordingDrawSink` directly, so it never touched `DraftCanvas`. One is weakened: `draft_canvas_test.dart:130` "one sink serves every paint" reads `state.sink.canvas` to prove the sink the widget owns is the sink that paints — under the vertices backend `CanvasDrawSink` is the fallback and takes only text, and the read still returns normally because `_DraftCustomPainter.paint` binds `sink.canvas` before it branches. Not a gap in Task 1's deliverable; a test whose meaning the default flip narrowed.
Task 1: minor (deferred): `draft_canvas_test.dart:130` "one sink serves every paint" should name `backend: RenderBackend.canvas` — it is a claim about `CanvasDrawSink`'s identity and lifetime, and at the platform default it now asserts that about the fallback. Task 10 owns backend parameterization of the test suite; hand it there.
Task 1: complete (commits 548fa8e..2a7b762, review clean, 1 minor deferred)
Task 2: implemented by subagent a1a52fac (commit d17a436, DONE_WITH_CONCERNS — device verification blocked on Task 7).
Task 2: review NOT APPROVED — 2 Critical. (1) `_lastFlushDisposed = true` is an unconditional write beside the dispose call; reviewer removed `vertices.dispose()` and all 26 tests still passed, so the suite cannot catch the leak the task exists to prevent. (2) the recorder test never rasterises, so `expect(picture, isNotNull)` cannot distinguish safe disposal from corrupt disposal — the exact question the task was built to settle.
Task 2: fix round 1/5 dispatched to the original implementer — derive the flag from `Vertices.debugDisposed` (sky_engine painting.dart:6786, assert-guarded), and make the recorder test rasterise via `picture.toImage()` + `toByteData()` against an undisposed control, kept to one or two segments so software Skia finishes.
Task 2: fix round 1/5 (2 addressed, 0 open; commits d17a436..4a36455). Re-reviewer independently ran the mutation — deleted `vertices.dispose()`, both tests went red, restored via trap/cp, git clean. Also confirmed from sky_engine that `Canvas.drawVertices` itself asserts `!vertices.debugDisposed`, so the dispose-before-submit mutant dies on an engine assert; the test's `// MUTATION:` comment says so rather than overclaiming.
Task 2: carried to Task 7 — the brief's step 7 (verify on a real device that disposal immediately after submit is safe) could not run because `apps/dev_harness_2d` does not compile until Task 7 repairs it. The `PictureRecorder` + `toImage` rasterisation test is the evidence on record until then. Task 7's dispatch must carry it.
Task 2: complete (commits 2a7b762..4a36455, review clean after 1 fix round)
Task 3: implemented by subagent a7dc7f0b (commit aeb2373). Measured residue: three objects per flush (`Vertices.raw` + two `sublistView` wrappers), nothing per entity; `3 * (textOps + 1)` per frame. CLAUDE.md untouched, as the plan requires.
Task 3: review — spec ✅, quality approved, 1 Important. The mutation concern is closed: the reviewer reproduced the `OutOfMemoryError` red, tried an off-by-one guard (green, benign) and an additive non-doubling variant (hangs), and judged the OOM acceptable because any real regression of this property is unbounded at corpus size.
Task 3: Ruling: the Important finding is a defect I introduced in the plan, not the implementer's — the brief's file header described a profiler-ratio methodology while the brief's own Step 1 test is a deterministic capacity check. The header is wrong wherever it came from and ships in the file; it gets rewritten to describe the instrument actually used. — Why: `query_allocation_test.dart` trains a reader to take the header as the mechanism, so a wrong header misroutes whoever extends the file. Cost if wrong: a comment says less than it could.
Task 3: fix round 1/5 dispatched to the original implementer — rewrite the header of paint_allocation_test.dart to describe the deterministic capacity check it performs.
Task 3: fix round 1/5 (1 addressed, 0 open; commits aeb2373..aca6d22, comment-only).
Task 3: OPEN FOR THE HUMAN — the CLAUDE.md amendment. Measured: three objects per flush, nothing per entity, `3 * (textOps + 1)` per frame. Proposed wording: "**The frame path allocates nothing per entity in steady state, and O(1) per flush.** `packages/jet_cad_2d/test/invariants/query_allocation_test.dart` and `packages/jet_cad_2d_flutter/test/invariants/paint_allocation_test.dart` measure it." Not applied. Task 15's exit criterion 7 depends on the answer; surface it to the human at the end of the run.
Task 3: complete (commits 4a36455..aca6d22, review clean after 1 fix round)
Task 4: implemented by subagent a01eb2d8 (commit 9031c57, DONE_WITH_CONCERNS). 9 new join tests, 3 pre-existing test files updated, 198 passing / 2 skipped.
Task 4: review NOT APPROVED. Geometry verified correct at coordinate level (two-triangle miter covers the whole notch with no gap along AB; threshold miters at exactly kMiterLimit*half; one shared walk; no per-entity allocation). Both of the implementer's disputed judgements upheld — the brief's `(8.0, 2.0) isFalse` probe really is unsatisfiable, and `frameTriangleCount` as a literal count is right. Findings: (1) Important — every join fixture turns left; collapsing `s = cross > 0 ? -half : half` to `s = -half` leaves all 198 tests green. (2) Important — `_endRun`'s `assert(!closed)` is unreachable because both call sites hardcode false, and its comment claims a guard that does not exist; `polyline(..., closed: true)` silently drops the closing segment. (3) Minor — `_inked`'s loop bound `i + 5` skips the last triangle. (4) Minor — the "drop the bevel triangle" MUTATION comment names a crack mechanism, but the test actually dies on triangle count first.
Task 4: Ruling: forward `closed` from `polyline` to `_endRun` rather than deleting the comment's claim. — Why: it makes the assert live, makes the shipped comment true, and hands Task 5 the RED it needs; the curve flattener's site stays false until Task 5 implements the seam. Cost if wrong: Task 5 finds one line already done.
Task 4: Ruling: the harness `frame_timing_test.dart:158` `frameSegmentCount` reference stays broken through Task 6 and is repaired in Task 7, together with changing the printed `segments=` label to `triangles=` so the transcripts do not lie. — Why: Task 7 is the task that repairs the harness, and splitting the repair across two tasks means neither can run it. Cost if wrong: the harness is uncompilable for three more tasks, which it already is. Reviewer flagged this as an Important finding because the ruling was not in its inputs; it is a handoff, not a defect. STATUS.md:22 records the app as analyze/format clean and will need updating when Task 7 lands.
Task 4: minor (deferred to Task 5): `_inked` returns true for any zero-area triangle at every probe point, so a degenerate seam join would make every `isTrue` probe vacuous.
Task 4: minor (deferred): `cosHalf <= 0` and `mlen == 0` in `_emitJoin` are unreachable given the `dot < kMinMiterCosine` bail — brief-specified verbatim; Task 5 must not treat them as live paths.
Task 4: minor (deferred): the differential stray-ink slack now applies its join term to plain quads too; tightening it per-triangle is a later task's work.
Task 4: NOTE FOR TASK 5 — the skipped test `'a closed polyline gets a closing segment and a seam join'` (vertices_draw_sink_test.dart, `skip: 'Task 5'`) has an EMPTY BODY. Unskipping it runs nothing until Task 5 writes one.
Task 4: fix round 1/5 dispatched to the original implementer — right-turn fixture that kills `s = -half`, forward `closed`, fix `_inked`'s loop bound, correct the bevel MUTATION comment.
Task 4: fix round 1/5 (4 addressed, 0 open; commits 9031c57..a5575c5). Re-reviewer ran `s = -half` itself: exactly one test failed, the new right-turn fixture, on the geometric probe `_inked(11.5, 1.5)` and not on a count. Confirmed `_flatten`'s call site still passes `closed: false` and `_endRun`'s closed branch is still the bare assert. Skip accounting explained: the empty placeholder became a live test asserting the AssertionError fires.
Task 4: complete (commits aca6d22..a5575c5, review clean after 1 fix round)
Task 5: implemented by subagent a213d69b (commit 2c9415f). Review APPROVED, spec ✅, no fix round. Reviewer verified the seam by coordinates: closed square emits (V,A,B)+(A,M,B) with M at the outer corner, appended after the closing quad in walk order; circle r=40 seams at (40,0) with the tip at (42.0118, 0), outside the 42 outer edge, so no notch. Both of the implementer's divergences from the brief upheld — the brief's `6+6` triangle count was wrong (correct is `6+4`, derived independently), and the `_runSegments >= 2` guard really is unreachable-false with all three candidate inputs checked.
Task 5: reviewer ran four mutations; every `// MUTATION:` comment in the diff matched what actually happened, including the rewritten one. The skip-seam mutation's `_inked` red fires independently of every count red, so the seam geometry is genuinely tested — not Task 4's count-hides-geometry pattern.
Task 5: minor (deferred to Task 11): `closed` is decided structurally (`circle()` true, `arc()` false) with no tolerance. A DXF ARC carrying `sweep == 2*pi` draws a full circle with an unjoined notch at its start angle, where `Canvas.drawArc` closes it. That is a backend divergence NOT on the spec's list of five permitted ones, so Task 11's sink-against-sink comparison is where it must surface; carry it into that dispatch. Whether a real ARC entity can carry a 2*pi sweep through the DXF reader is unverified.
Task 5: minor (deferred): the one geometric seam fixture sits at centre (0,0) on the identity residual at start angle 0 — `circle()` has no start angle, so that part is unavoidable. The rotated/translated case is covered only by counts. Reviewer dumped a rotated+translated circle by hand and found the seam geometry correct, and could not construct a mutation that survives the origin fixture.
Task 5: minor (deferred): nothing pins the seam's buffer order directly; the reorder mutation was caught only incidentally by `_endCentre`'s fixed-stride indexing.
Task 5: complete (commits a5575c5..2c9415f, review clean, no fix round, 3 minors deferred)
Task 6: implemented by subagent a101d675 (commit eba3dfa). Review APPROVED with 2 Minor. Both unrequested changes upheld: the brief's `canvasCallCount == 1` really is degenerate (holds under old and new `point` alike), and the three `draw_sink_test.dart` edits are genuine — the reviewer recomputed the Rect by hand and it matches. Goldens confirmed unaffected: the only "point" mentions in golden fixtures are prose and a `_cross` helper built from two line calls, never `DrawSink.point`.
Task 6: Ruling: the `a == d` finding enters the fix loop despite its Minor label. — Why: every residual in the new and changed tests is a pure rotation or a uniform scale, so `a` and `d` are never distinguished; the reviewer cross-swapped them in the coordinate math and all tests stayed green. This task exists to defeat the degenerate fixture, and a surviving one-line mutation against exactly that is not something it can ship with, whatever the severity label says. Cost if wrong: one fix round spent on a finding the process would have deferred.
Task 6: minor (deferred, report-only): the report's "no allocation introduced" overstates — the new `point` allocates a `Rect` the old one did not, in the same class `circle`/`arc` already incur. Not a regression against any tested invariant; `paint_allocation_test.dart` gates only VerticesDrawSink. Correcting the wording in the same fix round.
Task 6: confirmed — `CanvasDrawSink` has no minimum-stroke-width floor at all (`_widthFor` has no `kMinStrokeDevicePixels`, and the class has no `devicePixelRatio` field). That is one of the design spec's five PERMITTED divergences (spec:175), left for Task 10 to weigh.
Task 6: fix round 1/5 dispatched — add a non-uniform/sheared residual fixture that kills the a/d cross-swap, and correct the allocation claim.
Task 6: fix round 1/5 (1 addressed + report wording corrected, 0 open; commits eba3dfa..8961ba4). Re-reviewer ran the a/d cross-swap itself: both fixtures went red on the marker's *position* (point_shape_test.dart:108 expected (6,9,6,9) got (3,7,8,12); draw_sink_test.dart:317 expected Rect(14.5,25,15.5,26) got Rect(11.5,29,12.5,30)) — geometric, not count. All three new residuals checked for `a != d`, real shear (`b != -c`) and an off-origin probe. Hand-recomputed one expected value and it matches.
Task 6: complete (commits 2c9415f..8961ba4, review clean after 1 fix round)
Task 7: implemented by subagent ad6610f7 (commit 3351232). Review APPROVED, spec ✅ on the brief and all three accumulated items. `backend=` confirmed sourced from `DraftCanvasState.resolvedBackend`, not echoed input; an unrecognised BACKEND value throws a StateError at load rather than falling back silently — the Plan 3c `TEXT=1` failure cannot recur here. `analysis_options.yaml` and `project.pbxproj` both absent from the commit.
Task 7: ⚠️ resolved by the controller — the reviewer flagged that the report's two device transcripts were excerpted inconsistently (the canvas excerpt omitted the `+1/+2/+3` status lines the vertices one kept) and could not reach the raw logs. I read them: `scratchpad/run_canvas.log` and `scratchpad/run_vertices.log` are genuine captures, both contain the omitted lines, and the report's excerpts are hand-trimmed for readability, not reconstructed. No finding.
Task 7: DEVICE RESULT — Task 2's open question is settled. Two `flutter drive --profile -d macos` runs, 242 frames each, `backend=` differing between them, no crash: disposing a `Vertices` immediately after the `drawVertices` that submitted it is safe on a real Impeller rasteriser, not only under `PictureRecorder` + `toImage()`.
Task 7: PHASE A MEASUREMENT (10,000 entities, macOS profile, R2 pan/zoom, 242 frames). canvas: build p50 16.01 / p95 18.24 ms, raster p50 58.71 / p95 79.12 ms, canvasCalls 39631. vertices: build p50 7.23 / p95 8.17 ms, raster p50 9.77 / p95 32.20 ms, canvasCalls 18, triangles 166279, drawVerticesCalls 19. Backend-independent fields identical across the two runs (screenSpaceLeafCount 1664, dashSpans 37376, collapsed 238, frames 242), which is what makes the pair a controlled comparison. Note the triangle count is post-joins and post-seam, so this is not comparable to the spike's pre-join figures.
Task 7: complete (commits 8961ba4..3351232, review clean, no fix round). PHASE A COMPLETE.
Task 8: implemented by subagent ad27846b (commit 77472f6). Review APPROVED, spec ✅, no findings, no fix round.
Task 8: reviewer confirmed the implementer's two flagged items independently. The typedef move is justified — pasting it where the brief's snippet showed would have reattached the class's ~80-line design doc to the typedef. And the "call the observer after disposal" mutation genuinely cannot be made to fail: `Vertices.raw` copies into native memory synchronously, so the 3-line window between `dispose()` and the assert block that records `_lastFlushDisposed` has no observable effect through any public API. An honestly unfalsifiable window, recorded rather than papered over.
Task 8: note — the "call after rewind" mutation goes red through `expect(disposedAtCallTime, isFalse)`, not through the length assertions, because `Float32List.sublistView` locals are fixed-length views that do not reflect the rewind by content. The property is pinned; the assertion that pins it is not the one the test's own comment implies.
Task 8: complete (commits 3351232..77472f6, review clean, no fix round)
Task 9: implemented by subagent a6f04e1b (commit ced1a06). Review NOT APPROVED — but on calibration, not correctness: the reviewer computed the fill rule, both windings, the clip and three hand-computable cases independently and found no defect in the implementation. Both of the implementer's changes to the brief upheld (the brief's RGBA marker was wrong given the `0xAABBGGRR` pack; the brief's clip fixture genuinely covered the whole canvas).
Task 9: three mutations survive the suite. (1) Important — reversing the `observe` loop leaves all 11 green, so nothing pins triangle order *within one buffer*, which is the one property the sink is built around. (2) Important — `x <= maxX` to `x < maxX` leaves all 11 green, so a triangle running off the right or bottom edge could silently lose its last column. (3) Minor — hardcoding alpha to `0xFF000000` leaves all 11 green; every fixture uses opaque colours, while `_coveredArgb` deliberately fades sub-pixel strokes.
Task 9: further minors in the same round — one `// MUTATION:` comment names a mechanism the test never reaches (the reviewer applied it and the named pixel still inks); the winding assertion `expect(cw.inked, ccw.inked)` is vacuous and survives a fill-killing mutation; the brief's stub end-to-end test is a tautology superseded by the implementer's own; `inked` does not bounds-check x; the "RGBA" wording is host-endian-dependent; and the flat-fill-from-vertex-0 assumption is undocumented.
Task 9: fix round 1/5 dispatched to the original implementer — all fixes are assertions and comments; no production change beyond the bounds check.
Task 9: fix round 1/5 (7 addressed, 0 open; commits ced1a06..79d6529). Re-reviewer ran both Important mutations itself: reversing the `observe` loop now fails exactly one test, the new intra-buffer ordering probe, on a colour assertion (Expected 16711680, Actual 255) — not a count. `x <= maxX` to `x < maxX` now fails exactly the new `(7,0)` clip assertion. Finding 4's corrected comment matches the real mutant behaviour. Scan conversion byte-for-byte unchanged; 11 to 12 tests with one stub deleted and two added.
Task 9: minor (deferred): the `pixels` doc claims host-endianness holds on "every platform this repository builds or tests on" — asserted, not verified against a build-target list.
Task 9: complete (commits 77472f6..79d6529, review clean after 1 fix round)
Task 10: implemented by subagent a92ae31e (commit 292f158). Review NOT APPROVED — 1 Critical, 3 Important, 3 Minor. All 14 pre-existing canvas PNGs byte-identical (md5-compared), no lib/ file touched, Task 1's carried minor closed, both harness fixes confirmed genuine (paint 3 bit-identical to paint 2, so the captured buffer is what the first paint submitted).
Task 10: THE BLANK GOLDENS — cause established by measurement, and it is not the one the implementer's report gives. dpr in the ladder widget tests is 3.0; `_rule` lineweight 9 gives 0.3402 logical px against a floor of 1.0/3.0 = 0.3333, so neither `_halfWidthFor`'s clamp nor `_coveredArgb`'s fade fires. The sink emits the rule correctly (rung 1: 2 triangles spanning x in [199.8299, 200.1701]). `TriangleRasterizer` samples pixel centres at k+0.5, and that band contains none. The rasterizer is built at logical-pixel resolution while the sink's width math is calibrated in device pixels — a structural blind spot. The reviewer proved the consequence: mutating `polyline` to return immediately (no geometry at all) left `text_ladder_1` and `_2` vertices goldens GREEN.
Task 10: Ruling: rasterize at device resolution (buffer 400*dpr x 300*dpr, positions scaled by dpr) rather than excluding the text ladders, raising a lineweight, or accepting documented blanks. — Why: Impeller rasterizes at device resolution, so a logical-resolution scan-converter was always sampling something Impeller does not; this makes the instrument more faithful rather than merely louder. The 14 PNGs are new in this task and pin nothing yet, so regenerating them costs nothing, and the fill loop grows 9x to about 1.1 Mpx, still milliseconds. Cost if wrong: a slower golden suite and one more regeneration.
Task 10: Ruling: land the minimum-ink assertion in all three helpers even though the device-resolution fix makes the current blanks ink. — Why: the two fixes protect against different futures; without the assertion the harness cannot distinguish "drawn" from "nothing drawn" for any of the 14. Cost if wrong: three redundant lines.
Task 10: the spec's permitted sub-pixel divergence is NOT exercised anywhere in this suite — no fixture reaches the floor (ladders 0.3402 logical vs 0.3333; dash 1.134; stroke_width 4.0 at dpr 1.0). Task 11 or the results note must say so rather than let a reader assume it is covered.
Task 10: minor (deferred): the vertices branch rebuilds the widget tree instead of reusing `_at`/`_framed`, so the two backends' trees can drift silently.
Task 10: minor (deferred): the two golden sets are framed differently — canvas goldens are 800x600 full-surface captures because `kCanvasKey` sits on a `SizedBox` and `matchesGoldenFile` walks to the root boundary, while vertices goldens are the 400x300 canvas alone.
Task 10: minor (deferred): `stroke_width_golden_test.dart:191` pins dpr 1.0 while the two ladders run at the binding's 3.0 — two device-pixel regimes in one suite, documented in neither.
Task 10: WHAT THE GOLDENS SHOW (reviewer opened all 14): dash_ladder_2's circle has NO notch at three o'clock at 10x zoom — Task 5's seam holds. stroke_width_0_5x/_1_0x/_8_0x show no crack at any miter base — Task 4's two-triangle miter holds. anisotropy_bypass shows uniform width on every edge including the 8:1 stretch.
Task 10: the reviewer's own mutation — suppressing the bevel triangle in `_emitJoin` — moved exactly 3 vertices goldens and zero canvas ones. That is the hairline crack the spec names, so the surviving goldens do pin the geometry.
Task 10: fix round 1/5 dispatched — device-resolution rasterization, minimum-ink assertions, corrected causal record, one doc comment.
Task 10: fix round 1/5 (5 addressed, 0 open; commits 292f158..002b5e0). Re-reviewer ran the negative control itself — `polyline` returning immediately now turns ALL 14 vertices goldens red, isolating the two that share an aborting `for` loop to confirm each independently. dpr confirmed derived, not hardcoded: the ladders read it from `MediaQuery.devicePixelRatioOf` through the sink, `stroke_width` names `_kVerticesDevicePixelRatio = 1.0`, and the suite passes at both regimes at once. `stroke_width_*` PNGs are absent from the diff entirely, which is what a dpr-1.0 no-op scale should produce. The 14 canvas PNGs still byte-identical.
Task 10: the five regenerated text-ladder PNGs each show the anchor rules at exactly the canvas version's positions with glyphs correctly absent — text goes to `CanvasDrawSink` and never enters the vertices buffer.
Task 10: minor (deferred): `stroke_width_golden_test.dart`'s three-zoom `for` loop aborts on the first assertion failure, so one mutation can mask whether later zooms would also have caught it. Pre-existing test shape, not a regression from this fix.
Task 10: complete (commits 79d6529..002b5e0, review clean after 1 fix round)
Task 11: first implementer (aa531539) died on an OAuth expiry, not on the task, after 238k tokens spent fighting the widget-test route — 19 throwaway probe files and a test that did not finish in 7 minutes. Re-dispatched fresh on opus with the diagnosis and a hint: drive both sinks directly over one op sequence the way `stroke_width_golden_test.dart` already drives `CanvasDrawSink`, with no `DraftCanvas`, no `pumpWidget` and no nested `runAsync`. The direct route runs the whole file in under a second.
Task 11: implemented by subagent a5506592 (commit 405dd90). Review APPROVED conditional on one fix. THE RESULT: stray = 0, uncovered = 0 on both agreement fixtures, no widened tolerance. The reviewer instrumented its own probe and reproduced every number rather than trusting the report (dpr 3.0, 1200x900, canvas 22398 / vertices 23241 / 0 / 0 / 2 flushes; direct ops 18291 / 18906 / 0 / 0 / 1). It rendered the diff images and read them: a visibly elliptical circle, the oblique corner, the 160 degree fold, the open arc, the point square and a closed pentagon with a joined seam — every brief-required fixture element present and inking.
Task 11: why the zero is trustworthy — the comparison is symmetric over the same two masks with the same `_nearInk`; the text mask hides 61,631 canvas ink pixels and ZERO vertices pixels, so it excludes glyphs and not geometry; and the thresholding is conservative in the dangerous direction, canvas ink being the strict alpha >= 0xC0 core while vertices ink is all coverage, which makes stray harder to zero than to inflate. Every mutant red landed on a geometric assertion, never on a floor or a count.
Task 11: Important — `sink_comparison_test.dart:167-170`, the full-sweep-ARC characterisation, calls `measureAgreement` directly and so bypasses `expectSinksAgree`'s ink floors, with no upper bound on uncovered. The reviewer made `VerticesDrawSink.arc` return early for sweep > 6.0 — the vertices side emitting nothing at all — and all five tests passed. The one test whose job is to hold a known divergence still is the one that passes vacuously. Third occurrence of this shape in this plan.
Task 11: ARCHITECTURAL FINDING for the results note — the seam join and Task 6's point-shape fix have NO coverage through any frame path, not the goldens and not `draft_painter_*`; they are pinned against the `DrawSink` interface only. `draft_painter.dart:425-429` routes point, line and polyline unconditionally through `_emitScreenSpace` whose residual is a bare `Transform2.translation` (:498), so a rotated residual never reaches `point`, and `_emit`'s `case EntityKind.point` (:566) is dead code. The seam is *unobservable* through the painter rather than unreachable — the painter does reach `closed: true` via `sink.circle` (:588, :619).
Task 11: the 2*pi ARC question is answered — the corpus cannot produce one (`generate_document.dart:617` caps at [0.25pi, 1.75pi), `:540` at [0.5pi, 1.5pi), the only two arc producers) and there is no DXF reader in the repository at all (`lib/src/codec/` holds only `json_codec.dart` and `schema_version.dart`). The reviewer re-derived the sub-pixel argument and it holds: steps = theta*sqrt(R/8*tau) gives a notch of about half*sqrt(2/R), which at half 2 px and R 40 px is 0.45 px, under the dilation. Characterised, not fixed.
Task 11: minor (deferred): `lib/src/draft_canvas.dart:192`'s devicePixelRatio plumbing is referenced by no test file; the report should not be read as claiming coverage of it.
Task 11: fix round 1/5 dispatched — ink floors on the ARC characterisation, two comment corrections, and the sharpened claims for the report.
Task 11: fix round 1/5 (1 addressed + 2 comment corrections, 0 open; commits 405dd90..c678ec3). The implementer swept the same shape further on its own and found a second vacuous test: the anisotropy comparison's `stray > 1000` and `verticesInk > canvasInk` are both satisfiable by an empty CANVAS side. Re-reviewer verified that algebraically, ran both mutants (arc returning early for sweep > 6.0 → red on `verticesInkPixels > 4000`, Actual 0; circle emitting nothing → red on `verticesInkPixels > 10000`, Actual 0), and confirmed no `measureAgreement` caller is left unguarded. Floor margins judged well-chosen: 4000 against measured 5688/5710, 10000 against measured 19194/24580. `lib/` byte-identical to 002b5e0.
Task 11: minor (deferred): `writeDisagreementImages` ships unused. Both reviewers judged it justified — it is the executable half of "do not widen the tolerance, look at the images", and both used it to produce the images they read.
Task 11: complete (commits 002b5e0..c678ec3, review clean after 1 fix round). PHASE B COMPLETE.
Task 12: measured by subagent a764ec55 — no commits, readings only, tree clean at c678ec3. Review APPROVED. The reviewer recomputed all 10 medians by hand from the pasted transcripts and every one matches; checked all 30 blocks' `backend=` against their defines with no mismatch; and spot-checked one cell itself (10k vertices: build p50 5.40 vs reported 5.71, raster p50 7.22 vs reported 6.68, within 5-8%, with every invariant field matching exactly).
Task 12: DESKTOP ROWS (macOS profile, median of 3, build p50 / raster p50). 10,000 — canvas 12.35 / 44.32, vertices 5.71 / 6.68. 50,000 — canvas 15.36 / 66.94, vertices 7.07 / 8.53. 500,000 — canvas 44.29 / 508.00, vertices 17.44 / 21.64. R4a@50k — canvas 15.96 / 67.11, vertices 8.73 / 4.71. R4b@50k — canvas 17.34 / 64.89, vertices 7.26 / 3.65. No crossover anywhere; the raster margin widens 6.6x, 7.8x, 23.5x. Nothing failed, no OOM at 500,000.
Task 12: R2's three pairs are controlled — `screenSpaceLeafCount`, `dashSpans` and `collapsed` match exactly across backends at each size.
Task 12: Ruling: the R4a/R4b invariant fields were dropped by Step 3's grep pattern, so those two pairs' control is plausible but not demonstrated. Record the gap rather than re-run the sweep. — Why: they are secondary rigs, the corpus is deterministic at a fixed ENTITIES, and re-running costs over half an hour of device time to confirm something the R2 pairs already establish for the same corpus. Cost if wrong: the note claims control for two rows it cannot show. Task 15 must state that plainly rather than assert control.
Task 12: Ruling: the `git checkout` used to revert a temporary instrumentation line in `frame_timing_test.dart` stands, and is not a precedent. — Why: the file carried no other pending change and the added line only printed an already-shipped getter, so nothing was at risk; the rule exists because uncommitted work was lost, and an implementer cannot always be sure that is not the case. Future dispatches keep the `cp` rule as written. Cost if wrong: none here, verified by the clean tree.
Task 12: FOR THE RESULTS NOTE — peak vertex-buffer capacity at 500,000 entities is 8,388,608 vertices, and at 12 bytes each that is exactly 96.00 MiB pinned for the widget's life. The doubling reserve never shrinks. That is a cost the canvas backend does not carry and the note must state it.
Task 12: FOR THE RESULTS NOTE — the widening raster ratio is framed against entity count, but the real driver scales far more slowly: from 50k to 500k entities, a 10x rise, `screenSpaceLeafCount` and `dashSpans` grow only about 2.1x and 3.0x. The note needs that caveat so the ratio is not read as linear in entities.
Task 12: minor (deferred): R4a's command time is backend-dependent (canvas ~0.05 ms, vertices ~0.12 ms, consistent across three runs each) for a pure document mutation that should touch neither sink. Tens of microseconds; plausibly a cache or allocator artifact of the much larger vertices buffer, not established as a real backend cost.
Task 12: complete (no commits, readings only, review clean)
Task 13: first attempt reported BLOCKED after root-causing two real chromedriver problems (a Gatekeeper-rejected ad-hoc Homebrew binary hanging at `_dyld_start`, then a 148-vs-151 version mismatch) without unhanging `flutter drive -d chrome`, which stalls past device re-validation before reaching Chrome-launch code. `flutter devices` lists Chrome here, so the controller re-dispatched on a second route: drop `flutter drive` and chromedriver, share the R2 rig so the harness drives itself, and use `flutter run -d chrome --profile`.
Task 13: THE WEB DEFAULT FLIPS. CanvasKit's `drawVertices` beats its `drawPath` decisively. 10,000 — canvas 117.80 / 79.30 ms, vertices 6.80 / 1.40 ms. 50,000 — canvas 155.70 / 107.90 ms, vertices 8.90 / 1.80 ms. `defaultRenderBackend()` now returns `RenderBackend.vertices` unconditionally (commit 25bed0d). This is the design's own stated consequence, not an implementer's invention. 500,000 was not run on web, which the brief's Step 3 authorises verbatim.
Task 13: review APPROVED conditional on a doc-comment caveat, since landed (9f1a3a7). The reviewer traced all 12 figures to real captures — 10 blocks in /private/tmp/web_rows.log, 2 in the session transcript — and found nothing invented. The retrieval path is sound: a `ZoneSpecification.print` appending whole lines to one StringBuffer, written once to `localStorage['r2Result']` after the rig completed, read back through a single `Runtime.evaluate(..., returnByValue)`. No scraping, no fragment assembly, no truncation path. `backend=` reads correctly in all 12 blocks; median-of-3 confirmed for every cell.
Task 13: THE HEADLINE IS NOT WHAT JUSTIFIES THE FLIP. `build` is commensurable across platforms, `raster` is not. Web vertices build is 3.22 us/leaf against macOS's 3.43 — the same Dart-side work at the same cost. Web canvas build is 2.23 us/call against macOS's 0.31, the expected CanvasKit JS-interop penalty. But a web vertices raster of 1.40 ms against macOS's 6.68 ms on 34% MORE geometry is not a credible GPU measurement: CanvasKit's `rasterDuration` almost certainly ends at command submission. The flip rests on the within-platform ratios and the 17.3-17.5x build ratio, not the 56-60x raster figure. Also the two platforms did not draw the same drawing — desktop 10k is 1664 leaves / 37,376 dashSpans, web 10k is 2111 / 50,120, a different viewport at 1200x723@2.
Task 13: Ruling: the raw web captures and the retrieval scripts go into the repository beside the results note, not just into this ledger. — Why: the rows cannot be re-taken from what was committed. They ran under a `main.dart` in neither commit (a `runZoned` + `dart:html` capture) plus two /tmp scripts, and the committed `RUN_R2` mode produces no output at all on this machine because profile mode never links a debug service here. A published row nobody can re-take is a number nobody can check. I have copied all four artifacts to `<workspace>/task-13-raw/`; Task 15 commits them. Cost if wrong: four small files in docs that nobody reads.
Task 13: FOR THE RESULTS NOTE — three things the reviewer requires be carried. (1) Commensurability: the desktop and web tables cannot be merged into one, and `raster` is not comparable across them. (2) Non-reproducibility: the rows cannot be re-taken from the committed path, and the note must say so. (3) Task 10's rationale for keeping the 14 canvas goldens — "canvas is the web renderer, so they stop testing code nothing draws through" — is now VOID. It survives only in the plan document (lines 2109, 2243-2244) and Task 10's report, in no code comment or test. If the note repeats it, the note contradicts itself: those goldens now pin a backend no platform defaults to.
Task 13: `CanvasDrawSink` is NOT dead after the flip. `DraftCanvasState._attach` constructs `VerticesDrawSink(..., fallback: sink)`, and the vertices runs show `canvasCalls=19`/`24` — it draws text and fallbacks on every frame.
Task 13: minor (deferred): `frames` does not match across each pair (10k vertices 238/240/241 against canvas 241/241/241; 50k vertices 241/239/233 against 241/241/241), from `onReportTimings` batching at the tail in app-run mode. Every other invariant matches exactly. Does not move a p50, but the note must not claim the frame counts matched.
Task 13: minor (deferred): the published median mixes runs — 10k canvas 117.80 / 79.30 pairs run 3's build with run 2's raster, and no single run produced that pair. Same convention as Task 12; state it rather than change it.
Task 13: minor (deferred): every measured run executed inside a `runZoned` the shipped code does not have. Both backends equally, so the ratios hold, but the absolute figures now in a doc comment were taken under code that is not the committed code.
Task 13: minor (deferred): `localStorage['r2Result']` was never cleared at startup, so a stale prior-run read was possible in principle. Not observed — every block's `backend=` and `R2 (N)` matched its own run's defines.
Task 13: complete (commits c678ec3..9f1a3a7, review clean after 1 fix round)
Task 14: implemented by subagent aacb5c11 (commit 927567a) — `docs/superpowers/notes/plan-3d-mutation-log.md`, 362 lines, 39 mutants accounted for. TWO SURVIVORS FOUND AND KILLED, which is the task doing its job: A1 (a `Paint` allocated per flush) survived because `paint_allocation_test.dart` measured buffer capacity only and was blind to a `Paint` object; S2 (colour grouping before the flush) survived because the existing test read the PRE-flush buffer, correct by construction, and could not see a reorder inside `flush()` itself — which is where the sink's real historic bug lived.
Task 14: review NOT APPROVED — 3 Important, 3 Minor. The reviewer re-ran both survivor remedies and both killed their mutants with output byte-identical to the log, spot-checked eight further entries fresh (S6, S8, E20_one, E20_all, the B1 proxy, J5, V1, J7 — all held, J7 dying on the geometric wedge probe with the count still 6), and verified the two cited entries against this ledger.
Task 14: Important 1 — A1 is recorded as killed but another form of the same mutant survives the whole suite. `canvas.drawVertices(vertices, BlendMode.dst, Paint()..color = const Color(0xFFFFFFFF))` — a per-flush Paint that never touches the field — passes `paint_allocation_test.dart` and all 237 tests. The log claims "the Paint is not allocated per flush"; the suite pins only "the field is not reassigned". Same shape this plan has hit repeatedly: the assertion is narrower than the mutant's name.
Task 14: Important 2 — B1's row names one mutation and runs another with no caveat in the log. The equivalence argument is sound (`resolvedBackend` assigned once at draft_canvas.dart:140, handed to the rig at main.dart:288, `defaultRenderBackend()` unconditional), but the stale-cache proxy actually run is a different property, and the design's own stated fixture — reading the backend back from both the widget and the rig — has no test at all. The report claims the log explains the equivalence; the log has one table row and no note.
Task 14: Important 3 — `cosHalf <= 0` and `mlen == 0` in `_emitJoin` are unreachable given the ~151 degree bail, and the reviewer disabled each alone with all 46 tests staying green. The log frames all three bails as live layered guards, silently counting two dead branches as covered. An unreachable branch does not want a test; it wants removal or a comment saying why it stays.
Task 14: minor — S2's test is one indirection from the submitted buffer: reordering by colour AFTER `observer?.call` and handing the grouped arrays to `Vertices.raw` survives the whole suite, goldens and sink_comparison included. The logged mutant still dies so the kill stands, but the log overstates what the test constrains.
Task 14: minor — headline arithmetic ("the other twenty-two kills" should be twenty; Part 2's heading says 33 while line 201 says 21 rows), and the seam gap's stated reason is wrong though its conclusion is right (the painter DOES reach `closed: true` via `sink.circle`).
Task 14: fix round 1/5 dispatched — tighten or honestly downgrade A1, add the B1 note, move the two unreachable branches to the gap list, plus three wording corrections.
Task 14: fix round 1/5 (6 addressed, 0 open; commits 927567a..4a16b41). Re-reviewer ran the call-site-local Paint mutation itself and it now dies exactly on the new assertion — "flush hands drawVertices the same Paint object every time, not a call-site-local one", Expected true / Actual false — with the round-1 `debugPaint` test and all 236 others staying green. Confirmed `SpyCanvas` reads the real object via `noSuchMethod` capturing `invocation.positionalArguments` from the actual `drawVertices` call, through the pre-existing public `sink.canvas` field with no test-only production hook, and that it pins identity ACROSS two flushes rather than within one. Tally 14+21+2+2+2 = 41 checks out. No `lib/` change in the fix round.
Task 14: complete (commits 9f1a3a7..4a16b41, review clean after 1 fix round)
Task 15: implemented by subagent ad840950 (commits 2f6223e, 787a914). THE EXIT GATE: 1 PASS, 2 PASS (qualified — B1 killed via an equivalent mutation, disclosed), 3 PASS (stray 0, uncovered 0), 4 PASS (14 fixtures x 2 backends, 28 PNGs, 23 golden tests), 5 PASS (vertices 10k build p50 5.71 [5.67, 5.81], raster p50 6.68 [6.63, 6.75], worst single reading 6.75 against a 16.67 budget), 6 PASS at both sizes (50k gap 58.22 ms, 500k gap 485.01 ms, intervals disjoint, no crossover so the design's tie fallback never fires), 7 MEASURED BUT NOT CLOSED (the CLAUDE.md amendment is the human's), 8 PASS (default vertices everywhere, justified by the 17.3-17.5x build ratio and not the raster headline).
Task 15: review APPROVED. The reviewer re-ran the three-package gate itself, recomputed criteria 5 and 6 from Task 12's raw p50s and matched every digit, and verified `CLAUDE.md` is untouched across all branch commits with no `analysis_options.yaml` anywhere. All nine of the ledger's required corrections are carried and none softened, plus eight more the implementer carried unprompted.
Task 15: fix round 1/5 (4 addressed; commits 2f6223e..787a914). The implementer found the problem was worse than reported — the ten captured block headers in `web_rows.log` are MIS-ORDINALLED, the block headed "10000 canvas run 1" carrying the report's run 2 — so it added a provenance banner and recovered both missing blocks as marked RECONSTRUCTIONS, writing three unrecoverable fields as `<not recovered>` rather than inferring them from neighbours. Re-reviewer verified the mapping against the Task 13 report and confirmed both recovered blocks match byte-for-byte.
Task 15: correction of record — `main` and `origin/main` are BOTH at `bb67137`. STATUS.md's "29 commits ahead, nothing pushed" was false, and so was the working assumption carried into this run that local main was ahead of origin.
Task 15: Ruling: STATUS.md must not carry a commit count that the act of writing it falsifies. — Why: the fix computed 24/32 with HEAD before the fix commit, which then landed inside the range making them 25/33; editing those to 25/33 would make them 26/34. The defect is the shape of the claim, not the number. Anchor it to a named commit or give the range and let the reader run it. Cost if wrong: a slightly less convenient STATUS.md.
Task 15: fix round 2/5 dispatched — make the commit count stable, and sweep STATUS.md for any other figure that editing the file changes.
