# Ledgers — the raw per-task record

`notes/` holds **results of record**: the conclusions, written to be read.
This directory holds the **working record they were drawn from** — the
per-task ledger, the implementer briefs, the reviewer reports, and the
`review-<from>..<to>.diff` package each review was taken against.

It exists because the SDD workflow keeps that material in
`.superpowers/sdd/<plan-slug>/`, which is **git-ignored** and lives inside a
worktree. When a plan's branch is merged and its worktree removed, everything
in it is gone — and for Plan 3c the ledger was the only place several findings
were written down at all, because TodoWrite was unavailable in the sessions
that ran it.

**Archived on merge, not maintained.** A ledger here is a snapshot of what was
true when the branch landed. Nothing appends to it afterwards; if a later plan
revisits a decision, the record of that lives in that plan's own ledger. The
live one for a plan in flight is still `.superpowers/sdd/<plan-slug>/` in its
worktree.

## What is here

| Plan | Contents |
|---|---|
| `2026-08-17-jet-cad-2d-plan-3c-text/` | Plan 3c (text), 15 tasks. `progress.md` carries **56 numbered rulings** — the decisions taken while executing, each with its cost-if-wrong — plus every task's mutation table. Beside it, task briefs, task reports, and ten review diffs. |
| `2026-08-20-jet-cad-2d-plan-3d-vertices-sink/` | Plan 3d (the vertices sink), 15 tasks. `progress.md` carries **14 rulings** and the finding from every review round — including five mutations that reviewers ran and implementers had not, each of which left a green suite hiding a real gap. Beside it, task briefs, task reports, the whole-branch fix report, and twenty-six review diffs. Its own `README.md` says what the directory holds and what was committed to the repository proper instead. |
| `2026-08-25-jet-cad-2d-plan-3h-pan-frame/` | Plan 3h (the pan frame), 8 tasks — though Task 1 was dissolved into Tasks 7 and 8, and Task 8 itself split into 8a/8b, both times because Low Power Mode blocked device work. `progress.md` carries **19 numbered rulings**, one of which (15) was later withdrawn on hard evidence and reversed by Ruling 19. Six mutants ran; two survived, one of them (M5) found by a reviewer after the plan was written and initially thought to have no unit witness until it turned out to also kill a fourth mutant (M4) the plan had asserted was unwitnessed. The headline criterion missed its gate (2.35 against 2.4) and the controller ruled the miss stands rather than move the gate. The final whole-branch review found a shipped line, `canvas.translate`, with no test witness at all; the fix wave that gave it one also found the anti-vacuity clause meant to catch that class of gap was itself measuring the wrong rectangle, recorded as gap H7. Beside `progress.md`: every task brief and report, `final-fix-report.md`, a second `record-fix-report.md` for a prose-only correction pass, and fourteen review diffs. Its own `README.md` walks the chain of rulings in full. |

| `2026-08-26-jet-cad-2d-plan-3i-zoom-frame/` | Plan 3i (the zoom frame), 14 tasks, plus eight fix waves (A–H) and a whole-branch review that ran **before** the device runs. `progress.md` carries **21 numbered rulings**. Thirty-nine mutants fired across thirty-six entries in the mutation log; thirty-seven died, and the two survivors are both deliberate — **M8** declared a survivor before it was fired, and **M24** proved unkillable because the ceiling property it targets is held by the rest bake's up-front pricing rather than by the recency stamp it deletes. Its exit gate is **9 of 11**: criteria 8 and 9 both miss and neither threshold was moved. Two of the plan's own instruments were found vacuous by firing a mutant and watching it live (`captureLive` returned the tiled image byte for byte; `pumpTiled`'s canvas was never the viewport it claimed), and Ruling 21 records a whole n=9 device run that was **taken, read and discarded** because its arms were wired around a phase the flag cannot reach — the log for it is kept. Beside `progress.md`: every task brief and report, eight fix-wave reports, fifteen review diffs, and a `measurement-logs/` directory holding the eight raw `KEEP_*.log` device transcripts behind every published figure, the discarded run included. |
| `2026-09-21-floor-planner-app-skeleton/` | Plan 01 (the floor planner app skeleton), 10 tasks. `progress.md` carries the baseline rulings, the pre-flight conflict scan, eight execution rulings (the `ScrollSignalAction` rename, the placeholder `main.dart`, the ramped pinch fixture that killed M-01b, the OWED look) and every deferred minor; beside it, ten task briefs and reports, the final-review fix report, and every review package including the whole-branch one. |
| `2026-09-21-interaction-core/` | Plan 02 (the interaction core), 12 tasks, executed 2026-09-21..22. `progress.md` carries the pre-flight conflict scan, four process rulings (P-1 instances inside groups stay out of the band rule, P-2 model tiers, P-3 the commit trailer check, P-4 no implementer mutation sweeps), twenty-odd execution rulings (the singular instance transform judged forward, `toggle` net-change notify, `activate` unhooks before cancel, the `SelectionOverlayPainter` rename, the guarded `onExit`, the point cross, `ui.Path.getBounds`'s conic hull) and every deferred minor; eight of twelve tasks took one fix round each; the final whole-branch review found four cross-task seams (the query-filter asymmetry across the four container walks, the band corner at press time, no undo binding, no repaint source for a document change), fixed in one wave and re-reviewed clean; five residuals parked with rulings. Thirty named mutants, 28 fired and killed, 2 equivalent by construction (M-02b re-fired at the spec's own call site). Exit gate 14 of 15, criterion 15 (the human's look) OWED at archive time. Beside `progress.md`: twelve task briefs and reports, the final-fix brief and report, the reviewers' constraints file, the rulings and deferred-minors extracts, and every review package including the whole-branch one. |
| `2026-09-22-page-grid-rulers/` | Plan 04 (page, grid and rulers), 12 tasks, executed 2026-09-22 in one day on a spec that two reviewers (Codex, an Opus subagent) had taken from rev 1 to rev 2 — three blockers, among them a root-handle component edit that rebuilt the whole spatial index (spec D13 is the fix). `progress.md` carries the pre-flight conflict scan, nineteen execution rulings (04-1 the startup history cleared; 04-7 `pick` takes `minorMinPixels` because the shipped 64/8 thresholds could never reach the null-minor branch; 04-11 imperial rungs as foot fractions for bit-exact coincidence; 04-13 the seeded sweep placed on the sheet after it proved vacuous in 50 of 50 trials; 04-14 the major pass writes after the minors' span; 04-16 the fit deferred to a post-frame callback; 04-19 `components` declared lowest so a mixed compound reconciles) and every deferred minor; three tasks took one fix round each (4, 6, 9); the final whole-branch review found the enum-order hole under the D13 skip and three tests that passed for the wrong reason, fixed in one wave and re-reviewed clean. Twenty-three named mutants fired and killed (M-04q re-fired once because its first form did not compile). Exit gate 15 of 16, criterion 16 (the human's look) OWED at archive time. Beside `progress.md`: twelve task briefs and reports, the mutation anchors the controller located for Task 11, the final-fix brief and report, the reviewers' constraints file, the deferred-minors extract, and every review package including the whole-branch one. |
| `2026-09-23-grips-and-transform/` | Plan 03 (grips and transform), 12 tasks, executed 2026-09-23 on a spec two reviewers (Codex, an Opus subagent) took from rev 1 to rev 2 — three blockers: the root's transform is not in the world mapping (canvas, index and oracle start at identity), an undo could land mid-drag, and the selection box came from `Path.getBounds`. The plan (drafted by an Opus subagent) carried 21 rulings, eight of them where the spec was wrong against the code. `progress.md` carries the pre-flight scan, the controller's rulings (T2-a truthful per-model commit trailers; T7-a/b; T11-a; T12-a; F-a/b/c) and every deferred minor; Task 7 took one fix round (a camera-listener leak and a centre-grip base that no test could catch, plus a stale-grip crash inside the slop); Task 10's addendum turned the per-task reviews' coverage gaps into seven new tests; the final whole-branch review found grip draw positions, the rotation disc and the circle reshape preview unguarded, fixed in one wave that also exposed a b/c-transposition blindness under the reflecting test camera (parked, offered as a follow-up). 68 mutants exercised: 65 killed, 1 designed survivor (M-03e), 2 equivalent. Exit gate 15 of 16, criterion 16 (the human's look) OWED at archive time. Beside `progress.md`: twelve task briefs and reports, the controller's standing implementer/reviewer instructions, Task 10's addendum, the final-fix list and report, the mutation backups, and every review package including the whole-branch one. |
| `2026-09-23-drawing-tools/` | Plan 05 (drawing tools), 11 tasks, executed 2026-09-23. The plan carried Rulings 05-1…05-15; `progress.md` adds the controller's own (P-1 this session's worktree hosts the branch; P-2 the ledger is archived, not deleted; P-3 the model tiers; P-4 Tasks 9–11 are dispatched too; P-6 truthful per-model commit trailers) and every task ruling (T3-a the hover-exactness and Tolerance fixes; T5-a AR5 replaced because it could not fail; T7-a spec D3 outranks the plan's own Review Focus wording, so a shortcut is swallowed mid-shape; T7-b/c/d the focus-lifecycle null check, the Escape guard and the `Flow` root; T9-a the PL8 fixture fix that killed M-05w′). The final whole-branch review returned "With fixes": F-1 (furniture moved clear of every door's leaf and swing), F-2 (F3/F bubble to the shell mid-shape, spec D3 amended) and F-3 (a returning tool no longer paints a stale snap marker) were fixed and re-reviewed clean; F-6 corrected doc slips; F-4, F-8 (the counter's new top leg across the kitchen/living doorway, with a suggested fix) and F-9 (the F3/F modifier guard untested) are parked as debt; F-5 and F-10 are recorded, not fixed. 30 named-plus-review mutants fired, 30 killed, 0 survived, 0 equivalent. Exit gate 13 of 14, criterion 14 (the human's look, fourteen items per platform) OWED at archive time — not merged. Beside `progress.md`: eleven task briefs and reports, the implementer's standing instructions, the closing-commits report, and every review package including the whole-branch one. |

Plans 1, 2, 3a and 3b were merged before this directory existed and their
ledger scratch was deleted; their conclusions survive in `notes/`
(`plan-3a-ledger.md` is a hand-written summary, not the raw material).
