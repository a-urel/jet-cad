# SDD ledger — plan: docs/superpowers/plans/2026-09-25-openings.md

Branch plan-08/openings, cut from spec-08/openings at e30386a (main 357bea6 + spike note 11f26bc + spec 75c6c0f, 48173e2 + plan 9be697f + amendment e30386a), worktree `.claude/worktrees/plan-openings`.
Spec: docs/superpowers/specs/2026-09-25-openings-design.md (revision 1, amended: R1 inset threshold line; D14 swing side from the band's midline). Spec review skipped on the human's instruction (as 07).
Brainstorm: 19 human decisions + controller rulings R1–R4 (brainstorm-decisions.md beside this file). Spike: spike/08-openings (634fa7c), never merged; note copied at 11f26bc.
Merge is the human's (--no-ff, from the main checkout). Push only on the human's word.
Environment: Linux x86_64 cloud container, Flutter 3.47.2 / Dart 3.13.2 at /root/flutter. macOS build and the look are the human's.

## Branch-point counts (this container, measured on main 357bea6; the spec branch adds docs only)
- engine +987 -2 (standing hash tests); render +931 ~1 -7 (standing goldens); harness +82 (plan agent, scratch copy of 48173e2); app +157.

## Pre-execution rulings
Controller (2026-09-25): spec open question 1 → R1 amended (gap = one inset threshold line) — 48173e2.
Controller (2026-09-25): plan finding 2 → D14 swing side from the band's midline; Ruling 08-23 rewritten; M-08z3 added (37 mutants) — e30386a.
Controller: the plan's other spec findings (1 count, 3 RC3, 4 hover scan, 5 EP1 collinear, 6 OG1 extended, 7 PlacementTool hook, 8 extents probe, 9 CS7 reachability, 10 barrel/palette) are accepted as the plan handles them — cost: none.

## Tasks
Task 1 (engine references + closure, D2 D3) dispatched with the standing brief.
Human (2026-09-25): push plan-08/openings when Task 6 is done.
Task 1: implementer reports 328df0b (amended from d15be41; the amend touched only references_test.dart). references/referrers/declared maps built in one pass over `order`; D3 closure (core = seeds + neighbours + references, before and after; + referrers of core); debugReferenceCalls; clients Post/Pin (08-2); test-local Peg type. Gates engine +994 -2, render +931 ~1 -7 (on d15be41), app +157, web ✓ (on d15be41). Mutants M-08d, r, r2, t, n + extras X1-self/dead/sort/sort-rev/over all red. Plan wrong: RF1's fixture could not kill X1-sort (ComponentStore.handles ascending per type) → Peg type added. RC2 medians ~5-9 ms vs NC4 ~1.3-3 ms (noisy). Notes for Task 2: diagnose_test.dart declares its own Tag/TagType; PostType.diagnose emits test.referrers — filter by code. Review dispatched (review worktree .claude/worktrees/plan-openings-review at 328df0b).
Task 1: review Approved, with I-1 carried to Task 2. Reviewer re-fired M-08d/r/r2/t/n and X1-* red; own "mutable referrer lists" red RF1:136; gates on 328df0b engine +994 -2, render +931 ~1 -7, app +157, web ✓. Allocation: none for objects without references.
Task 1 I-1: a deleted referent's component is still visible while the edit is planned — _run applies the D8 cleanup (regeneration.dart:386, applied at :418) AFTER _plan calls generate (:399); ParametricView.paramsOf (parametric_system.dart:116) reads components directly, so a referrer sees its dead host (toWorld falls back to identity). Probe: deleteObject(A) → P draws A's bottom edge at the world origin; drift() [3000]. Spec D4's "an orphan sees paramsOf(host) == null" and M-08f0's "childless group" are false on this engine.
Ruling (I-1): fixed in Task 2 — the view answers null (paramsOf) for an object lost in this edit (an object before, not after), and toWorld is not consulted for it, so D4 holds as written; a test pins it (the reviewer's probe) and a mutant (the view reads the component directly) must go red — cost: one test.
Task 1 minors → Task 2's brief verbatim: m-1 before/after halves of the closure not each pinned (drop references_before survives: kill with "delete a Pin" and "re-point a Pin A→C"; drop referrers_after survives: kill with "a loaded Pin names a plain group that then becomes a Post"; drop referrers_before masked by I-1 — Task 2's orphan tests should kill it); m-2 dedup not pinned (a referrer declaring [A, A] in RF1); m-3 RC2: ~7 allocations per referring object per survey, pass ≈20% of a line draw, linear; RC2 needs ≥4 warm-ups; the results note compares RC2 at n with NC4 at 2n.
Ruling (m-1, m-2): Task 2 adds the RF tests — cost: tests only. Ruling (m-3): RC2's warm-ups raised to 4 in Task 2; the comparison goes into the results note (Task 16); no fast path — cost: none.
Task 1: complete (commit 328df0b).
Task 2: implementer reports 5868948 — ReferencePolicy {cascade, orphan}, referencePolicy (default cascade), DanglingReferenceError; _cascade (rounds, one command at a time, inverse folded); _checkDangling (seeds only, declared); _run in D4's order; I-1 fixed (paramsOf null for an object lost in this edit; the view takes the before-survey); diagnostics' _unresolved (parametric.dangling/orphan); Tag (orphan, declares [host, host]); CS1–CS7, LV1, DR1, DR2, RF6–RF8; RC1 extended (a Pin edit = 2×600 calls; diagnostics() = 600); RC2 4 warm-ups. Gates engine +1007 -2, render +931 ~1 -7, app +157, web ✓. Mutants f, f0, f2, v, l, y, X2-node, X2-allobjects, X2-recall (both sites), I-1, m-1a/b/c, m-2, X1-sort, dedup red; X2-fillskip survived — argued equivalent (fills below boundaries in any valid doc; RemoveEntityCommand refuses the inverted case either way). Plan wrong: X2-recall could not turn RC1 red (no seeds in a line draw) → RC1 extended. Deviations: state() comparisons without handleSeed (undo never lowers the seed); DR2/RF8 fixtures hang the referrer off a Pin (a Post ticks per Pin referrer); CS7 reachable.
Task 2 finding: in drift()/diagnostics() (no before-survey) a Tag whose Post was re-parented still sees the Post's component (it stays on the nested group); proposed broader rule: paramsOf answers null for any handle not a live object of the view's survey.
Ruling (finding): adopt the broader rule — paramsOf answers null for any handle that is not a live object of the view's survey (edit, drift, diagnostics alike); taken in Task 2's fix round with the review's findings, with a test (re-parented referent, drift() empty) and a mutant — cost: one test; no caller reads a non-object (implementer checked wall.dart, box.dart).
Task 2 review dispatched (review worktree at 5868948).
Task 2: review Needs fixes. I-1 an empty-plan cascade reports inner.capability (components) — _geometryChanged = plan.isNotEmpty ignores the cascade (regeneration.dart:529) → SpatialIndex/TileCache keep removed entities (probe: 3002 dead, still indexed). I-2 _cascade decides each command (kindAt, _boundaryOf → Handle.checked) outside its rollback → a loaded fill with scalar -7 throws HandleRangeError mid-cascade: r0 + P1's cascade stay applied, no history, no event (breaks D4 step 4). m-1 X2-fillskip NOT equivalent: an unfillable (open polyline) boundary makes the committed cascade refuse the whole delete; the mutant (fill first) lands it; RV-R1/RV-R1b (cascade rollback) survive — no test makes a cascade command throw. m-2 ascending-referent order (08-5) unpinned (RV-R2 survives). m-3 cascade leaves nested nodes under a referrer (validate: tree.parent_child_mismatch); the select tool's _groupCascade recurses. m-4 RV-R4 survives (low value). Observation: re-point + delete in one edit still deletes the referrer (matches the spec). Broader paramsOf rule applied temporarily: engine +1013 -2 (with probes), app +157 — nothing breaks. Gates reverified: engine +1007 -2, render +931 ~1 -7, app +157, web ✓.
Ruling (I-1): _geometryChanged = plan.isNotEmpty || !identical(r, r0); a test on the reviewer's fixture (no neighbour) asserts the capability is not `components` and the index drops P1's child; mutant "drop the cascade term" — cost: one test.
Ruling (I-2): the whole cascade (deciding and applying, every round) runs under the rollback; the loaded scalar -7 fixture pins it (no partial state; the error rethrown; history and changes unchanged) — cost: one test.
Ruling (m-1): the cascade removes a region's FILL before its boundary (the skip goes: RemoveEntityCommand's "always available and never lossy" escape), so a file with an unfillable boundary deletes cleanly and undo restores it exactly; test on the reviewer's open-polyline fixture; RV-R1/RV-R1b must go red on I-2's fixture (a cascade command throws after an earlier referrer's removals applied) — cost: two tests.
Ruling (m-2): a cascade-policy client declaring two distinct dead referents pins the ascending order (RV-R2 red) — cost: one test.
Ruling (m-3): the cascade removes a doomed object's subtree as the select tool does (leaves, instances, nested groups recursively, then the node); test: a nested group under a Pin, delete A, validate clean, undo exact — cost: one test.
Ruling (m-4): not taken — cost: none. Observation: accepted (spec wording).
Ruling (broader paramsOf): taken in this fix round — paramsOf answers null for any handle that is not a live object of the view's survey; test: re-parented Post, drift() empty for its Tag; mutant: the narrower rule — cost: one test.
Task 2 fix round 1 dispatched to the same implementer.
Task 2 fix round 1: 9f9d3a3 — I-1 _geometryChanged includes the cascade (CS8); I-2 the whole cascade under one try, rollback in reverse then _undoInner(r0) (CS9, on a DIFFERENT fixture: a root-owned foreign fill naming P2's boundary — after m-1 the -7 fixture no longer throws; it lands cleanly in CS10); m-1 fills before other leaves via _subtreeRemoval (CS10: open boundary and scalar -7 both land, undo/redo exact; "reinstate the skip" now the mutant, red); m-2 DR3 (Brace client; [high, low] refused on low; seeds sorted); m-3 _subtreeRemoval recursion (CS11, nested group; instances coded, not exercised); broader paramsOf rule (LV2: re-parented Post, drift empty, parametric.orphan [T, A]). Gates engine +1013 -2, render +931 ~1 -7, app +157, web ✓. Mutants I-1 term, RV-R1, RV-R1b, reinstate-skip, RV-R2, RV-R2b, no-recursion, narrower rule, direct read, M-08f/f0/f2/v, X2-node red. Findings: DR3 compares without the handle seed (a refused create raises it — existing behaviour); CS8's index assertion is not isolated from the capability assertion under the mutant. Scoped re-review dispatched to the Task 2 reviewer (review worktree at 9f9d3a3).
Task 2 fix round 1: re-review Approved. Verified each ruling + test + mutant (I-1 term, RV-R1, RV-R1b, RV-R2, direct read, old-skip, non-fill-first, no-recursion red); TileCache end-to-end probe: geometry capability, +50 invalidations, 0 tiles holding the child (mutant: components, +0, 25). _subtreeRemoval probes: fill nested/boundary in P2 → refused, rolled back exactly; boundary nested → lands; region two deep → lands; InstanceNode → lands, but "no instance removal" survives the suite; misplaced component on a nested group outlives its node (select tool's existing behaviour). Gates engine +1013 -2, render +931 ~1 -7, app +157, web ✓.
Minors → Task 3's brief as a first step (engine, tests + one-line change): m1 an InstanceNode under P1 in CS11 (kills "no instance removal"); m2 _subtreeRemoval collects every fill of the whole doomed subtree before any boundary, so probe (a) lands instead of refusing (test: fill in a nested group, boundary in the doomed object — delete lands, undo exact).
Ruling: both minors taken in Task 3's first step — cost: two small tests, one reorder.
Task 2: complete (commits 5868948, 9f9d3a3).
Task 3: implementer reports db662ee (Task 2 minors: fills of the whole doomed subtree first — CS12; an InstanceNode under P1 in CS11; mutants old-order CS12:705, no-instance CS11:659 red; engine +1014 -2) and a70ad40 (opening.dart: OpeningKind/HingeEnd/SwingSide/OpeningParams/isOpeningWidth; opening_geometry.dart pure: HostFrame, hostFrameOf (local re-check), obstaclesOf, stretchesOf, placeCut, mergeCuts, overlaps; wall_geometry.dart: capsOf, WorldWall gains params/toWorld, strictlyInside public; test/support/opening_fixture.dart pure oracles; OP1, HF1–HF6, OG5). App +165, web ✓; engine/render unchanged by a70ad40. Mutants M-08w OG5, X3-span HF2, X3-tee HF3, X3-xing HF4, X3-sliver HF5, X3-tie HF6 red; M-08s pre-fire survives at the pure level (owners OG2/OG9, Task 4). Deviations: WorldWall carries params/toWorld; obstaclesOf skips walls parallel within wallJoin.angular (07 classify tees a collinear overlapping end; D7 says no obstacle; HF4 asserts it); document-level oracles left to Task 4; mergeCuts/overlaps built but pinned only in Tasks 4–5. Notes: HF1's 1e-9 bound tight (free-end u ≈ 8.7e-10); unused_import is a warning in the app, not an error. Review dispatched (review worktree at a70ad40).
Task 3: review Approved (db662ee, a70ad40). Verified: capsOf == outline's decision; hostFrameOf local re-check vs 07's own decision — P3 80,000 frames, 5,991 fallbacks, 0 disagreements; oracles independent; fixtures non-degenerate; WorldWall no new allocation, built only on a stale cache. Mutants re-fired red (M-08w, X3-*, parallel skip removed, X "strictly inside" dropped). Parallel skip: drops nothing D7 would give (probe P2). Gates engine +1014 -2, render +931 ~1 -7, app +165, web ✓.
Task 3 S1 (spec): D7's clamped-T obstacle is only B's square end (≈20 mm on HF3's 10° fixture) while B's band crosses 1,192 mm of the host's band → a door can be placed under B.
Ruling (S1): D7 amended by the controller (spec, this commit) — a T's interval is the u-range of 07's T cap points TOGETHER WITH the points where B's two faces cross the host's near face; follow-up in Task 3 fix round 1 (obstaclesOf + HF3 + oracle + a mutant "cap points only") — cost: one fix round.
Task 3 minors → fix round 1: m1 a k=0 stem from the host's right side (kills RV-tee-end-only); m2 nested obstacles ("+" of two tees 200 and 115 on opposite sides at the same u; kills RV-nested-obstacle); m3 an obstacle wholly beyond uE (a T inside the end cap's mitre zone; kills RV-straddle-uE); m4 HF1's bound → 1e-8 (sweep: 114 of 3,600 neighbouring fixtures exceed 1e-9, max 1.75e-9); nit OP1's comment. m5 → Task 5's brief verbatim: mergeCuts joining only when `c.$1 <= out.last.$2` (no + linear) must turn OG6 red; overlaps without the `− linear` must make a touching pair report opening.overlap. Observations: M-08s's HF5 kill is incidental — OG2 (Task 4) must carry the real one on a joined host; unused_import is a warning in the app (plan's Global Constraint overstates it).
Ruling (minors): m1–m4 and the nit in Task 3 fix round 1; m5 carried to Task 5; M-08s note carried to Task 4 — cost: tests only.
Task 3 fix round 1: 680f24b — S1 (T interval = cap points ∪ stem faces × near face; oracleTeeFootprint decides 07's clamp independently; HF3: WG4, m1 k=0 stem from the right on a 240 host, WG19 10° ≈[2690.0, 3881.8] and 25°; no stretch holds 2710–3610); m2 "+" nested (HF5); m3 T inside the end cap's zone (HF5); m4 HF1 bound 1e-8; OP1 via a JSON string. Mutants cap-points-only HF3:154, RV-tee-end-only HF3:122, RV-nested HF5:257, RV-straddle HF5:275, X3-span, X3-tee, 3 extra frame mutants under 1e-8 all red. App +165, web ✓; packages untouched. Scoped re-review dispatched to the Task 3 reviewer.
Task 3 fix round 1: re-review Approved. Code = amended D7 word for word; oracle independent; differential P5: 20,000 random tees (5,394 clamped, 2,750 from the right) worst |diff| 9.2e-9. Mutants re-fired red + own near-face variants (always hl, always hr, swapped, from b.d). App +165, web ✓.
Task 3 minors (recorded, not fixed): m1 a short stem is over-blocked (D7 crosses B's faces as infinite lines: a 100 mm stub blocks ≈1,080 mm with no stem there) — errs safe; m2 the parallel skip is now a visible jump (≤1e-9 rad: nothing blocked; 2e-9 rad: the rest of the host) — D7's collinear exemption; m3 the plan's Task 3 HF3 bullet is stale ("the stem's square end") → Task 16 amends it.
Ruling: m1 and m2 recorded as known limits in the results note (Task 16) — cost if wrong: a door cannot be placed near a short stub or a near-parallel wall; m3 to Task 16's amendments — cost: none.
Task 3: complete (commits db662ee, a70ad40, d15c5a1 spec, 680f24b).
Task 4: implementer reports 4950c40 — OpeningType (geometry capability, empty reach, references [host], cascade, empty generate/diagnose per 08-7); _pieces behind piecesOf/centrelinePieces (start piece first, CCW, cap-vertex snap, drop ≤ linear, simplifyRing); view adapter wallsInView/layoutOf/layoutInView; HostFrame.e; WallType._cut; catalog registers OpeningParams; document-level oracles (OpeningOracle, tiling, uncutTwin, …); OG1, OG2, OG8, OG9, OR3, OR6. App +171, web ✓; packages untouched. OG9: 1,517 walls, 2,256 openings, 0 refused, 2,340 pieces, 0 tiling violations, all triangulate. Mutants M-08b (OG1/OG2/OG9/OR3; survives the centred Q3d1 control), M-08c, M-08e (survives the one-face Q3d2 control), M-08s (OG2 193:9 on a joined host — carry-over 1), M-08snap (OG9: 72 refused), M-08k, re-fired M-08d/r (OR3), X4-drop, X4-order, X4-adapter all red. Deviations: HostFrame.e; oracle span read structurally; oracle T = amended D7; uncutTwin deletes all openings in one compound; OG2's M-08s guard per justification pair (right/right has uE == L); OG9 counts from the oracle until Task 5.
Task 4 finding: a cut spanning a whole free wall (w ≈ L) drops both pieces → a childless, unpickable wall group; the spec does not cover it.
Ruling (finding): a cut that would leave its wall with no piece makes that opening no-fit (the wall stays whole, the symbol draws outside, opening.nofit) — spec D8 amended by the controller; implemented and tested in Task 5 (test + mutant "allow a zero-piece wall") — cost: one test.
Task 4 review dispatched (review worktree at 4950c40).
Task 4: review Approved. Verified D9 end to end; oracle independent except 07's classify/cap (intended differential); OR6 + probe: undo/redo of a second opening restores kids/handles exactly (canon differs only in handleSeed); 07's uncut path byte-identical to 680f24b over 200 random plans (1,577,032 bytes, cmp IDENTICAL); no per-frame work, nothing O(n²). Mutants re-fired red + own RV-2, RV-3, RV-6 (pins mergeCuts's use), RV-8. App +171, web ✓.
Task 4 minors: m1 zero pieces also from merged cuts that each fit alone (two 1,000 windows on a 2,000 wall) and from cuts leaving only pieces ≤ linear; m2 "anticlockwise" unpinned (RV-4 middle piece clockwise survives); m3 HostFrame.e unpinned (RV-1 end centreline recomputed survives; differs from stored end in 4,193 of 10,000 random walls).
Ruling (m1): spec D8 amended (this commit) — decided on the merged result: if no piece > wallJoin.linear remains, the fitting opening with the highest handle becomes no-fit and the cuts are recomputed; repeat. Implemented in Task 5 with two tests and the mutant "allow a zero-piece wall" — cost: two tests.
Ruling (m2, m3): Task 5 adds isSimpleCcw on every stored piece (kills RV-4) and exact stored start/end on the first/last centreline piece (kills RV-1) — cost: assertions only.
Task 4: complete (commit 4950c40).
Task 5: implementer reports b74787b — cutsOf (placement, merge, amended D8 keep-a-piece loop: highest handle yields); HostCuts/hostCutsInView shared by the wall's cut and each opening's diagnose; OpeningType.diagnose (D17: nofit, clamped [names corner/obstacle walls], overlap once per pair by lower handle, orphan, degenerate); oracle cutsOn applies keep-a-piece independently; storedPiecesSimpleCcw; OG1/OG2/OG9 CCW; OG8/OG9 exact centreline ends; OG9 compares diagnostics() with the oracle per trial (0 mismatches over 300); new opening_obstacle_test.dart OG3, OG4, OG6, OG11 (a)(b)(c), OD1. App +178, web ✓; packages untouched. Mutants M-08o, x, m, q, q2 (both forms), X5-touch, X5-cause, keep-a-piece, X5-lowest, RV-4, RV-1 (OG9; survives OG8), m5a (pure level only — equivalent at document level, control shown), m5b all red. Plan wrong: M-08m survives the plan's OG6 pair (negative middle extent dropped) → nested door-in-window case; m5b needs 5e-7 into/apart cases; M-08q2's reporter shown via a 6,000 mm no-fit window at the middle handle. Decisions not in the spec: a degenerate host → its openings opening.nofit; orphan vs dangling via view.referrers(host) containing the opening; clamped message wording; OG11 new ID (Task 16 lists it); OD1 compares children (seed). Review dispatched (review worktree at b74787b).
Task 5: review Needs fixes. hostCutsInView genuinely shared; all owned/carried mutants re-fired red; own RV5-clampedCut, diagOwnCut, orphanAlways, overlapStored red. I-1 the loop's "repeat" untested — RV5-yieldOnce survives the full suite (brings back a childless wall; OG9 has 0 keep-a-piece cases). m-1 degenerate host → opening.nofit untested (RV5-degHostSilent survives) and its message ("drawn outside") is wrong — no frame. m-2 clamped names "a corner" when a wall moved it by < tolerance (walls named via tolerant overlaps; clamped exact). m-3 HostCuts.params unread (nit). S1 the highest-handle rule can make an opening no-fit uselessly and depends on handle order; proposed: admit in ascending handle order, an opening is no-fit only if admitting it empties the wall. S2 degenerate host: no frame → draw nothing. S3 clamped walls by exact overlap. Cost: diagnostics() not on the edit path (tests only); 50 openings on a wall: 7.98 ms diagnostics, 3.22 ms per edit; carry to Task 6: when each opening's generate needs its cut, n × hostCutsInView lands on the edit path (~8 ms per wall edit at n = 50). Gates engine +1014 -2, render +931 ~1 -7, app +178, web ✓.
Ruling (S1): taken — spec D8 revised (this commit): admit fitting openings in ascending handle order; one is no-fit only if admitting it would leave no piece; tests OG11 (a)–(d) and OG9's generator produces keep-a-piece cases; mutants "allow a zero-piece wall" and "check only once" (RV5-yieldOnce's analogue) — cost: fix round.
Ruling (S2, m-1): spec D8: a degenerate host's openings cut nothing, report opening.nofit, draw nothing; message fixed; OD1 tests it (RV5-degHostSilent red) — cost: one test.
Ruling (S3, m-2): spec D17: walls named by exact overlap; "corner" only when the unclamped interval leaves the span — cost: one test (the 5e-7 probe).
Ruling (m-3): left for Task 6 (it may read params) — cost: none.
Ruling (cost carry): Task 6 measures the per-edit cost with 50 openings on a wall and memoises hostCutsInView per host within one generation pass if it is > a few ms — cost: none now.
Task 5 fix round 1 dispatched to the same implementer.
Task 5 fix round 1: 2b64812 (amended once before report, test-only) — cutsOf admits in ascending handle order; degenerate host → opening.nofit, "degenerate", draws nothing; clamped names walls by exact overlap, "corner" only outside the span (OG9: 0 of 1,145 clamps explained by neither); oracle cutsOn/obstacleWalls/clampCause independent; OG11 (d) (probes + a two-yield case); OG3 5e-7 probe; OD1 degenerate host; OG9 full clamped comparison, 0 childless walls, keep-a-piece cases from a separate Random(809) stream (321 made no-fit to keep a piece). App +179, web ✓. Mutants zero-piece, check-once (2 forms), highest-handle (old rule), RV5-degHostSilent, tolerant overlap, M-08m red. Note: the shared scratchpad script mut.sh was overwritten by another agent mid-round — standing briefs amended: unique scratch-file prefixes per agent. Scoped re-review dispatched to the Task 5 reviewer.
Task 5 fix round 1: re-review Approved. Admission per amended D8 (single pass, deterministic); degenerate host per D8; clamped per D17; oracle independent. Re-fired RV5-yieldOnce (OG11 d, OG9) and RV5-degHostSilent (OD1) red; own rv5b-descending, rv5b-checkAlone red; rv5b-cornerTol (corner judged with tolerance) survives → m-1. App +179, web ✓.
Task 5 m-1: the exact corner comparison is unpinned — probe: free 3,000 wall, 150 left, far origin 23°, a 900 window stored 5e-7 past uS → real: "a corner of wall … moved it"; mutant: "its stretch moved it". Observation: OG11 (d) filters opening.clamped for window A at [0, 1000] without saying why.
Ruling: m-1 into Task 6's brief (one assertion on the probe); the OG11 (d) filter gets a one-line comment there too — cost: a few lines.
Task 5: complete (commits b74787b, 0b300b1 spec, 2b64812; spec 5b74810 before it).
Task 6: implementer reports f1073b7 — symbolOf (door LINE+ARC, hinge on the swing face, sweep anticlockwise per D10; window 3 lines, no-fit translated by t in D10's child order; gap one inset line m = min(t/4, w/4), no-fit at lOff+t/2); OpeningType.generate via toWorld(self)⁻¹·toWorld(host), nothing for degenerate opening / non-wall host / degenerate host; hostCutsInView memoised per view+host via an Expando (app-side; engine untouched); HostCuts.params removed; OG7, OG10, OR7, OR8 (real shell tap), RD1 (pixels, Blueprint), OG1 extended with the door (M-08h), OG2 + Task 5 m-1 probe, OG6 symbols over the merged gap + outside for the 6,000 window, OG11 (d) comment (premise checked: A is not clamped here; the filter guards against rounding). App +184, engine +1014 -2, render +931 ~1 -7, web ✓. Mutants M-08u (window, gap), g, h (OR7, OG1), j, j2, X6-hinge/face/sweep/cover (RD1 pixel)/nofitdoor, rv5b-cornerTol all red; plan click claims checked separately (OR8 scratch). Cost, 50 openings on one wall: thickness edit 2.97 ms before symbols → 21–27 ms symbols without memo → 4.1–6.0 ms with memo; move 2.1–2.6 ms; diagnostics 1.2–1.7 ms. Deviations: arc sweep per D10 (plan wording holds for half the cases); no-fit window child order kept; RD1 3×3 brightest sample.
Task 6 review dispatched (review worktree at f1073b7).
Task 6: review Approved. Memo sound (one view per pass, plan applies after; Expando weak; staleness probe P1 green, rv6-globalMemo red in 17 tests). Arc correct for 4 hinge×swing × 3 justifications in rotated groups (P5). RD1 robust (Blueprint, DPR-independent, 3×3 brightest; leaf 1.00, paper 0.22). Cost at 50 openings ~5 ms (≈ pre-symbol cost); memo off grows n³; cutsOf admission ~n² log n per host, accepted. Mutants re-fired red + own rv6-globalMemo, rv6-clampedStored, rv6-winOrder. Gates engine +1014 -2, render +931 ~1 -7, app +184, web ✓.
Task 6 minors: m-1 (spec) no-fit symbols can lie inside their host's own lobe at clamped acute corners / 3+ nodes (random P2: 28 of 481 no-fit; 0 fitting) — D12's argument assumed the band stays in its strip; m-2 OG9 checks neither the symbols against the oracle nor D12 across random geometry (P2: 0 off the oracle; 8 oracle errors `no face edges` in capUs on geometry OG9 never builds); m-3 mirrored own group: the arc is anticlockwise in own space, clockwise in world — D10's wording.
Ruling (m-1): D12 amended (this commit) — a recorded limit; code unchanged — cost if wrong: a no-fit symbol next to a node may hide under its host's lobe. Ruling (m-3): D10 wording amended (own space) — cost: none. Ruling (m-2): Task 7's brief — OG9 checks every drawn symbol against the oracle and that no FITTING symbol lies strictly inside its host's pieces — cost: assertions only.
Task 6: complete (commit f1073b7).
Pushed plan-08/openings at c71d3b1 (human: push when Task 6 is done).
Human (2026-09-25): push plan-08/openings again when Task 11 is done.
Task 7: implementer reports 762b268 (tests only; no lib, no packages) — opening_object_test.dart OR1, OR2, OR4, OR5, DF1; opening_cost_test.dart RC3; OG9 extension (Task 6 m-2): 973 doors, 1,285 windows, 977 gaps, 0 off the oracle; 2,348 fitting, 0 strictly inside their host's pieces; 887 no-fit, 9 inside (exempt, D12's limit); capUs never threw. Fixture helpers moved from opening_symbol_test.dart. App +190, web ✓. RC3: n=600 line draw ≈5.3 ms, move ≈6.8 ms (noisy). Mutants M-08r (OR1 setup), r2 (OR1, OR4), t (OR4), h (OR1), f (OR5 undo depth 6), f0, f2, X7-json (OR2), X7-dirty (DF1, OR4), rv6-clampedStored (OG9 D12 assertion), winMid (OG9 oracle) all red — re-fired after a runner slip (double backup of parametric_system.dart for M-08f; caught by git diff, restored from the original, invalid runs discarded). Note for Task 16: at 600 walls a root line draw costs ≈5 ms (the two surveys' O(n)). Review dispatched (review worktree at 762b268).
Task 7: review Approved. No residue (only test files in the commit; lib files byte-identical). OR1 not one hop (opening reach empty; M-08r2 red at the move); DF1 sound combined with expectOnOracles (fixture oracles never call opening_geometry.dart). Mutants re-fired red. Gates engine +1014 -2, render +931 ~1 -7, app +190, web ✓. Minor 1: OR2's values are all integers — rv7-posRound survives OR2 (killed only by OP1). Minor 2 (observation): DF1's bitwise comparison adds little beyond drift(); history-order mutants equivalent (ComponentStore.handles ascending). Equivalent: rv7-invOrder.
Ruling (Minor 1): Task 8's brief — OR2 gets a fractional position and width; rv7-posRound must go red on OR2 — cost: one edit. Minor 2 and rv7-invOrder recorded.
Task 7: complete (commit 762b268).
Task 8: implementer reports 127185a (OR2 fractional: gap 2600.37 / 512.625, slide 1100.25; rv7-posRound red at OR2 178, survived before) and 71de39d (WallGrips.drag appends _keptPut: for each moved wall with start changed and end unchanged (exact ==), each live opening (root-level GroupNode) gets p′ = L′ − (L − p), ascending; EP1–EP5 in opening_end_drag_test.dart). App +195, web ✓; packages untouched. Mutants M-08p (EP1), M-08p2 (EP1, EP2, EP3), X8-refuse (EP5), X8-first (EP1 on wall C), rv7-posRound (OR2) red. Deviations: EP5 drags back to the drawn world point (a grip round trip at the far origin loses ulps); EP4 through GripDrag.move/rotate; EP1 exact ==; the liveness check is unpinned. Review dispatched (review worktree at 71de39d).
Task 8: review Approved. Verified _keptPut vs D13; mutants re-fired red; own rv8-worldLen, rv8-desc red; rv8-noLive survives (m1), rv8-before survives (m3, cosmetic), rv8-addForm and rv8-startOnly equivalent (both-ends-moved is unreachable: the drag is refused at length 0). End-moved walls keep world positions (P1: 0.0 drift). Drift: 2,000 random end drags → max 1.9e-7 mm (a random walk, no creep). Start-drag shortening (P2): clamped + overlap diagnostics, keep-a-piece holds, exact restore. App +195, web ✓.
Task 8 minors: m1 the liveness check is unpinned (reachable only by a hand-built command: OpeningParams on a nested group or a node-less handle); m2 EP5 covers only the end-drag branch; m3 EP1's reason claims "after the walls" but asserts only handle order. Note for Task 16: D13's "both ends moved" wording is harmlessly imprecise.
Ruling: m1–m3 into Task 9's brief (EP6 like the reviewer's P4; P2's start-drag case added to EP5; EP1 reason text fixed) — cost: tests only.
Task 8: complete (commits 127185a, 71de39d).
Task 9: implementer reports dc54f3f (Task 8 minors: EP6 liveness, EP5 start-drag shortening, EP1 reason) and 267e66e (WallBands shared cache + hostAt/joinInto moved out of wall_tool.dart; document adapter wallsInDocument/hostFrameInDocument/layoutInDocument/openingsInDocument; OpeningTool per plan steps 1–9 with symbol + jamb preview; shell: tools D/N/G (900/1200/900), kShellLetterKeys; OT1 (shell, 90 clicks by a mitre and a T), OT2 (centre + right-justified), OT4 (+cost), HF7). App +203, render +931 ~1 -7, harness +82, web ✓. Mutants rv8-noLive (EP6), M-08z, z2, z3 (right-justified case only), M-08b (tool writer), X9-raw, X9-unclamped, X9-naive, X9-cache, X9-cacheDoc, X9-clickRebuild, M-08h at both preview sites, X9-adapter red; X9-adapterDesc equivalent. Findings: (1) spec gap D14/D16 — the stored centre needs an ulp correction (storedCentreOf: (x + w/2) − w/2 ≠ x for ~10% of clamped placements; else clamped from birth); Task 11's slide grip needs the same; (2) the new opening's handle predicted as handleSeed.current + 1 for keep-a-piece; (3) hostAt scans non-live wall components (hand-built files only); (4) OG9's generator moved to the fixture (randomOpeningPlan), print unchanged; (5) the oracle's exact clamped flag differs by ~1e-9 from the app — OT1 checks diagnostics(); (6) Task 14: 07's join sites moved to wall_bands.dart. OT4 n=600: 3.68 µs per hover over no wall, 10.56 µs over a wall with preview. Review dispatched (review worktree at 267e66e).
Task 9: review Approved. All requested mutants red; 07 regression checked — 07's "No endpoint rule", "Wrong side", "Highest handle first" re-fired at their new wall_bands.dart sites, red with 07's exact values (join arithmetic bit-identical). storedCentreOf: bounded (≤ 8 ulp steps), 200k fuzz: drawn cut within 1.8e-12 of the clamped cut; give-up only at the fit/no-fit knife edge (places clamped by 1 ulp, reports clamped) — acceptable, record in D14. Handle prediction exact by construction (build's first act is handleSeed.next()). Shortcuts D/N/G conflict-free. Hover cost acceptable; per-move allocation only; invariants green. Gates engine +1014 -2, render +931 ~1 -7, harness +82, app +203, web ✓.
Task 9 minors: m1 hinge from the stored centre unpinned (rv9-hingeRaw; a clamp moving the centre across L/2); m2 swing from the RAW point unpinned (rv9-swingResolved; a midpoint snap at scale 0.15 puts the resolved point on the centreline → one-sided swing); m3 handle prediction unpinned (rv9-selfFirst); m4 D/N/G guard entries unpinned (rv9-guardDNG); m5 WallBands caches non-live wall components (hand-built files: D does nothing over a live wall).
Ruling: all five into Task 10's first commit ("Task 9 minors"): OT2 gains the clamp-across-L/2 hinge case and the midpoint-snap swing case; the tool allocates the new handle before placing and passes it to build (removes the prediction) plus a keep-a-piece tool test; an A8-style guard test with D, N, G literal; WallBands filters liveness (root-level GroupNode) with a probe test. D14 amendment (storedCentreOf, its give-up) → Task 16 — cost: tests + two small code changes.
Task 9: complete (commits dc54f3f, 267e66e).
Task 10: implementer reports 8408561 (Task 9 minors: OT2 hinge-across-L/2 and swing-from-raw at a midpoint snap; the handle allocated inside build, no prediction, keep-a-piece tool test; D/N/G guard test; WallBands live walls only) and 71c7047 (edgeSnap over stretch ends and other openings' drawn cuts, nearest wins, ties to the lower centre; OpeningTool.selfSnap gated on F3; markerPoint on the host's centreline; render PlacementTool gains @protected markerPoint (B12); host-scan memo). App +212, render +932 ~1 -7, harness +82, web ✓. Mutants hingeRaw, swingResolved, selfFirst, guardDNG, noLive, noParent, M-08sn (two sites), X10-gate, X10-marker, renderMarker, nearest, tieHigh, strict, noCuts, noStretch, reproject, apertureLocal, scanMemo, X9-clickRebuild (new site) red. Deviations: edgeSnap has no frame param; aperture in host-local units (scaled groups from files); snapped centre stored exactly (OT3 1e-11); invalidate moved to onPointerDown before super; host-scan memo; marker at the projected/snapped u. For Task 16: D15 amendments (markerPoint, local aperture, exact storage); M-08sn becomes multi-site once Task 11 calls edgeSnap. Review dispatched (review worktree at 71c7047).
Task 10: review Approved. markerPoint default bit-identical (no other overrides); render +932 = B12 alone; paint_allocation green. m3 hover vs commit differential P2: 1,928 presses, 0 mismatches (sensitive: 39 under selfFirst-hover). Refused press leaves no history; permission denial allocates nothing. Memo no new staleness (rv10-clickRebuild, rv10-scanNoGen red). Edge snap outranks nearer snaps per D15; F3 gate right. Own rv10-stickyEdge, rv10-onHostStale red; rv10-edgeAnyHost equivalent. Gates engine +1014 -2, render +932 ~1 -7, harness +82, app +212, web ✓.
Task 10 minors: m1 OT3 cannot tell drawn from stored edges (rv10-storedCuts survives; probe P1: a clamped window by a T); m2 the local aperture divides by √|det| — off for a non-uniformly scaled host (files only); exact divisor |toWorld applied to f.d|.
Ruling: m1 and m2 into Task 11's first commit (OT3 gains a clamped other opening and a no-fit one; the aperture divides by the length of toWorld(f.d)) — cost: a test + a one-line change.
Task 10: complete (commits 8408561, 71c7047).
Task 11: implementer reports b975db8 (Task 10 minors: OT3 rv10-storedCuts on the reviewer's P1 with clicks at o2+1312/1205 and a no-fit neighbour — the carry's numbers were garbled; the aperture divides by |toWorld(f.d)|, t11-apertureDir on a scale(2, 0.5) group) and 2670033 (render: movableKey in grip_cache.dart exported, GripDrag._capture/rotatable/hitsRotationGrip/cursor/4 select-tool sites honour movable, GripDrag.move/rotate gain {objects}; app: OpeningGrips (slide grip: storedCentreOf, edgeSnap on the resolved point, no-change within wallJoin.linear → null, no preview for no-fit), ObjectGrips composite, shell wiring; MV1–MV4, SG1 (+shell), SG2). Render +936 ~1 -7, harness +82, app +217, web ✓. Mutants rv10-storedCuts, noFitStored, apertureDir, M-08i (3 sites), X11-rotgrip, X11-cursor, X11-site1–4, X11-stored, M-08sn (3 sites; tool site control green for the grip), M-08b (grip writer), X11-gate, clampNoFit, unclamped, exactNull red. Deviations: OpeningGrips.movable false for openings (plan said true — read as a slip); grip candidates as currently drawn (the tool admits the would-be opening last); off-centreline projection under non-uniform scale (files only). For Task 16: D16 amendments (tolerance no-change rule; candidates as drawn); D14/D15 note; M-08i and M-08sn multi-site for Task 14. Review dispatched (review worktree at 2670033).
Task 11: review Approved. Render regression: 03/07 grip tests explicitly green (render +97, app +25); mixed selection (wall + door + line): move and rotate move the wall and the line in one undo step, the door is skipped (not refused); a door alone: no rotation disc. movableKey O(1), hover calls it only for a selected hovered key; paint_allocation green. Slide grip per D15/D16/08-15. Mutants re-fired red (M-08i 3 sites, X11-*, M-08sn grip site, M-08b grip writer, storedCuts, apertureDir, exactNull, X11-stored) + own rv11-previewSnap red; rv11-selfCand and rv11-fillMovable survive (m1, m2); rv11-nestedAsk equivalent. Gates engine +1014 -2, render +936 ~1 -7, app +217, harness +82, web ✓.
Task 11 I1 (root cause Task 8's D13 rewrite): p′ = L′ − (L − p) leaves a door flush on the far stretch end clamped by an ulp — 99 of 200 start drags leave opening.clamped; the slide grip's no-change rule (|new − stored| ≤ wallJoin.linear → null) then cannot re-seat it. Not user-visible today (no UI shows diagnostics()).
Ruling (I1): fixed in Task 12's first commit — _keptPut stores storedCentreOf when the rewritten position is clamped only by rounding; the grip returns null only when centre == position, or within tolerance AND the current cut is unclamped; tests: the reviewer's 200-wall D13 probe (0 clamped after) and a grip re-seat of an ulp-clamped door; mutants: each half reverted → red; D16's "changes nothing" and D13 amendments → Task 16 — cost: one fix commit.
Ruling (m1): SG1 gains a nudge (a door at 1000 dragged to 1030 with aperture 67 must move); rv11-selfCand → red. Ruling (m2): a one-line test (a fill + an opening selected draws no rotation grip); rv11-fillMovable → red. Ruling (m3): movableKey stays public; recorded in Task 16's amendments — cost: none.
Task 11: complete (commits b975db8, 2670033).
Pushed plan-08/openings at 2670033 (human: push when Task 11 is done).
Human (2026-09-25): push plan-08/openings again when Task 16 is done.
Task 12: implementer reports d0640e4 (I1: _keptPut → _seated (storedCentreOf when p′ is clamped only by ≤ wallJoin.linear; adapter moved: map); grip null only on == or within tolerance and unclamped; EP7 (200 walls: 0 clamped after, 97 under the old rule), SG3 ×2, SG1 nudge, SG4 fill + door → no rotation grip) and c9ba0e7 (the Opening section: PanelFieldFocusNode fields, pinned targets, tool-settings mode with negative sentinel handles per kind, refused-edit catches incl. DanglingReferenceError, flips as OutlinedButtons, read-only without components+geometry; OS1 ×2, OS2, OS3, OS4). App +221 then +226, web ✓; packages untouched. Mutants t12-keptOld (EP7), t12-keptAll (EP5), t12-gripOld (SG3 ×2), rv11-selfCand (SG1), rv11-fillMovable (SG4), M-08pin (OS2), X12-gapmin, X12-enter, X12-catch (2 sites) red. Deviations: no-fit counts as unclamped for the grip rule; EP7 within wallJoin.linear; SG3's old-D13 door written by hand (part 1 makes it unreachable); tool-settings sentinels Handle(-1 - kind.index); no position field in tool mode. For Task 16: D13 (re-seat, moved:) and D16 (no-change rule) amendments. Review dispatched (review worktree at c9ba0e7).
Task 12: review Approved. _seated never moves a door visibly (≤ 1e-6 + ulps, to where it is drawn); a corner-flush door stays put (P5: 0 of 40 re-seated); grip rule as carried; the panel meets every 07/fix-post-07 convention; sentinels Handle(-1/-2/-3) cannot collide (Handle.none = 0; guarded paths). Shell probes P1–P4 (selection change with typed text, flip with unsaved width, 07 Task 8 I1 class: typed width then palette/canvas click) all correct. Mutants re-fired red incl. M-08pin also red on 07's WS7 (Wall and Box). Own rv12-seatWide, rv12-movedHostOnly, rv12-posEnd, rv12-gripNoFit survive (m1–m4); rv12-chgCurrent equivalent through the UI. Gates engine +1014 -2, render +936 ~1 -7, app +226, web ✓.
Task 12 minors (test gaps): m1 moved: neighbour half unpinned; m2 _seated's ≤ wallJoin.linear bound unpinned (P5 as EP8); m3 OS1 position upper bound L inclusive untested; m4 "no-fit counts as unclamped" for the grip's null rule untested.
Ruling: all four into Task 13's first commit — cost: tests only.
Task 12: complete (commits d0640e4, c9ba0e7).
Task 13: implementer reports f197521 (Task 12 minors: EP9 moved: neighbour half on 24 L corners — 11 of 24 clamped under the old rule; EP8 P5 on 12 walls; OS1 position 0 and L inclusive; SG5 no-fit grip null within tolerance) and 645a969 (the sample plan built from walls and openings). Step 1 probe (t13-probe.log beside this ledger): EVERY spec figure matches — 549 live entities, 72 wall children / 24 pieces (E1 2, E2 3, E3 5, E4 3, P1 3, P2 2, P3 3, P4 2, P5 1), 14 door / 24 window children, SP4 tightest margin 40.0, extents (12000, 8000, 26000, 17000) exact, drift/diagnostics empty, obstacle intervals as the spec tables, all 15 cuts unclamped. SP1 pins 549, SP2 draw order, SP5 replaced (tables exact), SP6 save/load/save byte-identical; shell tests rewritten (E1 delete + door in one step; E1+E4 with 3 openings). App +229 then +230, web ✓. Mutants rv12-movedHostOnly (EP9), rv12-seatWide (EP8), rv12-posEnd (OS1), rv12-gripNoFit (SG5), X13-hinge (SP5), X13-dispose (12 shell tests), X13-order (SP2) red. Plan wrong: its click point (x0+100, y0) selects E4 not E1 (vertex outranks edge; tie → higher root) — uses (x0+3000, y0), and (x0, y0+4500) for E4. Deviations: SP3/SP4 read furniture as root-owned regions only (wall pieces are regions too); SP2 adds walls-below-finishes. Stale comment in a status-text test (Ruling 08-1 forbade editing) → Task 16.
Review dispatched (review worktree at 645a969).
Task 13: review Approved. Independent probe: every D18 figure matches (549; 24 pieces per-wall; obstacle intervals; 15 cuts unclamped and exact; extents exact; SP4 tightest 40 mm measured continuously; no swing reaches another wall — min 490 mm; drift/diagnostics empty fresh and under the shell's system; expander null, history empty). Every door's hinge = the old hinge moved t/2 onto the swing side; windows cover the old ranges. SP3/SP4 re-scope sound. Click points: E1's robust across surfaces; E4's tied to the 800×600 surface (pick radius < 506 mm). Rendered PNGs (rv13-*.png, scratchpad) look right on Blueprint and White. Own mutants rv13-swing, wrongWall, just, bed2, noClear, disposeEarly red; rv13-wallsMid survives (m1). Gates engine +1014 -2, render +936 ~1 -7, app +230, web ✓.
Task 13 minors: m1 SP2's "walls below the finishes" compares to the highest finish handle (rv13-wallsMid survives) → assert maxWallChild < minFinish; m2 stale comments in startup_plan.dart (lines ~124–138: the swing/approach clearances moved with the hinge onto the face) and startup_plan_test.dart:268; m3 the E4 click point's comment should note the ≤ 506 mm pick-radius limit.
Observation (for the final review): the reviewer's capture with a HAND-BUILT camera matrix dropped every LINE entity (fills and arcs drew); with ViewportTransform.fit everything draws. Not caused by Task 13; uninvestigated.
Ruling: m1–m3 into Task 14's first commit; the capture observation goes to the final whole-branch review's brief — cost: small.
Task 13: complete (commits f197521, 645a969).
Task 14: implementer reports 3d1713f (Task 13 minors: SP2 vs the lowest finish — rv13-wallsMid red; comments with measured clearances; E4's ≈506 mm bound) and b1b4c98 (docs/superpowers/notes/plan-08-mutation-log.md). Tally: 213 fired, 204 killed, 1 survived, 8 equivalent, 8 N/A; the M-08b/M-08e controls survive by design. The 37 spec mutants: 50 fires (M-08b, h, i ×4 sites; sn ×3; u ×2; q2 ×2 forms; pin vs OS2 + 07's WS7 Wall/Box), 49 killed; M-08a structural on a scratch copy → OR1 red. Extras: 154 fired, 153 killed. Baselines: 115 commands exit 0; 219 restores diff 0. Gates engine +1014 -2, render +936 ~1 -7, harness +82, app +230, web ✓.
Task 14 findings: F1 t11-apertureDir at the slide grip's copy (opening_grips.dart _Slide.place) survives the whole app suite (the tool's copy is killed); F2 OpeningGrips.movable duplicates ObjectGrips.movable and is never reached (M-08i site 4 "equivalent"). Two ledger equivalents are killed on this tree: rv7-invOrder (engine CS1/CS10/CS11/CS12) and rv8-addForm (EP8/EP9). N/A: 6 reviewer mutants never written down (RV-2/3/6/8, RV5-clampedCut, rv9-toolNaive) + 2 superseded (X2-fillskip, X5-lowest). Task 2's non-fill-first reconstruction: killed by CS12.
Ruling (F1): a test — SG1's provider on a scale(2, 0.5) host, the edge snap engaging exactly at the world aperture; t11-apertureDir at the grip site → red. Ruling (F2): ObjectGrips.movable delegates to OpeningGrips.movable for openings (one rule); M-08i site 4 then killable — re-fire. Log updated (F1/F2 killed; rv7-invOrder, rv8-addForm reclassified killed). Fix round 1 to the same implementer — cost: a test + a delegation.
Task 14 fix round 1: de66cd2 — F1 test SG1 (t11-apertureDir, the grip's site) on a scale(2, 0.5) host: red under the mutant (678.17 vs 688.97); F2 ObjectGrips.movable delegates to OpeningGrips.movable (one rule); M-08i re-fired at both sites red (SG2 415). Log: tally 213 fired, 206 killed, 0 survived, 7 equivalent, 8 N/A; rv7-invOrder and rv8-addForm reclassified killed; killed lines read from the saved logs. App +231, web ✓; packages untouched.
Ruling: Task 14's review and Task 15 (invariants and greps; read-only unless a grep finds a defect) go to one independent reviewer, as 07 did — cost: none.
Task 14 review + Task 15 dispatched (review worktree at de66cd2).
Task 14: review Approved. 31 log entries re-fired, every value/line reproduced (incl. M-08a structural on a scratch copy; all 4 sites of b/h/i, 3 of sn, 2 of u, pin vs OS2 + WS7 Wall/Box); tally 213 = 206 killed + 7 equivalent, 0 survived, 8 N/A; all 37 spec names logged; equivalents X9-adapterDesc and rv9-adapterSelf survive the whole suite. Own own-wallMovable, own-gripAtLocal, own-markerLocal red; own-defaultMovable survives (Minor 1). Minor 2: 5 log entries cite opening_grips_test lines from 3d1713f (shifted by 45 in the fix round).
Task 15 (reviewer, read-only): every plan command ran unbroken; positive controls confirm each grep can match. Engine pure Dart (import-anchored grep empty; 3 unanchored hits are doc comments); invariants unchanged and green (query +5, paint +3); no Tolerance.standard / handleSeed.next / TrueColor in the opening files; engine lib diff = parametric_system.dart + regeneration.dart only; render lib diff = placement_tool, grip_cache, grip_drag, select_tool only (barrel unchanged); 07's wall tests unedited; 35 of 35 commits since 357bea6 carry both trailers; analysis_options.yaml never committed. Gates engine +1014 -2, render +936 ~1 -7, harness +82, app +231, web ✓.
Capture observation (Task 13): NOT a render defect — flutter_test's non-antialiased drawVertices skips a 1-px stroke quad centred on an integer device coordinate (VerticesDrawSink's kMinStrokeDevicePixels floor); fit's fractional translation avoids it; device MSAA covers it. Capture tests should avoid whole-pixel translations.
Ruling: Minor 1 (a box group is movable through ObjectGrips; own-defaultMovable → red) and Minor 2 (a one-line note in the log) into Task 16's first commit; the capture finding recorded in the results note — cost: small.
Task 14: complete (commits 3d1713f, b1b4c98, de66cd2). Task 15: complete (no commit).
Task 16: implementer reports 6b3b944 (Task 14 minors: SG6 a box movable through the composite, own-defaultMovable red at 627; the log's shifted lines — nine entries, not five; the status-text comment) and 6ea2d70 (results note 2026-09-25-plan-08-results.md; spec "Amended at execution (Plan 08)" D2, D4, D5, D7, D9, D13, D14, D15, D16, architecture, tests, mutants, open questions; plan amendments per task; STATUS, roadmap/08, roadmap/00 — executed, NOT merged). Tally now 214 fired, 207 killed, 0 survived, 7 equivalent, 8 N/A. Gates on 6b3b944: engine +1014 -2, render +936 ~1 -7, harness +82, app +232, web ✓. Prints: RC2 (600 objects) line 2.56 / move 2.48 ms vs NC4 at 600 2.65 / 2.33; RC3 n=600 5.03 / 4.85; OT4 3.62 / 1.98 / 10.22 µs. Review dispatched (review worktree at 6ea2d70).
Task 16: review Approved. 15+ figures traced (exact ones identical, timings same order); exit gate 15 PASS + criteria 1 (macOS half) and 17 (the look, 11 items × 3 platforms) OWED, never simulated; amendments consistent with the code (6 claims checked); limits and debt complete; STATUS/roadmap correct; SG6 non-degenerate, own-defaultMovable and rv16-alwaysMovable red. Gates engine +1014 -2, render +936 ~1 -7, harness +82, app +232, web ✓. Minors: the log's shifted-lines note wrongly said SG6 moves none (its import shifts every line by one at 6b3b944); the results note said OR1's move was 60 m (it is 75 m). Observation: STATUS headings for Plan 07 and Plan 06 still say "not merged" (pre-existing; renaming changes anchors — left).
Ruling: both minors fixed by the controller (docs only, this commit); the headings left — cost: none.
Task 16: complete (commits 6b3b944, 6ea2d70, + controller docs fix).
Pushed plan-08/openings at 5739442 (human: push when Task 16 is done).
Final whole-branch review dispatched (opus; review worktree at 5739442).
Final review interrupted (API session limit, 429; container restarted). Reviewer left only an untracked probe (apps/floor_planner/test/fr_fuzz_test.dart) in the review worktree — saved to scratchpad plan08/final/fr_fuzz_test.dart.interrupted and removed; no lib file mutated (git status clean). Final review re-dispatched after the limit reset.
Final review (opus, retry): With fixes. Fuzz (7 seeds × 150 trials × 40 steps + a 16-op variant): 41,892 steps, 31,997 undo/redo pairs, 2,273 deep sequences, 2,306 save/loads, 505,697 symbol and 255,347 tiling checks, 4,692 tool placements (0 clamped at birth), 1,999 slides, 1,913 cascade deletes — 0 drift, 0 exceptions; oracle disagreements at acute/folded joints all app-correct per D7 (93 excluded, counted). Log re-fires killed (M-08d, f0, t, s, e, z3, p2, i-capture, pin, b-grip); own fr-X1, X2, X3, X5, X6, X10 killed; fr-X4, X7, X8, X9 survive. Gates engine +1014 -2, render +936 ~1 -7, harness +82, app +232, web ✓; non-negotiables and seams hold.
Final I1 (Important): an invalid (non-triangulable) wall piece refuses the edit when an opening is clamped against uE/uS at a slightly kinked joint — the end piece [R(uE), lobe vertex, endpoint on the centreline, L(uE)] back-tracks after the cap-vertex snap; keep-a-piece checks extent only. Repro: A (3000, 200 centre) + B (1800 at 25°? a 2° kink, 150 right) + a 600 gap at 2900 → refused. Sweep: 79 of 1,620 refused (bends 0.25°–12°, both signs; 0° never). Reachable: the Window tool places nothing; the slide grip's ArgumentError escapes pointer-up (select_tool.dart rethrows — 07's final-review I1 class); a neighbour's end drag is refused. Criterion 9's PASS holds only on OG9's generator.
Final minors: m1 the tool stores a no-fit position outside [0, L] (D6 says tools stay in range; the grip clamps); m2 fr-X4 (08's local-space fallback in hostFrameOf), fr-X8 (_Slide.of ignoring keep-a-piece admission), fr-X9 (WallBands._indexAt length bound — 07's code moved in Task 9), fr-X7 (wallsInDocument liveness; hand-built only) survive; m3 docs: criterion 9 caveat, D6 wording, D7/known limits on acute joints (≲12°, folded) shrinking/moving/emptying the span; m4 (07, pre-existing) a 1.6° three-wall node reached by an end drag stores a ~10.4 m zero-width spike along a face line.
Ruling (I1): the admission (D8) checks that EVERY piece left is valid (simple, anticlockwise, triangulable), not only that one remains; an opening whose admission would leave an invalid piece becomes no-fit, as keep-a-piece; before that, clean the back-tracking vertex (a vertex lying on the segment between its neighbours within wallJoin) so valid cuts are not needlessly no-fit. Tests: the reviewer's repro; the 1,620-case sweep → 0 refused; the Window tool places (or no-fits) near a kinked end; the slide grip to that end produces a command or no-fit, never an exception; the neighbour's end drag is not refused. Mutant: the validity check removed → red. Spec D8/D9 amended — cost: a fix wave.
Ruling (m1): the tool clamps a no-fit stored position to [0, L] like the grip; test + mutant. Ruling (m2): tests for fr-X4, fr-X8, fr-X9 (a WT-style test in 07's area is allowed for the moved code — add it in a new file, 07's six files stay unedited), fr-X7 (a small hand-built test). Ruling (m3): results note (criterion 9 caveat updated to the fixed state), spec D6 and D7/known limits. Ruling (m4): recorded as 07 debt in the results note — cost: none now.
Final fix wave dispatched.
Final fix wave: ce8497c — _dropBacktracks (removes a spike tip v when a neighbour lies within wallJoin.linear of the segment v→other neighbour; plain collinear vertices kept, so valid pieces are bit-identical); isValidPiece (simple + CCW + triangulable) in cutsOf's admission, memoised by span; the tool clamps a no-fit position to [0, L]. Tests KJ1 ×2, KJ2 (1,620-case sweep: 79 refused at 5739442 → 0 refused, 0 no-fit), KJ3–KJ5 (shell: Window tool, slide grip, neighbour's end drag), KJ6, WB1, HF8, HF9, SG7, OT1 (ffw-noFitRaw). Wider search 7,776 joints: 238 refused → 0 (560 no-fit; 8 from the validity check at 1e-6° kinks). App +244, engine +1014 -2, render +936 ~1 -7, harness +82, web ✓; OG9 counts bit-identical. Mutants ffw-validPiece (KJ1), ffw-dropBacktrack (KJ1–KJ5), ffw-validNoSimple/NoTri (KJ6), ffw-memoAlways, ffw-noFitRaw/Low (OT1), fr-X4 (HF8), fr-X7 and ffw-obstacleLive (HF9), fr-X8 (SG7), fr-X9 and ffw-startBound (WB1) red; ffw-memoNever equivalent (cost only). Tally 228 fired, 220 killed, 0 survived, 8 equivalent, 8 N/A. Docs: spec D6, D7 (acute/folded limit), D8, D9; results note; mutation log; STATUS. Known limit: hairline kinks (1e-6°) → no-fit.
Scoped re-review dispatched to the final reviewer (review worktree at ce8497c).
Final fix wave: re-review Ready to merge (opus). I1 repro lands; 1,620 sweep 0 refused / 0 no-fit / 1,602 clamped / 0 invalid; UI probes (slide grip, neighbour drag, Window tool) pass; fuzz 4 seeds (23,932 steps) + 16-op variant (6,000): 0 refused, 0 exceptions, 0 drift; _dropBacktracks fires only in opening_kink_test, max area change 7.8e-8 mm², tip-not-endpoint confirmed (fr2-dropEndpoint killed by KJ1); ffw-validPiece, ffw-dropBacktrack, fr-X4, fr-X8, fr-X9 + own fr2-oneSide, fr2-tolZero red. Gates engine +1014 -2, render +936 ~1 -7, harness +82, app +244, web ✓. Minors: the spike is not always refused by the triangulator; removing the endpoint adds the triangle lobe–endpoint–tip. Unverified by the reviewer: the 7,776-joint search and the 50-opening timing (scratch-only figures).
Ruling: both wording minors fixed by the controller (the _dropBacktracks docstring and spec D9) — cost: none.
Closing (controller): results note gains the re-review section; STATUS resume point (the human's macOS build and look, then the merge); ledger archived to docs/superpowers/ledgers/2026-09-25-openings/ as the branch's last commit (progress.md, brainstorm decisions, every brief and carry file, t13-probe.log). Nothing is appended after the archive.
Pushed plan-08/openings with the archive (the human asked for pushes of this branch at Tasks 6, 11 and 16; the archive completes the branch for the human's macOS build).
