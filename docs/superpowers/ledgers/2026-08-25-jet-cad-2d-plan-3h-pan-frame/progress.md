# SDD ledger — plan: docs/superpowers/plans/2026-08-25-jet-cad-2d-plan-3h-pan-frame.md

Spec: docs/superpowers/specs/2026-08-25-jet-cad-2d-plan-3h-pan-frame-design.md (c4ff51e)
Branch: main (no worktree — user consented; same arrangement as Plans 3e, 3f, 3f.1, 3g)
Preflight BASE: f642202

## Preflight scan

### Pairs sharing a file or an interface

| A | B | shared | A produces | B consumes | finding |
|---|---|---|---|---|---|
| 1 | 8 | `STATUS.md` | G3→3i, buffer→3j renumbering | adds Plan 3h section, rewrites "Resume here" | compatible; 8 must not undo 1's renumbering — carried into T8 dispatch |
| 1 | 7 | results note | baseline section + 3 p95 figures | divides by them for criterion 3 | clean |
| 7 | 8 | results note | criteria sections | exit gate summary | clean |
| 2 | 3 | `tile_fixture.dart` / sweep | `fillingGrid` | `of: fillingGrid` | clean |
| 2 | 4 | `tile_cache_test.dart` | one edge test | `group('stripFor')` | disjoint additions, sequential |
| 3 | 4 | `tile_cache.dart` | `debugLastStrip` + `_lastStrip` | `stripFor` top-level | **CONFLICT — see Ruling 1** |
| 3 | 5 | `tile_cache.dart` fallback branch | `_lastStrip = Offset.zero & viewport` | replaces with `_lastStrip = strip` | clean once Ruling 1 applies |
| 4 | 5 | `stripFor` | pure fn + `dart:math` import | calls it in `paintFrame` | clean |
| 5 | 7 | `tile_cache.dart` | narrowed walk | M4 reverts the walk, keeps the clip | clean |
| 6 | 7 | `PAN_STEP` / `runTilePhases` | `kPanStep`, `panStep:` param | `--dart-define=PAN_STEP=30/60` | clean |
| 1 | 6 | device baseline | measured with PAN_STEP absent | default must not rescale (T6 step 3) | clean — T6 asserts `magnitude=7.6158` |

### Self-consistency, per task

| task | its tests vs its code | its files vs later touches | finding |
|---|---|---|---|
| 1 | no tests; device runs only | `STATUS.md` also T8's | clean |
| 2 | `fillingGrid` test needs `captureLiveFrame` — `tile_cache_test.dart` already imports `support/tile_comparison.dart` (:16) | — | clean |
| 3 | Step 5 expects GREEN; Step 4 asserts `strip != Offset.zero & kTileViewport` while Step 3 writes exactly that value | — | **DEFECT — Ruling 1** |
| 4 | M1 drops the clamp → tests 2,3,4 red, test 1 green: matches "three of the four" | — | clean |
| 5 | Files name `tile_fallback_test.dart`; no step edits it | `git add` of it is a no-op | **Ruling 3** |
| 6 | `main.dart:201 _intDefine`, `measurement_rig.dart:330/357/450/523` all verified present at the cited lines | — | clean |
| 7 | device only; sweep must stay green under M4 (stated) | reverts T5 then restores | clean |
| 8 | suites re-run, not quoted | `STATUS.md`, results note | clean |

### Verified against the tree (not assumed)

`captureLiveFrame`, `measureTiledAgreement`, `InkReport` + its five counters, `TileRig{tileDevicePixels,tilesBakedPerFrame,document}`, `paintOnce`, `panBy`, `cache.resetCounters`, `bakeBudgetDevicePixels`, `blitCount`, `liveDrawCount`, `tilesHolding`, `kTileSlack` (`tile_cache.dart:91`), `_drawInto(Canvas,Size,ViewportTransform,DraftPainter,CanvasDrawSink,VerticesDrawSink?,Vector2,void Function(Handle)?)`, `ViewportTransform({required worldToScreenMatrix})`, `const Transform2(a,b,c,d,e,f)`, the fallback branch at `tile_cache.dart:779-786`, and `export 'src/tile_cache.dart'` in the barrel. `dart:math` is NOT imported by `tile_cache.dart` — T4 must add it, as the plan says.

## Rulings

Ruling 1: **Tasks 4 and 3 swap execution order (4 first), and Task 3 records `_lastStrip = stripFor(uncovered, viewport);` in the fallback branch while the walk still passes `viewport` and `quantised` unchanged.** — Task 3 as written cannot go green: its anti-vacuity clause asserts the recorded strip differs from the viewport, and its own Step 3 records exactly the viewport, so every sample fails and Step 5's "both tests pass" is unreachable. The clause is the one the plan argues hardest for (`uncovered` is a bounding rectangle, so `bakeCount`/`liveDrawCount` cannot witness an interior edge), so the fix is to give Task 3 the real strip rather than to weaken the assertion. `stripFor` is pure, has no dependency on Task 3, and Task 5 already consumes it — moving it first only makes the existing dependency direction explicit. Task 3 still gates **today's walk**: the recorded rectangle is metadata, the pixels come from the unchanged full-viewport walk, and Task 5 remains the change that moves behaviour. — **Cost if wrong:** Task 3's instrument records a rectangle the fallback does not yet walk, so `debugLastStrip`'s doc must say "the strip the fallback owes" until Task 5 makes it "the rectangle handed to the fallback"; if `stripFor` were later redefined, the anti-vacuity clause's meaning moves with it.

Ruling 2: **`Rect? _lastStrip` is permitted under "this plan adds no mutable field to `TileCache`".** — The constraint's target, inherited from Plan 3g, is a third *test-only settable knob* alongside `bakeBudgetDevicePixels` (:319) and `:333`. `_lastStrip` is private, written only by the shipped `paintFrame`, and reachable from outside solely through a read-only getter — `tilesHolding` is the named precedent and the plan's own Task 3 text argues this explicitly. — **Cost if wrong:** one private field of state on the cache that no test can set, removable by recomputing the strip in a test, which is the reimplementation trap Task 3 exists to avoid.

Ruling 3: **Task 5's `git add test/tile_fallback_test.dart` stages nothing and that is fine.** — No step in Task 5 edits that file; the criterion 1/1b records the Files block refers to live in the task report and in Task 8's mutation log, which is where the plan actually asks for them. — **Cost if wrong:** none; the numbers are recorded either way.

Task 1: BLOCKED at step 1 — `lowpowermode 1`. Nothing measured, nothing edited, tree clean at f642202.

Ruling 4: **Task 1 is deferred, not abandoned, and Task 5 is gated behind it.** — Low Power Mode is on and `sudo pmset -a lowpowermode 0` changes a machine setting outside this worktree, which is the controller's stop condition, so the user is asked while execution continues elsewhere. Tasks 2, 4, 3 and 6 need no device timing and proceed now. Task 5 must NOT land before Task 1's baseline is taken: the baseline is a measurement of the *unnarrowed* code, and once Task 5 commits there is no unnarrowed tree left to measure. If Low Power Mode is still on when Tasks 2/4/3/6 are done, the fallback is to take criterion 3's baseline from Task 7's M4 arm, which reverts the walk and keeps the clip — behaviourally the pre-Task-5 code. — **Cost if wrong:** taking the baseline from the M4 arm makes criterion 3 a within-run ratio against a mutant rather than against the shipped predecessor; the two differ only by the recorded `_lastStrip`, which costs nothing measurable, but the note would have to say so plainly.

Task 2: implementer DONE_WITH_CONCERNS (commit e23d912, 366 passed / 1 skipped, analyze+format clean). Concern: the brief's predicted non-vacuity mutation (`240.0`->`120.0` in the first loop only) does NOT redden; the two loops each saturate all four edges independently. Implementer found and recorded a mutation that does redden (both loops truncated -> `top edge` fails) and amended its commit message to drop the disproven claim. Reviewer dispatched with that discrepancy named as the thing to adjudicate.
Task 2: review — spec compliant, quality approved. Reviewer reproduced BOTH mutations itself: the brief's single-loop truncation does not redden (confirmed), the both-loops truncation reddens `top edge` (confirmed). One Important finding, and it is against the plan, not the code.

Ruling 5: **Task 2 stands as committed; the fixture's redundancy is recorded, not fixed.** — The reviewer's Important finding is that `fillingGrid`'s two loops each saturate all four edges alone, so the edge property is over-determined and no single-loop edit is guaranteed to redden. Nothing in Plan 3h depends on each loop being independently necessary; what Task 3 needs is that ink reaches within one tile of all four viewport edges, and that is held and independently proven non-vacuous by the both-loops mutation the reviewer reran. Redundancy makes the property more robust, not less true. Rewriting the fixture to make one loop load-bearing would weaken the very property the sweep consumes. — **Cost if wrong:** a future edit that shortens exactly one loop's reach passes this test; the sweep would still see a filled viewport through the other loop, so the failure is invisible rather than harmful. Task 8's mutation log records the corrected mutation.

Task 2: complete (commits f642202..e23d912, review clean, 1 Important ruled on)

Task 4: implementer DONE (commit 18cdf90). M1 reddened 3 of 4 cases, matching the brief. Suite 370 passed / 1 skipped, cross-checked with the JSON reporter after the text reporter dropped and duplicated lines under concurrent isolates. analyze clean; format needed one reflow of the pasted snippet, then clean. Reviewer dispatched with M1 and the four cases' strength named as the things to verify by hand.
Task 4: review — spec compliant, quality approved, no findings. Reviewer refired M1 itself: 3 of 4 red, survivor `pads an interior rect on every side`, count and identity both matching. It also probed for a mutation the four cases miss (sign flip, min/max swap, dimension swap, dropped pad, one-edge clamp) and found none. Accepted the JSON-reporter cross-check of the suite.
Task 4: complete (commits e23d912..18cdf90, review clean)

Task 3: implementer DONE (commit 2783e07, suite 372 passed / 1 pre-existing skip, analyze + format clean). All eight offsets produced a strictly interior strip on the first run -- no substitution needed. One deviation: dropped `import 'dart:ui';` from the new test file because `unnecessary_import` fails analyze here, `Offset` arriving transitively through flutter_test; verified against a stashed baseline that analyze was otherwise clean. Reviewer dispatched with instructions to break at least two anti-vacuity clauses on purpose and confirm the sweep goes red.
Task 3: review — spec compliant as amended, quality approved. Reviewer broke two anti-vacuity clauses by hand and both went red: a budget that bakes the whole pan frame gives `ran no fallback`, and forcing the recorded strip to the viewport gives `left no interior strip edge`. It also verified the `dart:ui` removal in both directions (analyze exit 1 with the import, 0 without) and confirmed the walk is byte-identical to pre-diff with no `canvas.translate`.
Task 3: minor (deferred): the per-offset table in the report came from a throwaway unstaged debug file; no in-tree comment says how it was produced.
Task 3: complete (commits 18cdf90..2783e07, review clean, 1 minor deferred)

Task 6: implementer DONE (commit fa1e408). Step 3 printed `dx=-7.0000 dy=-3.0000 magnitude=7.6158` with PAN_STEP unset -- the sentinel holds. Step 4 threw `Invalid argument (PAN_STEP): not a finite number: "30px"` and failed the R2 test rather than proceeding silently. Deviation: `integration_test/frame_timing_test.dart` also calls `runR2Rig` and needed `panStep: kPanStep,` once the parameter became required; one mechanical line, disclosed. No project.pbxproj rewrite this run. Reviewer asked to check the scaling by hand at NaN/30/60 and to grep for any missed caller.
Task 6: review — spec compliant, quality approved, zero findings. Reviewer hand-checked the scaling at NaN/30/60, read `measurement_rig.dart:357` directly to confirm R2's own pan is untouched, grepped the repo and found exactly two `runR2Rig` call sites (both updated) and one internal `runTilePhases` caller, confirmed `project.pbxproj` is absent from the diff, and ran analyze and format itself.
Task 6: complete (commits 2783e07..fa1e408, review clean)

Ruling 6: **Ruling 4's gate is lifted. Task 5 proceeds now; Task 1 splits.** — Low Power Mode is still on and the user has not answered, but Task 5 needs no device timing at all: its gates are M2 and M3, both widget-suite mutants, which Low Power Mode cannot touch. What Ruling 4 actually protected was the ability to measure an unnarrowed arm later, and Task 7's M4 already reverts the walk while keeping the clip -- behaviourally the pre-Task-5 code. So the baseline is folded into Task 7 and measured in the SAME session as the narrowed arm, which is methodologically stronger than Task 1's separate baseline: same thermal state, same binary lineage, and criterion 3 becomes the within-session ratio it was designed to be. Task 1's other half, the `STATUS.md` renumbering, is Low-Power-Mode-independent and runs as Task 1a. Deferring Task 7 to last also buys the most time for the user to turn Low Power Mode off. — **Cost if wrong:** if Low Power Mode is still on at Task 7, criterion 3's ratio rests on the assumption that the ~24% skew `STATUS.md` records is uniform and multiplicative across both arms, which was observed empirically and never proven; criterion 3b's absolute figure against 16.67 ms is not measurable at all under it and must be recorded as NOT MEASURED with the reason, never smoothed.

Task 5: implementer DONE (commit 853c65a, suite 372 passed / 1 pre-existing skip, analyze + format clean). M3 (`kTileSlack` -> `-20.0`) went RED with `uncoveredPixels: 1642` -- the sweep is a real gate. M2 (`kTileSlack` -> `0.0`) SURVIVED green across all eight offsets on `fillingGrid`, which is the plan's pre-committed second outcome and is recorded as gap H5. Reviewer dispatched on the most capable model with the camera arithmetic to derive by hand, the unnarrowed clip to confirm, both mutants to refire, and one open question: name a mutation of the narrowing itself that the sweep would not catch.
Task 5: review — spec compliant, quality approved, ONE Important finding. Reviewer derived the camera arithmetic by hand and found it an exact identity (`s' = s - strip.topLeft`, `canvas.translate` puts it back), agreeing in sign with `_bake`'s `bake.e + pad` / `into.translate(-pad, -pad)`; confirmed `origin` is passed through unchanged, which is why criterion 2b's Float32 bound did not move; confirmed the clip still takes `uncovered`; refired M3 (`uncovered: 1642`, digit-identical) and M2 (survives), and judged the H5 record honest.

Important finding (answering my item 5): **the sweep catches shrinking the query but cannot catch growing it.** The reviewer replaced `Size(strip.width, strip.height)` with `viewport` -- leaving the translate, the camera offset and `_lastStrip` alone -- and the whole package stayed green at `+372 ~1`. That mutant walks a superset of `uncovered`, so every pixel is still correct, `debugLastStrip` still reports the strip, and the full-frame walk this plan exists to delete is silently back. Call it **M5**.

Ruling 7: **M5 enters the fix loop; Task 5 gets a walk-extent gate before it is marked complete.** — The finding is not a defect in the committed code, it is missing coverage of the plan's headline claim: nothing in the suite distinguishes this narrowing from no narrowing at all, so "the fallback walks the strip" currently rests on reading the source. Plan 3h's only witness for that claim was M4 on device, and Low Power Mode has made the device arm's timings unusable for now -- which turns a merely desirable unit gate into the only living witness the plan would have. `VerticesDrawSink` already exposes `frameTriangleCount` (`:243`) and `resetCounters` (`:246`), so the cost of the gate is small and it needs no new production API. The reviewer's own alternative -- asserting the passed `Size` against `debugLastStrip` -- was rejected: it compares two recordings of the same intent and a mutant that grows the `_drawInto` argument alone would still pass. Only counting what the painter actually emitted observes the walk. — **Cost if wrong:** a triangle-count bound is a cost gate, so it is sensitive to fixture and budget; if it is set too loosely M5 survives anyway and the gap stands, and if too tightly it becomes a flake. The bound must be measured with margin and proved to kill M5, never guessed.

**Low Power Mode is now OFF (`lowpowermode 0`), observed after Task 5's fix round.** Ruling 6 stands unchanged: Task 5 is already committed, so an unnarrowed arm can only be reached through M4 anyway, and measuring both arms in one Task 7 session remains the stronger method. What changes is that criterion 3b's absolute figure against 16.67 ms is now measurable after all and must be recorded rather than marked NOT MEASURED. Task 7 must re-check `pmset` itself before its first run and stop if it has flipped back.

Task 5: fix round 1/5 (1 addressed, 0 open — M5 killed by a triangle-count-ratio gate; commits 853c65a..6cca683). Implementer reports the gate had to be opt-in (`checkTriangleBudget`, default false) because `nearAxisDiagonals`' few long diagonals put the ratio at exactly 1.0 or 0 under correct code, carrying no signal there. `lib/src/tile_cache.dart` verified byte-identical to the pre-fix baseline; only the two test-support files changed.
Task 5: re-review — M5 **ADDRESSED**. Re-reviewer fired M5 itself and got RED with `stray: 0, uncovered: 0, differing: 0, liveTri: 60, tiledTri: 70` -- the pixel gate reads perfect and the triangle gate is what kills it. M3 still RED. It then bracketed the bound rather than trusting the table: `0.84` green, `0.83` red at `Offset(0.0, 37.0)`, so correct code's true worst case is exactly 50/60 = 0.8333 and 0.9 sits centred between it and the 1.0 the mutant produces -- measured, not tuned. Verified the opt-in claim by enabling the flag on criterion 2b and watching correct code fail (`liveTri: 20, tiledTri: 20`). Confirmed `tile_cache.dart` untouched by the fix round. Suite `+372 ~1`, analyze and format clean.
Task 5: minor (deferred): the gate has 4 triangles of headroom at the tightest offset (50 of 54 allowed) -- deterministic, not a flake, but brittle to any future edit of `fillingGrid` or `kFallbackOffsets`; the bracket is recorded in the doc comment.
Task 5: minor (deferred): `checkTriangleBudget` defaults false, so a future third caller of `sweepFallbackAgreement` silently gets no gate. Two call sites today; the load-bearing one enables it.
Task 5: out of scope, carry to Task 8: under M3, criterion 2b also reddens (`differing: 417` against a bound of 60). The plan's M3 record claims only criteria 2 and 2c; the mutation log should record the fuller kill.
Task 5: complete (commits fa1e408..6cca683, review clean after 1 fix round, 2 minors deferred)

Ruling 8: **Task 1 is dissolved into Tasks 7 and 8 rather than run as its own task.** — Ruling 6 already moved its baseline measurement into Task 7's M4 arm, which leaves Task 1 holding two orphans: creating the results note, which Task 7 must now do anyway because it is the first task to write a figure into it, and the `STATUS.md` renumbering, which Task 8 already rewrites the same file for. Dispatching a task whose every step now belongs to a later task would cost a full implementer and review seat to produce two edits that then get edited again. — **Cost if wrong:** the G3-to-3i renumbering lands at the end of the plan instead of the start, so a session that reads `STATUS.md` mid-plan still sees G3 assigned to 3h; the window is this session only, and no other session is running.

Task 7: BLOCKED. Low Power Mode read `0` at the start and `1` mid-task -- the machine came off mains and auto-enabled it at ~79% battery. Amendment item 5 is binding, so the implementer stopped. Nothing mutated, no results note, tree clean at 6cca683. It also discarded several failed device runs before the block: the named `Service has disappeared` flake, one harness-killed run, and two 0%-CPU hangs; it switched to `--dart-define=RIG=pan` to route around repeated R4a/R4b instability (disclosed deviation, R2 alone produces every figure this task needs), and it found and fixed a self-matching `pgrep -f` polling loop that had left runaway shells alive. One clean narrowed run exists at /tmp/3h_narrowed_1.log but n=1, so it scored nothing.

Ruling 9: **Task 8 splits; its machine-independent half runs now as Task 8a.** — The device arm needs mains power, which is the user's to restore, and stalling the whole plan on it buys nothing. Four of the plan's five mutants -- M1, M2, M3 and the reviewer-found M5 -- live entirely in the widget suite and their records do not depend on any timing; the full-suite verification and Task 1's orphaned `STATUS.md` renumbering are equally machine-independent. What must wait for Task 7 is the results note, M4's section, and the exit gate that quotes criteria 3 and 3b. — **Cost if wrong:** the mutation log lands in two commits instead of one and Task 8b must append rather than write, which is churn but not risk; if Task 7 later contradicts something 8a recorded, 8b amends it in the same file.

Task 8a: implementer DONE (commit b7ff8f5). Suites: `jet_cad_2d` 797/0/0; `jet_cad_2d_flutter` 372 passed / 1 skipped, golden tag 35 passed, no PNG regenerated; `dev_harness_2d` analyze + format clean; all three analyze + format clean. Concern: two figures -- M3's criterion 2b `differing: 417` and M5's original green-against-the-whole-package discovery -- are not in `task-4-report.md`/`task-5-report.md` and were taken from the ledger and the brief; the substitution is flagged in the log's closing section and in the report. M4 left as an intentional placeholder. Reviewer asked to refire M3 and M5 and check both figures itself, and to treat any trace of a "Plan 3h is closed" claim in `STATUS.md` as Critical.
Task 8a: review — spec compliant, quality approved, ONE Important finding. Reviewer refired M3 (`differing: 1642, liveTri: 60, tiledTri: 10` on criterion 2/2c, and `differing: 417` against a bound of 60 on criterion 2b -- both exact matches, so the two ledger-sourced figures are confirmed real) and M5 (`liveTri: 60, tiledTri: 70` against 54 on today's gated tree). Re-ran all three suites itself: 797, 372+1 skip, 35 golden, no PNG touched. Confirmed `STATUS.md` carries no close and no exit gate, the results note does not exist, `Verified against` still reads 1b7ea04, and the commit message matches the mandated text.

Important finding: **M3's section is the only one that does not state which suites were run under it.** The plan requires every section to, because Plan 3g's costliest error was inferring suite-wide behaviour without running it. Entering the fix loop.
Task 8a: fix round 1/5 (1 addressed, 0 open — M3's suite scope now recorded from a run, not from expectation; commits b7ff8f5..1f2af06). The run found more than the finding asked for: **7 of 373 tests redden under M3**, including a previously unrecorded failure in `test/invariants/tile_budget_test.dart`'s criterion 12 (`uncovered: 1000, differing: 1000`). Re-reviewer fired M3 itself and reproduced all seven names and figures digit-for-digit, read the new failure's source and judged it mechanistically sound (it drives the same fallback-strip machinery), confirmed the restore and the 372/1 baseline, and confirmed M3's framing is not conflated with M5's historical caveat.
Task 8a: complete (commits 6cca683..1f2af06, review clean after 1 fix round)

**Remaining work is entirely device-bound: Task 7 and Task 8b.** Both need mains power. Machine is on battery with Low Power Mode auto-enabled. Stopping here to ask.

Task 7: implementer DONE (commit 894f3a2). `pmset` read `0` at all 20 checks. **Criterion 3 MISSES: 2.35 against a gate of 2.4** (median M4 p95 37.59 ms / median narrowed p95 15.99 ms). **Criterion 3b misses too**: the narrowed median 15.99 ms is under the 16.67 ms budget but one run of three reads 19.86 ms. Reported as measured, not adjusted. **The widget suite REDDENS under M4** -- `criterion 2 and 2c` fails on the triangle gate at `tiledTri=70` against 54 -- which contradicts the plan's and the existing mutation log's claim that M4 has no unit witness; the M5 fix round's gate, added after the plan was written, catches M4 too. `RIG=pan` used for every run, disclosed in both the note and the report.
Task 7: review — spec compliant, quality approved, criterion 3 referred to the controller. Reviewer traced all 30+ figures to the logs with zero discrepancies, checked log mtimes to confirm the prior attempt's log was overwritten not reused, and fired M4 itself: `+371 ~1 -1`, exactly one failure, `criterion 2 and 2c`, `liveTri: 60, tiledTri: 70` against 54. Key measurements it derived: narrowed p95 {13.43, 15.99, 19.86} CV **19.7%**; M4 p95 {36.14, 37.59, 38.14} CV **2.8%**; the nine pairwise ratios span **1.82-2.84** and straddle 2.4. The M4 arm ran LAST in a demonstrably noisier state (`tile hold` max 27.79/42.88/36.38 against 1.91/2.06/4.17, on a phase M4 cannot touch), so the ordering bias inflates the numerator and the true ratio is plausibly <= 2.35. `RIG=pan` is NOT the explanation: `R2 pan and zoom` is the first `testWidgets`, R4a/R4b run after it, and the workload counters match the spike's exactly. Two Important findings and three minors.

Ruling 10: **Criterion 3 stands as MISS. The gate is not lowered, not restated on another statistic, and the current arrangement is not re-run.** — Three things are true at once and the note must carry all three. (i) The criterion as written was not met: 2.35 against 2.4. (ii) The measurement cannot settle it either way -- at n=3 with a 19.7%-CV denominator the pairwise ratios run 1.82 to 2.84, so re-running this arrangement is a coin flip, which is precisely the behaviour the plan forbids. (iii) **The gate itself was mis-derived, and I derived it.** 2.4 came from the spike's 2.59, whose numerator was the cross-session 43.13 ms clean arm -- and the spec's own argument for making criterion 3 a ratio was to stop cross-session comparison. The reviewer found this independently by tracing where the gap sits: spike-clean 43.13 against M4 37.59 is -12.8%, while spike-clamped 16.66 against narrowed 15.99 is only -4%. The gap is in the numerator, which is the cross-session half. Restating criterion 3 on the mean was rejected: the means are non-overlapping and low-noise (narrowed {3.71, 3.89, 3.27} CV 8.8% against M4 {5.02, 5.17, 5.09} CV 1.5%, about 16.6 ms saved per fallback frame), but choosing a new statistic after seeing the old one miss is gate-moving whatever the arithmetic says. The mean goes in the note as **evidence that the effect is real and large**, labelled as evidence and never as a gate. Re-measuring at n=7-9 interleaved rather than blocked was rejected as this plan's work: it costs hours to test a ruler this same review just showed is bent. It is written into Plan 3i as the way to settle the question if it ever matters. — **Cost if wrong:** Plan 3h closes with its headline criterion unmet, so anyone reading only the criteria table sees a failed plan; the note must therefore carry the mis-derivation and the mean in the same breath as the MISS, or the record misleads in the other direction.

Ruling 11: **Criterion 6 is scored the way criterion 3b is scored: against the threshold, not against the median.** — The reviewer found criterion 6 marked PASS on a three-run median while run 1's raw p95 is 2.77 ms against a 2.5 ms gate. The spec states a threshold, not a median rule, and the note already refuses median-scoring for 3b in as many words. One document cannot use two rules for the same shape of result. — **Cost if wrong:** the criteria table shows one more MISS than a median reading would, on a criterion whose overrun is a single run of three; the alternative is a table whose scoring rule changes row by row, which is worse.
Task 7: fix round 1/5 (4 addressed, 0 open; commits 894f3a2..838c454). Re-reviewer confirmed every pre-existing figure is byte-identical across the diff -- no measurement moved, only prose -- and that every new number is a derivation from already-recorded per-run figures. It read the mean paragraph adversarially and found no reading that credits the mean as a pass: bracketed by "Evidence, not a gate" and "Criterion 3's ruling is, and remains, the p95 MISS". The summary table shows MISS on both 3 and 6. Criterion 6's consistency with 3b is stated in the note, not merely implied.
Task 7: complete (commits 1f2af06..838c454, review clean after 1 fix round)

Task 8b: implementer DONE (commit ff3751f). M4 fired a third time, `+371 ~1 -1`, `liveTri: 60, tiledTri: 70` -- digit-identical to two prior independent runs. Final-tree suites 797 / 372+1 skip / 35 golden, no PNG regenerated, three packages analyze + format clean.
Task 8b: review — spec compliant, quality approved, ONE Important finding. Reviewer fired M4 itself and re-ran all three suites, matching exactly. It skim-tested `STATUS.md` as a hurried session would and found both the TL;DR's and "Resume here"'s first Plan 3h sentence open with the miss. It counted the spec's criteria table independently -- 12 rows, 3 recorded-only (3b, 4, 5) exactly as the spec's own prose states -- and ruled the 9-gate denominator **honest, reproducing the spec's own distinction rather than an invented one**. It also checked `Verified against: 838c454` against this repo's precedent (Plan 3g's `1b7ea04` is likewise the parent code commit of the doc commit that recorded it) and found it correct.

Important finding: **the exit-gate tally contradicts itself.** `STATUS.md` says "7 PASS, 2 MISS" of 9, then two sentences later says criterion 1b is not a binary pass or fail and resolves to gap H5, then names only six as PASS (1, 2, 2b, 2c, 7, 8). The headline silently folds 1b into the passing side. Entering the fix loop.
Task 8b: fix round 1/5 (1 addressed, 0 open; commits ff3751f..ee61d86). Re-reviewer confirmed the three-way split, grepped `STATUS.md` itself for every form of the stale figure and found the only remaining hits belong to unrelated closed plans, confirmed 1b is still described as resolving to H5 rather than to a pass, and re-ran the skim test on the TL;DR and "Resume here".
Task 8b: complete (commits 838c454..ee61d86, review clean after 1 fix round)

**All eight tasks complete.** Dispatching the final whole-branch review over f642202..ee61d86.

## Final whole-branch review (f642202..ee61d86)

Verdict: **production change correct, allocation invariant HOLDS, keep the branch.** Reviewer proved the invariant by inspection -- five constant allocations per fallback frame, zero per entity, zero per tile, strictly cheaper than `_bake`'s existing per-tile trio -- and noted neither invariant test covers `paintFrame`. It re-fired every mutant: M3 `+365 ~1 -7` exact, M4 `+371 ~1 -1` exact, M5 exact, the `kTriangleBudgetRatio` bracket 0.517-0.833 exact. It judged the record honest in both directions and the 9-gate denominator the spec's own.

Three Important and three Minor findings, all in the record or in coverage, none in shipped behaviour:

- **I1 — `canvas.translate(strip.left, strip.top)` has no witness.** Deleting `tile_cache.dart:841` leaves the whole widget suite green (`+372 ~1`). Root cause measured, not reasoned: `fillingGrid` clears the visible box by only ~9-13 screen pixels while the sweep pans 37-71, so the two negative offsets -- **precisely and only the two whose strip does not start at (0,0)**, the only ones where the translate is not a no-op -- bring in canvas beyond the drawing and their pixel assertions are satisfied by a fallback that draws nothing. All six anti-vacuity clauses pass there, because `liveInk` counts the whole frame and no clause looks inside the band the fallback owes. That is the clause-interaction question answered.
- **I2 — M2 is recorded as SURVIVES but reddens three tests on the full widget suite** (`+369 ~1 -3`, the three `stripFor` value tests). The narrow H5 claim is accurate; the bare "survives" is not, and the same three tests are counted for M3 three lines below.
- **I3 — gap H3 claims a bound its arm cannot produce.** Every `nearAxisDiagonals` offset with band ink has `strip.topLeft == (0,0)`, where the translate is a no-op. Same root cause as I1.
- **m1** — narrowing the clip is a surviving mutant argued only in a comment; belongs in the gap list. **m2** — `checkTriangleBudget` defaults false and is now the primary witness. **m3** — `fillingGrid`'s doc comment promises a containment margin the fixture does not deliver.

Ruling 12: **I1 is fixed, not recorded as a gap.** — The reviewer offered both. H5 earned its place: the plan pre-committed to M2's survival with a reasoned argument from `_bake` and F1's history. H6 would be different in kind -- a production line the plan itself added, with no mutation that reddens, in a repository whose stated bar is that a test is worth landing only if a named mutation makes it go red. The line is half the branch's correctness argument. Fixing it costs about two hours of widget-suite work and no device runs. — **Cost if wrong:** extending `fillingGrid` re-lands the fixture every recorded mutant figure was measured on, so M2, M3 and M5 must be re-measured and `kTriangleBudgetRatio` re-bracketed; if the wider fixture changes what M2 does, gap H5 itself may need restating, and the implementer must report that rather than preserve the old finding.

Ruling 13: **All six findings go out as ONE fix wave, as the skill requires, and there is no second.** — Per-finding fixers each rebuild context and re-run suites. Residual findings after the one scoped re-review are adjudicated, not re-dispatched. — **Cost if wrong:** a finding that needs two passes gets parked with a ruling instead of fixed.

Final fix wave: **all six findings ADDRESSED** (commits ee61d86..122b6e3). Re-reviewer deleted `canvas.translate` itself and got `uncovered: 2224, differing: 2224` at `Offset(-41.0, 0.0)`, `+371 ~1 -1` -- the line has a witness. Production code byte-identical: `git diff ee61d86..122b6e3` over both lib trees is empty. Results note `+17 -0`, device sections untouched. Suites 797 / 372+1 / 35 golden, no PNG. It reproduced the ratio bracket endpoints (`0.9375` RED, `0.94` GREEN) and M5's floor of exactly 1.0000, and judged criterion 2b's double opt-out honest.

One new Important and three residuals.

Ruling 14: **The anti-vacuity clause's three false claims are corrected in wording; the clause is not re-pointed at `uncovered`.** — The re-reviewer proved the clause measures `debugLastStrip`, the **padded** strip, not `uncovered`, which is what the fallback is clipped to; the pad reaches outward into already-blitted area that carries ink, so the clause would NOT have caught finding 1. The witness for finding 1 came from the widened fixture. Three places say otherwise -- `tile_comparison.dart:79`, `:353-355`, `tile_fixture.dart:298` -- and they are wrong as written. This is the session's own recurring defect appearing inside the documentation of its fix, which makes leaving it the worst available outcome. But re-pointing the clause at `uncovered` needs a production-side accessor `TileCache` does not have, and adding one at the close of the plan to serve a clause whose job the fixture already does would be scope the plan never argued for. Correct the words; record the limitation as a named gap. — **Cost if wrong:** the clause stays weaker than a reader would assume from its name, so a future fixture that again fails to clear the sweep's pan distance would not be caught by it; the gap bullet is what stops that being a surprise.

Ruling 15: **The two off-by-one suite counts are corrected in the mutation log.** — The re-reviewer measured M2 `+370 ~1 -3` and M3 `+366 ~1 -7` against logged `+369` and `+365`, twice each, and re-fired M1 to `+368 ~1 -4` matching the log exactly -- so it is not environmental drift. "Never synthesize test output" is a non-negotiable of this repository, and a logged count that no rerun reproduces is exactly what that rule exists to prevent, whatever its magnitude. — **Cost if wrong:** two numbers in a log are one off; the reason to fix them anyway is that the log's whole value is that its numbers can be reproduced.

Ruling 16: **`STATUS.md`'s branch range is updated to end at the last code commit.** — It cites `f642202..838c454` in five places. The re-reviewer distinguished this from the benign `Verified against` self-reference: the repo's own rule is to point at the last **code** commit, and that is now `122b6e3`, so `git log f642202..838c454` silently hides the fixture widening, the re-bracketed constant and M6. — **Cost if wrong:** none; it is mechanical.

Ruling 17: **`kTriangleBudgetRatio` stays at 0.97 with its two triangles of headroom.** — The re-reviewer confirmed the bracket is real, that triangle counts are integer and deterministic headless so tightness is not a flake source, and that a bound above 1.0 would still kill M5 at six of eight offsets with more slack. But a gate whose threshold exceeds 1.0 asserts that the narrowed walk may emit *more* geometry than the full-frame walk, which is the opposite of what the gate means. Principle over headroom is the right trade here, and the implementer chose it deliberately. — **Cost if wrong:** if a future fixture edit moves correct code's worst ratio above 0.97 the gate flakes; the bracket's endpoints are recorded in the doc comment so the next person re-brackets rather than guesses.

Ruling 18: **These four are dispatched as one record-only correction, not as a second fix wave.** — The skill forbids a second fix wave because per-finding fixers rebuild context and re-run suites. This dispatch touches no code, runs no new measurement beyond re-firing two mutants to get the counts right, and corrects errors the re-review already demonstrated. Verification afterwards is a targeted check, not another full review. — **Cost if wrong:** one more short cycle at the end of the plan.

Record fix: commit bbb83e0. H7 added, the four (plus two more it found) mis-stating locations corrected, the stale hash fixed in all five places including `Verified against`, `lib/` byte-identical to 122b6e3, suites green.

**Tie-break on the M2 count, measured by the controller.** The implementer's re-fire gave `+369 ~1 -3`, matching the log and disagreeing with the re-reviewer's `+370 ~1 -3`; it correctly refused to overwrite and recorded the discrepancy instead. I fired M2 myself (copy aside to /tmp/tc.tiebreak, perl on lines 115-120, restore from the copy, confirmed `+372 ~1` after): **`+369 ~1 -3`**, same three `stripFor` names. The arithmetic settles it independently of any run: 369 passed + 3 failed + 1 skipped = 373, which is the suite's size; the re-reviewer's `+370 -3 ~1` would imply 374 tests, one more than exists. **The log's original figures are correct and Ruling 15 is withdrawn.**

Ruling 19: **Ruling 15 is withdrawn; the mutation log's M2 and M3 counts stand as originally recorded, and the dated discrepancy notes are resolved rather than left open.** — Two independent re-fires plus a total-count identity all give the log's figures; the re-review's were one high and cannot be reconciled with a 373-test suite. Leaving "flagged discrepancy" notes in the log would tell a future session the numbers are unsettled when they are settled. — **Cost if wrong:** if a later run reproduces 370, the resolution note is the thing that gets amended, and it names its evidence so that is cheap.
