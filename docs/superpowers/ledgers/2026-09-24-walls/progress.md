# SDD ledger — plan: docs/superpowers/plans/2026-09-24-walls.md

Branch plan-07/walls, cut from spec-07/walls at f2daba5 (main 22957d1 + spec dfb6969 + plan f2daba5), inside the session worktree `.claude/worktrees/walls`.
Spec: docs/superpowers/specs/2026-09-24-walls-design.md (revision 1; reviews skipped on the human's instruction, 2026-09-24).
Merge is the human's; never push; delivery by git bundle.
Environment: Linux x86_64 cloud container, Flutter 3.47.2 / Dart 3.13.2 at /root/flutter. macOS build and the look are the human's (Ruling 07-6).

## Branch-point counts (this container)

- engine: +951 -2 (the two Linux-only hash tests in generate_document_test.dart; Ruling 07-7)
- render: +923 ~1 -7 (the five standing text_ladder rungs 1-5, plus text_lod_ladder rungs 1-2, Linux-only)
- harness: +82
- app: +69
Ruling (07-7, extended): on this container the render layer's standing failures are seven — text_ladder rungs 1-5 (standing everywhere) and text_lod_ladder rungs 1-2 (Linux-only goldens) — cost if wrong: a real text_lod regression here would hide; the human's macOS gate still checks it.

## Tasks
Task 1: implementer reports 24c5e5b; engine +959 -2 (standing), render +923 ~1 -7 (standing); M-07m fired red on RG3, own RG2 mutant red; 4 deviations reported (RG5 fill edit throws StateError not GeneratedGeometryError; RG5 boundary error names the fill; RG7 added case on an existing object; RG2 checks triangulation by area). Review pending.
Task 1: review Needs fixes — Important: matchbound mutant survives (RegionRect's plain child is a LINE, so boundary exclusion from plain matching is unpinned; walls' centreline is a plain POLYLINE); Important: foreign/dangling boundary guard (owner check) unkilled. Minor: drift() can throw on malformed fills/bad regions (doc it); Minor: no two-objects-gain-regions-in-one-command determinism test.
Task 1: Ruling: SetEntityGeometryCommand on a region's fill throws the command's own StateError, not GeneratedGeometryError (the command refuses before the D6 guard runs; changing it is out of scope) — cost if wrong: a caller catching only GeneratedGeometryError sees a StateError; the UI never offers the edit. Spec amended in Task 11.
Task 1: Ruling: a boundary edit's GeneratedGeometryError names the fill (06 D6 reports the lowest touched generated handle; the boundary edit touches its fill) — cost if wrong: cosmetic.
Task 1: Ruling: RG7's added-region case runs on an existing object (a failed create legitimately leaves handleSeed raised, 06 D4 step 8) — cost: none.
Task 1: Ruling: RG2 checks re-triangulation by equality with triangulationFor and by area (a rectangle's index list is width-independent) — cost: none.
Task 1: fix round 1/5 dispatched (both Importants, both Minors).
Task 1: fix round 1 committed c285764 (RegionRect gains an open POLYLINE child; RG9 foreign/dangling boundary; RG10 two-object determinism; drift() doc). Implementer reports engine +961 -2; matchbound -> RG1/2/3/4/6/10 red; owner -> RG9 red; unsorted closure -> RG10 red. Re-review pending.
Task 1: re-review Approved (matchbound -> RG1/2/3/4/6/10 red; owner -> RG9 red; unsorted closure -> RG10 + N6 red; M-07m still RG3 red; engine +961 -2 reverified).
Task 1: complete (commits 49cbc68..c285764, review clean after 1 fix round)
Task 2: implementer reports ed501cc; engine +965 -2 (standing), render +923 ~1 -7 (standing); M-07o -> NC1/NC2 red (179400 = 2n(n-1)); closure=seeds -> 18 red incl. N2; view-reads-before -> NC2/NC3 red. Review pending.
Task 2 NC4 (JIT, median of 5, as reported by the implementer; before = unmodified lib, run 1):
  before n=100 line 2.32 ms, move 3.46 ms | n=300 line 7.24, move 6.26 | n=600 line 19.35, move 19.56
  after (file alone) n=100 line 0.59, move 0.96 | n=300 line 1.25, move 1.23 | n=600 line 1.95, move 2.16
Task 2: note — NC2's < 10n bound is (2 + closure - 1)(n-1); a move changing ~8+ neighbours would exceed it though still O(k·n); NC2 uses a small move (closure 6, 2093 tests).
Task 2: review Approved; 9 extra mutants killed (per-survey memo, before-reach snapshot, deleted/created objects, ordering); M-07o re-fired red (179400).
Task 2: minor (taken into Task 3's dispatch): NC2 asserts exact (1 + closure) × (n − 1) — kills a disabled memo (2392 < 3000 survives today); NC3 asserts neighbours both lost and gained.
Task 2: minor (deferred, 06 debt): no test pins "overlap by more than Tolerance.standard.linear" (touching boxes are not neighbours) — mutant T survives.
Task 2: minor (not taken): debugOverlapTests exported through the barrel — precedent EntityStore.debugRawTextAt; @visibleForTesting guards use.
Task 2: note for the results note — "before" NC4 medians are the implementer's run on unmodified lib (n=600 ~19-23 ms); the reviewer's M-07o stand-in measured 12.63/12.72 ms at n=600; JIT, container noise; state the method, not a single speed-up factor.
Task 2: complete (commit ed501cc, review clean)
Task 3: implementer reports 9bf8706 (diagnose, DG1-DG3) + 5e3b715 (NC2 exact count, NC3 lost-and-gained); engine +968 -2, render +923 ~1 -7 (standing); mutants never/noneighbours/unsorted/noguard/eager/stuck red on DG; nomemo now red on NC2 (2093 vs 2392). Deviations: DG1 undo compares world segments (handleSeed never moves back); DG2 uses two types (one store already sorts); DG3 added (plan table said DG1-DG2). Review pending.
Task 3: review Approved with 4 minors — (1) REAL BUG, pre-existing pattern from 06: nested _applying guard: an inner call's finally clears the outer guard (drift/diagnostics/ParametricEdit.apply); reviewer proved an execute lands; (2) survey/reach outside the guard in diagnostics() survives a mutant; (3) diagnostics() now propagates client reach/diagnose exceptions — undocumented; (4) memoised neighbour list is mutable by clients.
Task 3: Ruling: all four minors taken now (1 is a correctness bug, 2-4 are one-liners) in one fix commit, not deferred — cost if wrong: none.
Task 3: fix round 1/5 dispatched.
Task 3: fix round 1 committed a30ad24 (save/restore guard at drift/diagnostics/apply; DG4 reach-in-guard; DG5 + G11 nested scenarios; unmodifiable neighbour lists; doc). Implementer reports engine +971 -2. apply() restore unpinned (unreachable with guard set). Scoped re-review pending.
Task 3: re-review Approved (nested scratch test green; diagnostics/drift '= false' -> DG5/G11 red; apply-site mutant equivalent: apply is entered only via execute, whose expander throws when armed; survey-above -> DG4 red; modifiable -> NC3 red; invariant tests unedited 22957d1..a30ad24; engine +971 -2).
Task 3: Ruling: the apply() save/restore is defensive and unpinned — its mutant is equivalent (the expander refuses entry while armed) — cost if wrong: none reachable.
Task 3: complete (commits 9bf8706..a30ad24, review clean after 1 fix round)
Task 4: implementer reports f8b129d; app +91 (69 + 22), analyze/format clean, web build ✓; engine +971 -2 unchanged. WG14: holes 152/20000 (0.76%), fallback 54/49887 (0.11%). M-07a,b,c,d,f,i,j,k,n,r all red on their named tests; extras X-hub (WG12), X-anchor (WG16), X-samedir (WG17), X-degenerate (WG18), X-holeowner (WG14) red. M-07n at outline's call site is equivalent (lobes() already removes every reachable pinch); WG12 kills it on a spliced ring.
Task 4: deviations reported: (1) wedge corners intersect faces through the node point (hub), not each end's own point — spike's rule gives 1.19% holes with rotated groups vs 0.76%; (2) WG16-WG18 added; (3) WG14 also asserts one reporter per hole naming every member; (4) WG11 fixture splayed (Q4's short wall never falls back); (5) hole for a wall owning dropped lobes at both ends = union of both nodes' members; (6) same-direction test within wallJoin.angular.
Task 4: plan/spec issues reported: Ruling 07-3's "2 × wallJoin.linear" bound is wrong (counterexample 0/0.9/1.8 µm); spike note was not on this branch (copied: commit below); WG14 < 1.5% would not catch a regression to the spike's corner rule (WG12 does); D5.3 "node point never a vertex" holds only for inserted vertices; spike note's "485 refusals before, 0 after" credits simplification, but lobe splitting is what removed them (implementer could not reproduce any simplifyRing effect with the final code).
Task 4: spike note + images copied onto plan-07/walls from 45aecb6 (controller). Review pending.
Task 4: review Needs fixes — Important I1: D6's shallow-T clamp untested (removing it, or max->min, survives). Minors: m1 owner-sorts-first case untested (label-0 drop survives); m2 several-T lowest-handle rule untested; m3 M-07n comment wrong on facts (reviewer's probe: simplifyRing changed 567/700,076 raw rings, never changed a triangulation outcome); m4 run-on comment line; m5 literal "Tolerance.standard" in wall.dart comment trips Task 10's grep; m6 WG11 check comment misleading. Reviewer re-fired M-07a,b,c,d,f,i,j,k(3 variants),n,r: all red.
Task 4: Ruling: deviation (1) — wedge corners intersect faces placed through the node's reference point (the anchor/hub end), not each end's own endpoint — measured 0.76% vs 1.19% holes with per-wall rotated groups; bitwise agreement preserved (every member picks the same hub). Spec D5.2 amended in Task 11 — cost if wrong: corners shift by at most wallJoin.linear.
Task 4: Ruling: deviations (2)-(4), (6) accepted as reported — cost: none.
Task 4: Ruling: deviation (5) — a wall owning dropped lobes at both ends reports the union of members; Task 5 decides D12's one-entry-per-node shape — cost if wrong: one diagnostic for two nodes (not observed).
Task 4: Ruling: Ruling 07-3's bound is corrected to "the anchor end within wallJoin.linear of every other member" (counterexample 0/0.9/1.8 µm) — plan text and spec D4 amended in Task 11 — cost: none.
Task 4: note for Task 11: D6's "485 refusals before, 0 after" credits simplification; the lobe split is what removed them; after deviation (1) the pinched-node path is unreachable, simplifyRing is a guard (M-07n at the call site is equivalent for triangulation, not for stored bits).
Task 4: fix round 1/5 dispatched (I1, m1-m6).
Task 4: fix round 1 committed 845cf68 (WG19 shallow T; WG6 twice incl. owner-sorts-first; WG20 lowest-handle T; comments m3-m6). Implementer reports app +93; X2/X3 -> WG19 red, X15 -> WG6 red, X-teelowest -> WG20 red, M-07k still red. Scoped re-review pending.
Task 4: re-review Approved (X2/X3 -> WG19, X15 -> WG6, teelowest + first-found -> WG20, M-07k x3 -> WG6; app +93; grep Tolerance.standard empty).
Task 4: minor (folded into Task 5): wall.dart:11 comment is 94 chars; reflow.
Task 4: complete (commits f8b129d, 845cf68; review clean after 1 fix round)
Human (2026-09-24): send an interim bundle of plan-07/walls right after Task 6 is reviewed, for a first look (draw walls; no grips, no panel yet). The final bundle follows at the end.
Task 5: implementer reports 26f634e (WallType, catalog.dart, box rename, wall_fixture doc helpers, WR1-WR12); app +105, analyze/format clean, web ✓; engine +971 -2 unchanged. M-07d -> WR2 (one-ulp case), M-07e -> WR6 control, M-07g -> WR3, M-07h -> WR2 (each half red too), M-07l (view-level form) -> WR8 drift, M-07s -> WR11. Deviations: handles 1300/2600; WR2 two cases; WR3 L in one compound; WR4 asserts 3 of A's 4 points fixed (B left-justified); WR8 400-thick trio; M-07l only expressible at the view level; WR9 hole hard-coded (seed 7707 trial 161); WR12 26 walls; WR7 canon(sortNodes); box rename done here (plan put it in Task 6). Spec issues: D12 "one entry per node" vs union-at-both-ends; M-07l definition; WR4 wording. Review pending.
Task 5: review Approved; reviewer fired 19 mutants (M-07g, M-07h x3, M-07s x2, M-07d, M-07e x2, M-07l variant, forward toLocal, untransformed reach, world centreline, hole from every member, degenerate [], no fallback, WR12 with drift blinded) — all red except two equivalent: region emitted after the centreline (the planner orders regions first) and self in `others` (classify drops self).
Task 5: minor (folded into Task 6): WR4 asserts kids unchanged across the thickness edit; WR12 compares outlines exactly (reviewer found them bitwise equal); a widget-level Delete for Review Focus 2.
Task 5: minor (Task 11): stale boxCatalog/installBoxes mentions in STATUS.md:338 and spec :73/:122.
Task 5: Ruling: D12 is one diagnostic per code per wall; a wall owning holes at both ends names the union of both nodes' members (WG14 pins at most one reporter per node) — spec amended in Task 11 — cost if wrong: one entry for two rare nodes.
Task 5: Ruling: M-07l is defined at the view level (a wall squaring its end where a neighbour's own outline falls back) — outline() alone cannot express it — Task 9 logs it so — cost: none.
Task 5: complete (commit 26f634e, review clean)
Task 6: implementer reports e882160 (render/engine: per-tool snap mask, nearest marker), 594a942 (Wall tool on W), 7f1c173 (WR4 kids unchanged, WR12 exact, RF2 delete through the shell). App +113, web ✓; render +923 ~1 -7 (standing); engine +972 -2 (standing); harness +82. Mutants M1-raw, M2-reset, M3-noclose, M4-noW, M5-transform, X1-X5, E1, R1, R2, K1 red; WR12 exact-vs-1e-6 has no distinguishing mutant (drift() catches build-order dependence first).
Task 6: Ruling: resolveDragPoint gains an optional snap mask (default kDragSnapMask) and PlacementTool a snapMask getter; the Wall tool adds `nearest` — spec D11's "nearest makes a T" was undeliverable under 03 D8's mask; the nearest marker gains a glyph — separate commit e882160; spec amended in Task 11 — cost if wrong: one engine parameter and one render getter, defaults unchanged.
Task 6: Ruling: the tool keeps the chain's first point (for closing) and last point; self-snap to the last point only once a wall is down (M-05x's rule) — plan text amended in Task 11 — cost: none.
Task 6: review pending.
Task 6: review Needs fixes — Important I1: `nearest` also snaps to a wall's outline faces, so a click in the outer half of a band lands on a face → free end or a stem crossing through, not a T (reviewer probe: clicks 60/95 mm off a 200 centre wall → classify=Free). WT3 clicked only 20 mm off. Minors: m1 mid-chain settings change unpinned (capture-at-start mutant survives); m2 runtime refusal mid-chain unpinned (handle hoisted before the permission check survives); m3 render defaults (other tools don't snap nearest) unpinned. Reviewer verified snap-mask change is allocation-free (query_allocation_test covers nearest via SnapMask.all), defaults preserved at both call sites, invariant tests unedited.
Task 6: Ruling (I1): the Wall tool maps an accepted point (and its rubber-band hover point) that lies inside a wall's band — between that wall's faces, within its length — onto that wall: onto the nearer centreline endpoint when within one thickness of it along the centreline, else onto the centreline (a T); several walls: the lowest handle. App-side, O(walls) per click/hover, off the frame path; no further engine/render API — cost if wrong: a click meant to start a free wall inside another wall's band joins it (the user sees the joint and can undo).
Task 6: fix round 1/5 dispatched (I1, m1, m2, m3).
Task 6: fix round 1 committed a2da5bd (WallTool._joinBand: band → endpoint within one thickness, else centreline projection; lowest handle; applied to accepted and hover points; WT3 extended incl. justified host H2; WT2 mid-chain settings; WT8 runtime + structure-only denial; L7 render default). Implementer reports app +114, web ✓, render +924 ~1 -7 (standing). Mutants nomap/wrongside/noend/nolength/capture/hoist/needs/basenearest red.
Task 6: Ruling: band-joining is object snapping, so it is gated on object snap (F3): with object snap off the Wall tool maps nothing — consistent with every other snap — cost if wrong: a user with F3 off draws a free wall inside a band (they turned snapping off).
Task 6: ad1a199 band joining gated on object snap (WT9; 'gate removed' -> WT9 red). App +115, web ✓. Scoped re-review pending.
Task 6: re-review Needs fixes — I1, m1-m3 verified fixed (probe ±60/±95 mm → Tee; 8 mutants red; L7 red). New Important I2: _joinBand runs on every hover, O(walls), rebuilding WorldWalls (accumulatedTransform, vectors, records) per wall per move, even with no chain pending — 214 µs per hover at n=600 (JIT). Minors: m4 lowest-handle rule unpinned; m5 snap marker at the unmapped point while band/commit go to the centreline (cosmetic); m6 a click near an X crossing tees onto the lower handle with the stem end ~50 mm inside the other wall's band (drift/diag empty).
Task 6: Ruling (I2): hover returns early when no chain is pending; the world walls are cached and rebuilt only on a document change; a hover is then an O(walls) scan over cached scalars that allocates nothing in steady state — O(n) per move is acceptable at floor-plan scale (hundreds of walls) when allocation-free; measured cost recorded — cost if wrong: a large plan's hover costs a linear scan (index cull is the follow-up).
Task 6: m4 taken now; m5 deferred to the look/Task 11 (cosmetic); m6 recorded for Task 11's spec amendment.
Task 6: fix round 2/5 dispatched.
Task 6: fa94a83 (hover early return; Float64List cache of world walls rebuilt on doc.changes or own commit; allocation-free scan; WT10-WT13; debugBandScans). Implementer reports app +119, web ✓; WT12 n=600: 5.46 us per hover (band scan 2.48 us) vs 214 us before; mutants no-invalidate -> WT11, no-early-return -> WT10, highest-first -> WT13 red. Scoped re-review pending.
Task 6: re-review Approved (WT12 5.44 us/hover at n=600; invalidation covers execute/undo/redo/load/purge via doc.changes; bitwise endpoints; hover allocation-free; listener lifecycle sound; mutants no-invalidate/no-op listener/no early return/highest-first red).
Task 6: minors (folded into Task 7's dispatch): m1 a click in the same synchronous task as an edit uses the stale cache (unreachable from the UI; fix: mark stale in accept); m2 own-commit invalidation unpinned. m3 document-switch re-subscribe untested (unreachable; late final document) — no action.
Task 6: complete (commits e882160, 594a942, 7f1c173, a2da5bd, ad1a199, fa94a83; review clean after 2 fix rounds + 1 controller ruling)
Controller smoke test (after Task 6, before the interim bundle): web build with local CanvasKit (`--no-web-resources-cdn`, the proxy CA is not trusted by Chromium for gstatic), Playwright + container Chromium: the app loads; W draws a closed room and a T with mitred corners — but the wall bands render WHITE (paper colour), not black. Cause: layer 0 is IndexedColor(7); aciToRgb(7) = 0xFFFFFF; strokes evidently get ACI 7's contrast-with-paper swap, fills do not; no earlier feature drew a ByLayer fill (drawn regions use kDraftFillColor). No test renders a wall's colour. Spec D3's "a black band on white paper" is false as built.
Controller: defect fix dispatched before the bundle (Task 6b): apply ACI 7's contrast rule to fills as to strokes (preferred) or give wall regions an explicit colour; pixel-level render test required.
Task 6b: e13d2cb — walls paint black via an explicit generated colour (Generated/Generated.region gain `color`, used only when a child is added; draftRecord gains optional color; kWallColor = TrueColor(0x000000)); RG11 + WP5 (pixel test in the shell) red on old code. Implementer found NO ACI-7 contrast rule exists anywhere: aciToRgb(7) = 0xFFFFFF pinned by style_resolver_test and the 3f text spec; the sample plan's lines are explicit TrueColor(0x202020).
Controller smoke re-run (e13d2cb, Chromium): walls black, room mitred, T clean. CONFIRMED PRE-EXISTING DEFECT (outside 07): a line drawn with L and a box drawn with B render white (ByLayer on layer 0) — barely visible off-page, invisible on white paper. For the human: fix/<slug> branch.
Controller smoke finding: walls drawn with grid snap on come out skewed — the Wall tool's widened `nearest` snaps to the sample plan's hatch lines near each click, beating the grid. Band joining (fa94a83) now does the T/node joining for any point inside a band, so `nearest` is redundant and harmful.
Ruling: the Wall tool drops `nearest` (default snap mask); band joining alone makes T's and nodes; the unused per-tool mask plumbing from e882160 is removed unless still used — spec D11 amended in Task 11 ("a click inside a wall's band joins it") — cost if wrong: a click just outside a band's edge no longer snaps onto the face (it never should have).
Task 6b: review of e13d2cb + the nearest removal dispatched together.
Human (2026-09-24): the white-ByLayer-drafting defect (Plan 05 tools, 06 boxes) goes on a separate fix/ branch AFTER 07; not in plan-07/walls. Record it in 07's results note and STATUS as found-and-deferred.
Task 6c: f751d4a — Wall tool back to the default snap mask; e882160's mask plumbing and nearest marker removed (files identical to 26f634e); WT3 via band joining with grid on; WT14 walls land bitwise on grid points beside unrelated lines. Implementer reports engine +972 -2, render +924 ~1 -7, app +121, web ✓; 'nearest back' -> WT14 red; band joining removed -> WT3/WT11/WT13 red. Review of e13d2cb + f751d4a dispatched.
Controller smoke (f751d4a): walls black; with object snap ON the room is still slightly skewed; with F3 OFF the room lands exactly on the grid, crisp mitres, clean T. So the skew is 03 D8's standard object snap (endpoint/intersection of the sample plan's hatch within the aperture beats the grid), not a Wall-tool defect; my 'nearest caused it' diagnosis was partial. f751d4a stands on its own merit (band joining makes every joint; nearest would pull clicks onto any line). Recorded for the look: dense hatch makes object snap grab hatch features — a user-visible behaviour, not 07's.
Task 6b/6c: review Approved (defaults unchanged; colour survives undo/redo/load/regrow; WP5 real pixel check; 5 package files byte-identical to 26f634e; mutants red). Minor m1 (folded into Task 7): WT3's T foot loosened to 10 mm lets a 3 mm foot shift survive — pin exactly. Note for Task 11 spec: record colours are never rewritten by the planner; a future kWallColor change needs a migration.
Task 6: complete incl. smoke fixes (…, e13d2cb, f751d4a).
Interim bundle for the human: plan-07/walls at f751d4a.
Task 7: implementer reports 7ca4f32 (render: ObjectGripProvider seam, GripDrag.reshapeObject with node+children revalidation, {components, geometry} capabilities, preview paint) + c234103 (app: WallGrips; main wiring; EG1-EG5; shared wall_shell helpers; WT3 foot exact). Render +931 ~1 -7 (standing), app +126, web ✓, engine +972 -2. Mutants M-07q, provider ignored, revalidation dropped (x3 parts), capabilities (x2), world-not-local, + 6 extras, WT3 foot shift — all red.
Task 7: Ruling: a grip drag does not band-join (a grip dropped inside another wall's band makes no T; endpoint snap still makes nodes) — sharing the Wall tool's band cache would need the moving walls excluded and F3 plumbed through the provider — spec D11 amended in Task 11 — cost if wrong: a T is made with the Wall tool, not by dragging a grip.
Task 7: Ruling: WallGrips' preview caches the joined-end set per grip (visual only; drag() recomputes at release); degenerate walls at a corner are not moved (D2); with geometry allowed and components denied an object grip is drawn but a press stays a click — cost: cosmetic.
Task 7: review pending.
Task 7: review Approved (render +931 ~1 -7, app +126; no-provider path unchanged — route-everything mutant turns planner_grips_test red; M-07q, revalidation x3, permission, degenerate skip, sticky preview all red). Minors: m1 no-op object drag (guard at grip_drag.dart:275) untested — an out-and-back drag would record an empty undo step without it; m2 preview for-in loop (select_tool.dart:776) → indexed.
Task 7: controller slip — Task 6's minors m1 (mark the band cache stale in accept) and m2 (own-commit invalidation test) were NOT in Task 7's brief; carried into Task 8's dispatch with Task 7's m1/m2.
Task 7: complete (commits 7ca4f32, c234103; review clean).
Human (2026-09-24): pushing plan-07/walls to GitHub authorised (bundle import failed on their side). Pushed at c234103 (Tasks 1-7). main and other branches NOT pushed. Later pushes of plan-07/walls are covered by this authorisation only for this branch.
Human (2026-09-24): push plan-07/walls again once Task 8 is reviewed and approved.
Task 8: implementer reports 6563c7d (Wall section, pinned targets for Box and Wall, tool-settings mode) + e109314 (carried: accept marks band cache stale; WT15-17; EG1 no-op drag; indexed preview loop). App +137, web ✓; render +931 ~1 -7. Mutants M-07p (Wall and Box), justification ignored, ≤0 accepted, runtime editable, settings not shared, no stale-mark, own-commit invalidation (combination), no-op guard — all red.
Task 8: deviations: WS7 split Wall/Box; the Wall tool's settings take precedence over a selected wall while the tool is active; Handle.none as the settings target; a field re-pins after a commit (no double commit on Enter then blur).
Task 8: problems reported (outside the plan): (1) after Enter in any panel field the shell's letter shortcuts are dead until the canvas is clicked (pre-existing, page panel too); (2) in tool-settings mode, typing a thickness then clicking mid-chain without Enter uses the old thickness for that click's wall (focus-loss commit lands a microtask later). Review pending.
Task 8: review Needs fixes — I1 (real bug, 07 scope): tool-settings thickness typed then a mid-chain canvas click without Enter builds the wall with the OLD thickness (canvas accepts on pointer-down; focus-loss commit lands later; TapRegionSurface sits above the canvas Listener) — reproduced 200 vs 400. I2: "discard text for a dead pinned target" unpinned (WS7's second half passes because an unmounted field never notifies). I3: "re-pin after commit" unpinned (Enter then blur would double-commit C's text onto A). Minors: m1 a live pinned target loses typed text if its section hides while focused (unreachable from the UI); m2 dead `_read` branch. Problem (1) verdict: shortcuts dead after Enter reproduced; pre-existing (page panel identical); bites 07 mid-chain (Esc dead; only recovery is a canvas click that adds a wall); one-liner fix: onEditingComplete unfocus(previouslyFocusedChild).
Task 8: Ruling (I1): in tool-settings mode every valid keystroke writes the settings (onChanged); side effect accepted: erasing leaves the last valid prefix, not a revert — the field always shows the real setting — cost if wrong: a partial number becomes the setting while typing.
Task 8: Ruling (problem 1): take the one-liner for the Selection panel's fields now (07's tool mode needs Esc after Enter); the Page panel's field goes to the post-07 fix/ branch with the white-ByLayer defect — cost: none.
Task 8: m1 documented in _commit's comment; m2 removed. Fix round 1/5 dispatched.
Task 8: fix round 1 committed dcbc831 (onChanged in tool mode; onEditingComplete + onTapOutside unfocus(previouslyFocusedChild); WS3/WS6/WS7 cases B1, B6; m1 doc; m2 removed). App +137, web ✓. Mutants I1/I2/I3/item4/tap-outside red. Deviation: onTapOutside also changed (a plain unfocus clears the route's focus history so Enter later fell back to the scope). Scoped re-review pending.
Task 8: re-review Approved (I1/I2/I3/Enter mutants red; onTapOutside previouslyFocusedChild justified — plain unfocus clears focus history and overrides the canvas's focus request; F2 SE8/SE10 green; page_panel.dart unchanged).
Task 8: minor (deferred to the post-07 fix/ branch with the page panel): m3 after both Box fields, tapping panel background refocuses Width (focus history); reviewer's _handBack fix recorded.
Task 8: complete (commits 6563c7d, e109314, dcbc831; review clean after 1 fix round).
Pushed plan-07/walls at dcbc831 (human's instruction: push after Task 8).
Human (2026-09-24): pulled dcbc831 on macOS; the app runs and the Wall panel shows up. (Partial look only; the full owed look — Wall tool, joints, end grips, Wall section on macOS, Chrome, Firefox — is still criterion 13.)
Human (2026-09-24): push plan-07/walls again when Task 11 is done.
Task 9: implementer reports 8c87cb4 (plan-07-mutation-log.md): 51 fired, 51 killed, 0 survived, 4 equivalent recorded, 1 N/A; gates engine +972 -2, render +931 ~1 -7, harness +82, app +137, web ✓. Finding: M-07i at the node non-owner cap (wall_geometry.dart:326) is not killed by WG6 (the spec's named fixture) — killed only by WG8 in the same file.
Task 9: Ruling: land the proposed fixture as WG21 (three-way node, 200 centre at 10°, 115 left from 130°, 150 centre at 250°, no face through the node: the node point is on no ring), re-fire M-07i against it, amend the log entry (WG6 → WG6/WG21) — cost: one test.
Task 9: WG21 landed 330797c; M-07i re-fired red on WG21:719; log updated (tally unchanged 51/51). App +138, web ✓.
Task 9 review + Task 10 (invariants and greps; no code change unless a grep finds a defect) dispatched to one independent reviewer.
Task 9: review Approved (11 mutants / 14 runs re-fired exactly as logged; tally 51 = 25 named-mutant runs + 26 extras; equivalents argued; WG21 real — node point on no ring vertex or edge). Minors: m1 "no face through the node point" wording (w2 left-justified: its zero face runs through the node) — reword in Task 11; m2 log says every fire at dcbc831 but WG21 re-fire ran on the test-added tree (lib identical).
Task 9: complete (commits 8c87cb4, 330797c).
Task 10: done by the reviewer, read-only: invariant tests unchanged since 22957d1 and passing (query +5, paint +3); no Tolerance.standard in wall files; no handleSeed.next in parametric/ (grep needs -r — the plan's command is broken); render lib diff = grip_cache, grip_drag, select_tool; engine lib diff = parametric/ + drafting.dart (draftRecord color); 28/28 commits carry the trailer; 0 analysis_options.yaml commits; the dart:ui grep matches 3 pre-existing doc comments (grep too loose; an import-anchored grep is empty).
Task 10: Ruling: no code change; the two greps are corrected in the results note — cost: none.
Task 10: complete.
Task 11: implementer reports a582de3 (results note, spec amendments D3/D4/D5.2/D5.3/D6/D8/D10/D11/D12/mutant table/evidence, plan amendments 07-3/Task 6/Task 10 greps, log wording, WG21 title, STATUS, roadmap 07 + 00). Gates engine +972 -2, render +931 ~1 -7, harness +82, app +138, web ✓. Note: the ledger's '0/0.9/1.8 µm' is nm (wallJoin.linear = 1e-6 mm) — the note uses multiples of wallJoin.linear. Review pending.
Task 11: review Needs fixes (text only) — I1 attribution: the note and STATUS credited all three fix/ deferrals to the human; only the white ByLayer defect was the human's; the page-panel focus and Box m3 were controller rulings. m1 D11 amendment omitted the per-click stale mark; m2 T2 barrel-export ruling missing; m3 STATUS Plan-06 file list needed the Plan 07 rename note.
Task 11: corrections applied by the controller in 5cda53d (docs only); re-review Approved.
Task 11: complete (commits a582de3, 5cda53d).
Pushed plan-07/walls at 5cda53d (human: push when Task 11 is done).
Final whole-branch review dispatched (opus).
Human (2026-09-24): push plan-07/walls again when the final review is done.
Final review (opus): With fixes. Gates engine +972 -2, render +931 ~1 -7, harness +82, app +138, web ✓; ~11,700-edit differential fuzz (grids, rotated groups, T/X/node/clamp/short-wall): 0 drift, 0 undo/redo mismatches; re-fired M-07g/m/p/q/r red; cross-cutting X1/X2/X3/X15/X8 red, X8b survived (m5).
Final: Important I1 — outline() checks simplicity in world space, then generate maps the ring to group-local space; a folded mitre spike passing by ~1e-10 can self-cross after the rotation's rounding → _checkRegion throws ArgumentError → the edit (rotate / grip / panel) is refused with an uncaught exception (document untouched). 6/10,530 acute-L rotations; 1/2,346 fuzz edits. Reviewer's fix (tested, reverted): after mapping to local, if !isSimpleCcw(local) use the free rectangle computed in local space; diagnose reports wall.fallback for it; a test rotating an acute L.
Final: minors — m1 thickness accepts sub-ulp values → ArgumentError escapes the panel/tool; m2 a loaded degenerate wall made whole gets its region above its centreline (D3/D9 claim); m3 surplus-fill removal lacks the owner check (malformed load could remove a foreign boundary); m4 _editable checks only editCapability, commit also needs components; m5 reshapeObject's root guard unpinned (unreachable).
Final: Ruling — fix wave takes I1, m1 (thickness must exceed wallJoin.linear in panel and tool; _commit catches a refused edit, reverts, re-pins), m3 (owner check on surplus, StateError), m4 (_editable requires components too), m2 (D3 amendment only), m5 (documented as defence in depth, no test — unreachable); results note criterion 6 updated — cost: none.
Final fix wave dispatched.
Final fix wave: d9c8609 (I1 _localOutlineOf in generate+diagnose; isWallThickness > wallJoin.linear in panel and tool; _commit catches refused edits; _ownBoundaryOf on surplus; _editable requires components; reshapeObject comment) + b201296 (results note final-review section, criterion 6, log 57/57, spec D3/D6/D8/D11 amendments). Gates engine +972 -2, render +931 ~1 -7, harness +82, app +142, web ✓. Uncommitted sweep: 8,892 acute-L rotations — 41 threw at 5cda53d, 0 after.
Final fix wave: finding — m3's premise didn't hold: at 5cda53d RemoveEntityCommand already refused removing a foreign boundary (2 fills / owner mismatch) and _run rolled back; the new check refuses earlier with a clear message; the mutant is killed only via RG9's message — equivalent for the document. Logged as killed with that explanation.
Final fix wave: Ruling — _setJustification also catches a refused edit (consistency with _commit; a malformed load would otherwise throw from the toggle) — one-line follow-up before the re-review — cost: none.
Final fix wave: 55b55cb _setJustification catches a refused edit; WS9 extended; FW-toggle-catch red (WS9 line 726); app +142. Log/results to record it (58/58) in the controller's closing docs commit. Scoped re-review by the final reviewer dispatched.
Final re-review (opus, scoped to d9c8609..55b55cb): Ready to merge. Gates at 55b55cb engine +972 -2, render +931 ~1 -7, harness +82, app +142, web ✓; I1 evidence re-run (~9,400 fuzz edits, 10,530 acute-L rotations: 0 throws, 0 drift); fix-wave mutants re-fired red; m3's data-loss premise withdrawn (equivalent for the document). Optional minor: _commit/_setJustification catch every StateError, including _run's double-failure one — recorded as debt in the results note, not fixed.
Closing docs (controller): mutation log 58/58 with FW-toggle-catch; results note re-review section and debt line; STATUS counts, final-review line and Resume here.
Ledger archived to docs/superpowers/ledgers/2026-09-24-walls/ as the branch's last commit (this ledger holds progress.md only: briefs, reports and review packages were passed inline this plan and were not kept as files). Nothing is appended after the archive.
